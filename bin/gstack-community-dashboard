#!/usr/bin/env bash
# gstack-community-dashboard — compatibility shim for nexus-community-dashboard
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/nexus-community-dashboard" "$@"

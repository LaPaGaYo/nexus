#!/usr/bin/env bash
# gstack-learnings-search — compatibility shim for nexus-learnings-search
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/nexus-learnings-search" "$@"

# Factory Host Facade

Active source: `.factory/`

Planned target: `hosts/factory`

This file is documentation only. Factory host output remains generated under
`.factory/` for compatibility with existing installs.


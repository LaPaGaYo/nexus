# Nexus Host Facades

This directory is a navigation facade for repo taxonomy v2.

Active host source paths remain:

- `CLAUDE.md` and `.claude/`
- `.agents/`
- `.gemini/`
- `.factory/`

Generated host output and installer targets remain in the active paths above.
The `hosts/` tree records the intended maintenance taxonomy without changing
runtime discovery behavior.


{
  "schema_version": 1,
  "run_label": "nexus-v1.1.2-operator-attested-early-exit",
  "hypothesis": {
    "action": "make the canonical bin detect at the CLI seam (lift defaultExecutionSelection() to a const before await invocation.handler at nexus.ts:~139, gate with isInsideClaudeCodeHost()) that the resolved route's generator IS the calling session, and early-exit with exit 0 plus a structured operator_attested contract instead of letting the run reach the nested claude -p spawn",
    "users": "operators running /build from inside an active Claude Code session with execution_mode=local_provider, primary_provider=claude, provider_topology=single_agent",
    "outcome": "they complete the governed build in-session with zero silent hang and zero fabricated provenance fields (dispatch_command/receipt null, provenance_kind=operator_attested)",
    "evidence": "three recorded occurrences across two projects on a ~15-day cadence (idea-brief Sources 1-2-4) plus PR #160's merged diff proving the nested spawn is structurally pointless when the caller is the worker; the in-session Claude already produces verifiable test output and a git diff"
  },
  "success_criteria": [
    "Running /build inside Claude Code with local_provider/claude/single_agent yields adapter-output.json provenance_kind=operator_attested, exit 0, <5s, zero claude -p spawn (process audit shows no defaultRunCommand spawn).",
    "status.json + adapter-output.json have dispatch_command/receipt/actual_route absent or null when provenance_kind=operator_attested; schema accepts the attested variant without fabrication.",
    "/review reading the artifact surfaces provenance_kind as an observable value in its review input JSON; it does not silently treat operator_attested as spawned_provider.",
    "operator_attested artifact embeds verbatim git diff --stat HEAD; /review does not set ready:true when that embedded diff-stat is empty (operator-capturable, no ledger base-ref needed).",
    "With NEXUS_ALLOW_NESTED_CLAUDE=1 set, assertNestedClaudeAllowed (merged local.ts:807) returns early, spawn path runs, CLI early-exit skipped; regression test asserts both branches.",
    "review.ts:540-546 does not throw on null actual_route when provenance_kind=operator_attested; sets provenance_consistent from the attested contract; /review completes with a verdict (B3 survival branch)."
  ],
  "non_goals": [
    "Not a v1.2 provenance schema redesign; only the minimal optional provenance_kind field, the CLI-seam early-exit, and the single review.ts:540-546 B3 survival branch.",
    "Not closing the v1.1.1 topology-partial streaming gap (agent_team/subagents missing stream_to_tty + banner).",
    "Not changing governed_ccb or any non-claude / non-single-host dispatch.",
    "Not auto-committing the worktree; the empty-diff sibling bug is only detected as a guardrail.",
    "Not /review //qa //ship early-exit in this slice; /build is the recurrence surface."
  ],
  "out_of_scope": [
    "Retroactive migration/annotation of historical operator-attested artifacts (novelWriter SP2 + prior); a separate ungated one-time pass.",
    "SKILL.md human-facing restructuring beyond the minimal Step 9 early-exit branch.",
    "/review re-grading logic that acts on provenance_kind — weighting/advisory/verdict-change (v1.2). v1.1.2 includes only the B3 survival branch (don't crash, set provenance_consistent).",
    "Re-running or re-validating the novelWriter SP2 run itself."
  ],
  "acceptance_criteria": [
    "Given an operator in a Claude Code session with local_provider/claude/single_agent at SKILL.md Step 9, when they run ./bin/nexus build, then the bin exits 0 in <5s without spawning claude -p, emits an operator_attested contract JSON naming artifact paths, and adapter-output.json carries provenance_kind=operator_attested with null dispatch_command/receipt.",
    "Given the same session with NEXUS_ALLOW_NESTED_CLAUDE=1 set, when they run ./bin/nexus build, then the early-exit is skipped and the existing spawn path runs unchanged.",
    "Given an operator_attested build whose embedded git diff --stat HEAD capture is empty, when /review consumes the artifact, then it does not report ready:true for that build.",
    "Given an operator_attested build with null actual_route, when /review runs, then review.ts:540-546 does not throw and records a verdict (B3 survival branch)."
  ],
  "open_questions_to_plan": [
    "B2 RESOLVED: seam confirmed at lib/nexus/cli/nexus.ts ~139 — lift defaultExecutionSelection() (already imported, line 10; called at ~144) to a const before await invocation.handler({...}) and gate with isInsideClaudeCodeHost(). /plan confirms exact line post-#160-merge offsets.",
    "Does the bin write a skeleton status.json so the ledger advances atomically, or does the operator author all 7 files; how is the embedded git diff --stat HEAD capture enforced (schema-required field vs /review-side check).",
    "B3 scope line: /plan confirms the review.ts:540-546 branch is survival-only (no throw, set provenance_consistent) and does NOT alter the pass/fail verdict.",
    "Confirm subagents/agent_team stay deferred and the predicate excludes them from early-exit (assertNestedClaudeAllowed still throws for them, which is acceptable interim)."
  ],
  "provenance": {
    "upstream_idea_brief": "docs/product/idea-brief.md",
    "framing_topology": "agent_team",
    "perspectives_synthesized": ["engineering-feasibility", "risk-backward-compat", "product-scope-sequencing"],
    "related_pr": 160,
    "related_pr_status": "merged origin/main @ 857253cc8 (2026-05-18)",
    "related_issue": 159,
    "red_team_disposition": "B1 resolved by merge; B2 resolved by re-grounding on merged main; B3 confirmed, folded into scope as review.ts:540-546 survival branch"
  }
}

---
name: autoplan
preamble-tier: 1
version: 0.1.0
description: |
  Transitional alias for the canonical Nexus /plan command. Routes through Nexus and
  does not own separate contract, artifact, or transition logic. (nexus alias)
allowed-tools:
  - Bash
  - Read
---

{{PREAMBLE}}

# /autoplan — Transitional Alias

This alias routes to `/plan`.
This alias does not own separate contract, artifact, or transition logic.
Use `/plan` as the canonical lifecycle entrypoint.

Run:

```bash
{{NEXUS_RUN_COMMAND:autoplan}}
```

Then read:

- `.planning/nexus/current-run.json`
- `.planning/current/plan/status.json`

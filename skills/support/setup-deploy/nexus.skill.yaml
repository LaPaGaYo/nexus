schema_version: 1
name: setup-deploy
summary: Configure deploy settings for /land-and-deploy. Detects platform.
intent_keywords: ["setup deploy", "configure deployment", "deploy platform setup"]
lifecycle_stages: [ship]
classification:
  namespace: nexus_support
provenance:
  author: nexus
  version: "1.0.0"

schema_version: 1
name: benchmark
summary: "Performance regression detection via browse daemon: Core Web Vitals + page load + resource sizes."
intent_keywords: ["benchmark performance", "check web vitals", "performance regression", "load time check"]
lifecycle_stages: [qa, ship]
classification:
  namespace: nexus_support
provenance:
  author: nexus
  version: "1.0.0"

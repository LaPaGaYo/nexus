schema_version: 1
name: land-and-deploy
summary: Land + deploy + verify production health via canary.
intent_keywords: ["land and deploy", "ship to prod with health check", "merge and deploy"]
lifecycle_stages: [ship]
classification:
  namespace: nexus_support
provenance:
  author: nexus
  version: "1.0.0"

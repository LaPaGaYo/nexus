schema_version: 1
name: handoff
summary: "Governed handoff: approve route + provider lane before /build."
intent_keywords: ["approve the route", "hand off to build", "lock in the routing", "approve provider lane"]
lifecycle_stages: [handoff]
classification:
  namespace: nexus_canonical
provenance:
  author: nexus
  version: "1.0.0"

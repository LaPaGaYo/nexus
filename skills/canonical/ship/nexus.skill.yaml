schema_version: 1
name: ship
summary: Pre-merge readiness gate + branch decision tree.
intent_keywords: ["ship it", "merge to main", "release the change", "gate this branch on pre-merge checks"]
lifecycle_stages: [ship]
classification:
  namespace: nexus_canonical
provenance:
  author: nexus
  version: "1.0.0"

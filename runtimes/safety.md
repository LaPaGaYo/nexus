# Safety Runtime Facade

Active sources:

- `careful/`
- `freeze/`

Installed compatibility paths:

- `$NEXUS_ROOT/careful/bin/check-careful.sh`
- `$NEXUS_ROOT/freeze/bin/check-freeze.sh`

Planned targets:

- `runtimes/safety/careful`
- `runtimes/safety/freeze`

This file is documentation only. Hook helper binaries stay in their current
roots until installed skill compatibility is explicitly migrated.


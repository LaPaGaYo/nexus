# Track F Phase 5 Brief: Darwin upgrade for `/frame` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**

> **Historical context note (issue #148):** This document was authored
> against an earlier repository layout. Some commands and file paths
> below reference the pre-#142 flat `scripts/` layout — current paths
> live under `scripts/{build,skill,eval,repo,resolvers}/`. The contents
> are kept verbatim for provenance; substitute current paths when
> running commands from this document today.
**Darwin baseline:** 73.5/100 (per audit-v3 § "/frame (canonical)").
**Darwin target:** 84–86/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/frame (canonical)" (lines 205–235).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md` (commit `635a7fa`).
**Predecessor:** Track F Phase 1 (`/discover`, commit `cb50dc4`) — STRUCTURE + VOICE EXEMPLAR.
**Issue:** #117.

---

## Why F.5 sits where it does

`/frame` enters Track F with a comparatively healthy baseline (73.5) because Iron Laws Law 1 (seven-section PRD) and Law 2 (If/Then/Because hypothesis template) are already executable templates. The remaining gap is the same Darwin gap every Track E skill has:

| Dim | Score (current) | Issue |
|---|---|---|
| D1 Frontmatter | 6/10 → 4.8/8 | Description names the trigger ("when discovery is complete enough") but no example user phrases |
| D2 Workflow Clarity | 5/10 → 7.5/15 | No numbered "how to run framing" — Iron Laws are content-shape, not procedure |
| D3 Edge Cases | 8/10 → 8/10 | Already strong (seven-section contract + readiness gate fail conditions) |
| D4 Checkpoint Design | 6/10 → 4.2/7 | Inherited preamble; Law 3's `not_ready` verdict is implicit, no explicit operator gate |
| D5 Instruction Specificity | 9/10 → 13.5/15 | Strongest of canonical Track E; Iron Laws name pass/fail conditions verbatim |
| D6 Resource Integration | 7/10 → 3.5/5 | References `decision-brief.md`, `prd.md`, `design-intent.json`, `status.json` |
| D7 Overall Architecture | 8/10 → 12/15 | Cross-skill anchoring is tight (`/plan` Law 1, `/review`'s evaluation contract) |
| D8 Real-World Performance | 8/10 → 20/25 | Reader can produce a complete PRD by following Law 1 mechanically; can't yet execute step-by-step |

The lift is procedural: tell the operator *what to do in what order* without rewriting Iron Laws.

---

## Surface map

### File modified

```
skills/canonical/frame/SKILL.md.tmpl       ← edit (frontmatter + workflow + typical prompts)
skills/canonical/frame/SKILL.md            ← regenerated by gen:skill-docs (claude)
.factory/skills/nexus-frame/SKILL.md       ← regenerated by gen:skill-docs --host factory
.agents/skills/nexus-frame/SKILL.md        ← regenerated by gen:skill-docs --host codex
.gemini/skills/nexus-frame/SKILL.md        ← regenerated by gen:skill-docs --host gemini-cli
```

### File NOT modified

- `lib/nexus/stage-content/frame/*` — runtime content; not skill prose
- Iron Laws section (lines 21–70 of `SKILL.md.tmpl`) — preserved verbatim
- All other canonical/support skills — out of scope for this phase
- Any resolver code in `scripts/gen-skill-docs.ts`
- Tests — none added per phase constraint

### Frontmatter changes

`description` gets trigger phrases appended. `allowed-tools` already has `AskUserQuestion` from Track E and is left alone.

---

## Action 1 — Numbered "How to run /frame" workflow

Insert a new `## How to run /frame` section between the Iron Laws section (ends at the close of Law 3) and the Operator Checklist. The workflow does NOT restate Iron Laws — it references them by name (per Law 1, per Law 2, per Law 3). The order tracks the natural framing flow: read upstream → draft PRD → write hypothesis → verify gate → write artifacts.

**Voice (Nexus-native, matches F.1 exemplar):**

```markdown
## How to run /frame

These steps execute one `/frame` run. Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply.

### Step 1 — Read upstream context

Read `docs/product/idea-brief.md` from the prior `/discover` run. The brief carries:

- The Look Inward / Look Outward / optional Reframe sections
- The named user segment, observed pain with cost, evidence sources
- The hypothesis hint that `/frame` Law 2 sharpens
- The ≥3 open questions that `/frame` is responsible for answering

If `idea-brief.md` is missing or returned `not_ready` from `/discover`, framing has nothing to scope. Route back to `/discover` rather than guessing.

If a prior closeout produced `next-run-bootstrap.json` or `RETRO-CONTINUITY.md`, read those too — they may carry framing-relevant context (prior decisions, deferred scope).

### Step 2 — Draft `prd.md` with all seven sections

Open `docs/product/prd.md` and draft each of the seven sections required by Law 1, in order:

1. Problem statement (who hurts, how, how much)
2. Hypothesis (the If/Then/Because — see Step 3)
3. Success criteria (observable, falsifiable conditions)
4. Non-goals (≥3 concrete things this work will NOT do)
5. Risks (≥3 specific failure modes with mitigations)
6. Alternatives considered (≥1 rejected alternative + reason)
7. Decision rationale (why this scope, why now)

Sections may be drafted in any order, but ALL seven must end up non-empty before Step 6. A wishlist is not a PRD.

If unsure where to start, draft Problem statement first (Step 1's idea-brief feeds it directly), then Non-goals (cheapest pre-empt of scope creep), then iterate.

### Step 3 — Write hypothesis as If/Then/Because

Per Law 2, the hypothesis MUST be of the form:

> If we [action], then [users] will [outcome] because [evidence].

Each clause:

- **Action** — concrete verbs (build, ship, change, deprecate). Not "improve" or "enhance."
- **Users** — named segment, not "all users." Reuse the segment from `idea-brief.md`'s Look Outward.
- **Outcome** — observable behavior or system change, falsifiable in the same way Law 1 success criteria are.
- **Evidence** — what makes the team believe this hypothesis is true now. Pull citations from `idea-brief.md`.

If two teammates can't agree on the [evidence] clause, the framing is not yet ready for `/plan`. Iterate or route back to `/discover` for more evidence.

### Step 4 — Define `out-of-scope` distinct from non-goals

Law 3 #4 requires `out-of-scope` non-empty AND distinct from non-goals:

- **Non-goals** — things this work intentionally won't do (e.g., "won't add multi-tenant support")
- **Out-of-scope** — things adjacent users may expect that this work doesn't address (e.g., "doesn't change the existing billing flow even though users may assume framing changes touch it")

Both sections may overlap in spirit but the audience differs: non-goals talk to the team's discipline; out-of-scope talks to the user's expectations.

### Step 5 — Write ≥1 user story with explicit acceptance criteria

Law 3 #3 requires at least one user story in the form:

> Given [context], when [action], then [observable outcome].

This is what `/plan` Law 1 reads to translate framing into bite-sized tasks with verifiable acceptance criteria. Without it, `/plan` has nothing concrete to plan against.

A framing pass with ten stories but none in Given/When/Then form fails Law 3 #3 — one rigorous story beats ten vague ones.

### Step 6 — Verify framing readiness gate

Before writing `status.json`, check ALL five Law 3 conditions:

1. Success criteria are observable and falsifiable (Law 1 #3 + Law 3 #1)
2. Non-goals lists ≥3 concrete items (Law 1 #4 + Law 3 #2)
3. ≥1 user story has explicit Given/When/Then acceptance criteria (Law 3 #3)
4. `out-of-scope` section non-empty (Law 3 #4)
5. Hypothesis present per Law 2, all four clauses populated (Law 3 #5)

If any fail, surface the choice via `AskUserQuestion`:

> "Framing readiness gate failed on [list which of the 5]. Should I (a) iterate within `/frame` and try to satisfy the failing checks, (b) route back to `/discover` to gather missing input, or (c) continue and write `status.json` with verdict `not_ready`?"

The operator picks; record the choice in the decision brief.

### Step 7 — Write `decision-brief.md`

`docs/product/decision-brief.md` summarizes the framing in 1–2 pages:

- The hypothesis (per Law 2)
- The chosen scope and rejected alternatives (Law 1 #6 — the Alternatives Considered section, condensed)
- The decision rationale (Law 1 #7 — why this scope, why now)
- Any open framing questions that survived to `/plan`

The decision brief is what `/review` reads later when judging whether the work matches intent. It is also the artifact the operator points stakeholders at when explaining "what we framed and why."

### Step 8 — Write `design-intent.json`

`.planning/current/frame/design-intent.json` captures the framing's machine-readable contract:

- `hypothesis` (the four-clause If/Then/Because, parsed)
- `success_criteria` (array of observable conditions)
- `non_goals` (array of ≥3 items)
- `out_of_scope` (array)
- `acceptance_criteria` (array of Given/When/Then statements from Step 5)

Downstream stages read this file rather than re-parsing prose. Keep it consistent with `prd.md` — they are two views of the same framing.

### Step 9 — Write status

Run the canonical command (in the Routing section below). It writes `decision-brief.md`, `prd.md`, `design-intent.json`, and `status.json`. The advisor record carries the verdict (`ready` or `not_ready`); `/plan` reads it.

If verdict is `not_ready`, `/plan` cannot proceed. The operator either iterates within `/frame` or routes back to `/discover`.
```

This Action lifts D2 Workflow Clarity from 5/10 to ~8/10 (+3 raw, ×15 = +4.5 weighted) and D8 Real-World Performance from 8/10 to ~9/10 (+1 raw, ×25 = +2.5 weighted). Combined: ~+7 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus framing command. Writes repo-visible framing artifacts and stage
  status through Nexus-owned normalization and governance. Use when discovery is
  complete enough to define scope and success criteria. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus framing command. Writes repo-visible framing artifacts and stage
  status through Nexus-owned normalization and governance. Use when the user says
  "frame the scope", "write the PRD", "define what we're building", or when
  discovery is complete enough to define scope and success criteria before /plan
  can sequence tasks. (nexus)
```

Lifts D1 Frontmatter from 6/10 to ~8/10 (+2 raw, ×8 = +1.6 weighted).

Frontmatter character count: well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the Run block, before the Completion Advisor footer (matches F.1 placement).

**Voice (matches F.1 exemplar):**

```markdown
## Typical prompts

These are example user requests `/frame` handles well. The skill is invoked and produces `decision-brief.md`, `prd.md`, `design-intent.json`, and `status.json`.

### Prompt 1 — Direct framing request after discovery

> "We finished `/discover` for the telemetry idea. Frame the scope so we can plan the build."

`/frame` walks Step 1–9. Step 1 reads `idea-brief.md` from the prior discovery. Step 2 drafts the seven PRD sections; Step 3 sharpens the hypothesis hint into a full If/Then/Because per Law 2. Step 5 writes ≥1 Given/When/Then user story. Step 6 verifies the readiness gate; if it fails, the AskUserQuestion surfaces the iterate/route-back/not_ready choice. Verdict written to `status.json`.

### Prompt 2 — Reframing a vague success metric

> "Our success criterion is 'users will love the new flow' — frame this properly so review can evaluate it later."

Step 2 forces the seven sections; Law 1 #3 rejects "users will love it" as vague and demands an observable command/metric/artifact. The skill pushes the operator toward something like "task completion rate >85% on the X flow, measured via session telemetry over a 14-day window." Step 6's gate fails on Law 3 #1 until the criterion is concrete; the AskUserQuestion routes the choice.

### Prompt 3 — Deciding between non-goals and out-of-scope

> "What goes in non-goals vs out-of-scope for this PRD?"

Step 4 names the distinction explicitly: non-goals are intentional team discipline ("won't add multi-tenant"), out-of-scope is user-expectation management ("doesn't change billing even though users may assume so"). Both are required non-empty by Law 3 #2 and #4 respectively. The skill helps the operator draft both sections rather than collapsing one into the other.

These prompts test that `/frame` produces a complete framing artifact with hypothesis, falsifiable success criteria, and the readiness-gate verdict that `/plan` can act on.
```

Lifts D8 from 8/10 toward 9/10 (already partially captured by Action 1; cumulative gain ≈ +1 raw, ×25 = +2.5 weighted, mostly attributable here as marginal lift on top of Action 1).

---

## Action 4 — AskUserQuestion gate

Bundled into Action 1 Step 6: when the framing readiness gate fails on any of the 5 Law 3 conditions, the operator is surfaced the iterate / route-back / not_ready choice. No additional placement needed.

This satisfies the brief's Action 4 requirement (gate at Law 3 readiness checks). Choosing Law 3 over Law 1 for the gate placement because:

- Law 3's 5-condition gate is the natural decision point — it already produces a `not_ready` verdict, so the AskUserQuestion makes the implicit branch explicit
- Law 1's seven sections are sequential drafting, not branching — adding an AskUserQuestion per missing section would fire too often and become noise
- Law 3 is the framing edge with `/plan`; the gate is where governance lives

Lifts D4 Checkpoint Design from 6/10 to ~7.5/10 (+1.5 raw, ×7 = +1.05 weighted).

---

## Projected Darwin score after F.5

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6 | 8 | +2 | +1.6 |
| D2 Workflow Clarity | 5 | 8 | +3 | +4.5 |
| D3 Edge Cases | 8 | 8 | 0 | 0 |
| D4 Checkpoint Design | 6 | 7.5 | +1.5 | +1.05 |
| D5 Instruction Specificity | 9 | 9 | 0 | 0 |
| D6 Resource Integration | 7 | 7 | 0 | 0 |
| D7 Overall Architecture | 8 | 8 | 0 | 0 |
| D8 Real-World Performance | 8 | 9 | +1 | +2.5 |

**Projected total:** 73.5 + 9.65 ≈ **83.2/100**.

The RFC's projection was 84–86; F.5 lands at the lower end of that range, on plan. The dimensions Iron Laws already covered well (D3, D5, D7) are not double-counted.

---

## Implementation procedure

### Step 1: Edit `skills/canonical/frame/SKILL.md.tmpl`

Apply 3 changes:

1. Frontmatter description: append trigger phrases (Action 2)
2. Insert `## How to run /frame` between Iron Laws and Operator Checklist (Action 1, includes Action 4 gate in Step 6)
3. Insert `## Typical prompts` after the `Run` bash block, before `{{NEXUS_COMPLETION_ADVISOR:frame}}` (Action 3)

### Step 2: Capture Iron Laws diff baseline

```bash
sed -n '21,70p' skills/canonical/frame/SKILL.md.tmpl > /tmp/frame-iron-laws-before.md
# After edits, re-extract the same lines and diff:
sed -n '<new-line-range>p' skills/canonical/frame/SKILL.md.tmpl > /tmp/frame-iron-laws-after.md
diff /tmp/frame-iron-laws-before.md /tmp/frame-iron-laws-after.md   # MUST be empty
```

### Step 3: Regenerate all 4 hosts

```bash
bun run gen:skill-docs                      # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 4: Verify

```bash
bun run skill:check                                                                                  # clean across all 4 hosts
git grep -n "How to run /frame" skills/canonical/frame/SKILL.md                                      # 1 hit
git grep -n "Typical prompts" skills/canonical/frame/SKILL.md                                        # 1 hit
git grep -n "frame the scope" skills/canonical/frame/SKILL.md                                        # 1 hit (frontmatter trigger)
git grep -n "Framing readiness gate failed" skills/canonical/frame/SKILL.md                          # 1 hit (Step 6 gate)
```

### Step 5: Commit + push to `track-f-phase-5-frame`

```bash
git add docs/architecture/track-f-phase-5-frame-brief.md
git add skills/canonical/frame/SKILL.md.tmpl
git add skills/canonical/frame/SKILL.md
git add .agents/skills/nexus-frame/SKILL.md
git add .factory/skills/nexus-frame/SKILL.md
git add .gemini/skills/nexus-frame/SKILL.md
git commit -m "skills/canonical/frame: Track F.5 Darwin upgrade"
git push -u origin track-f-phase-5-frame
```

Issue #117 is NOT closed in this phase per task constraint — the operator closes it after merge.

---

## Acceptance criteria

This phase is complete when:

1. Frontmatter description carries trigger phrases ("frame the scope" / "write the PRD" / "define what we're building")
2. `## How to run /frame` section inserted between Iron Laws and Operator Checklist with 9 numbered steps
3. Step 6 includes explicit `AskUserQuestion` text for framing readiness gate failure (the iterate / route-back / not_ready three-way choice)
4. `## Typical prompts` section with 3 examples added after the Run block, before the Completion Advisor footer
5. Iron Laws section preserved verbatim (diff against pre-edit Iron Laws is empty)
6. `bun run skill:check` clean across all 4 hosts (claude, codex, factory, gemini-cli)
7. Branch `track-f-phase-5-frame` pushed to origin; no merge to main
8. Issue #117 NOT closed (per task constraint)
9. Commit message references Track F.5

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First do X, then do Y" | "Step 1 — Read upstream context. Read `docs/product/idea-brief.md` from the prior `/discover` run." |
| AskUserQuestion framing | Vague "ask user" | Explicit options: "AskUserQuestion: 'iterate within `/frame` / route back to `/discover` / continue with `not_ready` verdict?'" |
| Cross-skill reference | "Then plan it" | Iron-Law-aware: "`/plan` reads the framing verdict from the advisor record. If `not_ready`, `/plan` cannot proceed." |
| Workflow vs Iron Law | Either or both | Both with explicit relationship: "Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply." |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "frame the scope", "write the PRD", "define what we're building" |
| Workflow as constraint | "MUST do step X" in workflow | Workflow is procedure (guidance), not constraint. Iron Laws own MUST/non-negotiable framing. |

The numbered workflow is NOT an Iron Law — it's procedure. The steps reference Iron Laws by name ("per Law 1", "per Law 2", "per Law 3 #3") rather than restating them; the two surfaces stay distinct.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Numbered workflow ends up duplicating Iron Law content | Workflow steps reference Iron Laws by name (e.g., "per Law 1 #3", "per Law 3 #3"). The two surfaces stay distinct: Iron Laws say what; workflow says when/in-what-order. |
| 9 steps feel verbose for short framings | Steps 4 (out-of-scope distinction) and 8 (design-intent.json detail) are explicit reference rather than mandatory iteration. A trivial reframing uses ~5 steps end-to-end. |
| AskUserQuestion in Step 6 fires too aggressively | Triggered only when the 5-condition gate fails. The gate already produces a `not_ready` verdict in current implementation; the AskUserQuestion makes the operator's branch explicit instead of forcing silent iteration. |
| Step 8's `design-intent.json` schema is more prescriptive than the runtime contract | Step 8 names fields that already exist in `lib/nexus/stage-content/frame/artifact-contract.md`'s output description. No new schema invented. |
| Iron Laws drift from verbatim during edit | Pre-edit Iron Laws captured to `/tmp/frame-iron-laws-before.md`; post-edit diff verification mandated in Implementation Procedure Step 2. |
| Total SKILL.md.tmpl length grows from 90 to ~210 lines | Within design budget; canonical Track E skills' `.tmpl` source is comparable post-F. |

---

## Effort

- Brief: ~30 min (this document)
- Implementation: ~30 min (3 edits to one .tmpl, 4 host regenerations, verify, commit, push)
- **Total: ~1h** (matches RFC's M effort estimate for F.5)

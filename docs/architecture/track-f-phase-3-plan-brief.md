# Track F Phase 3 Brief: Darwin upgrade for `/plan` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**
**Darwin baseline:** 69.5/100 (tied bottom-3 in v3 audit alongside `/build`).
**Darwin target:** 82–84/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/plan (canonical)" (lines 103–134).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md`.
**Predecessor phase:** `docs/architecture/track-f-phase-1-discover-brief.md` (F.1, voice exemplar).
**Issue:** #115.

---

## Why F.3 now

`/plan` shares the bottom-3 Darwin baseline with `/build` and `/discover`, but unlike `/discover` it does not need a structural footer fix. The four-action Darwin upgrade applies cleanly. Per the audit (lines 103–134):

| Dimension | Score | Issue |
|---|---|---|
| D1 Frontmatter | 6/10 | Trigger language ("when the work is framed and needs to become execution-ready") is decent but no example user phrasings |
| D2 Workflow Clarity | 5/10 | Iron Laws read as a contract for what `sprint-contract.md` MUST contain, but the SKILL.md does not number "how to actually produce that contract" |
| D3 Edge Cases | 8/10 | Iron Laws 1–3 enumerate failure modes the operator might otherwise paper over |
| D4 Checkpoint Design | 6/10 | Inherited preamble + topology chooser nudge for `agent_team`; Iron Laws don't add new gates |
| D5 Instruction Specificity | 8/10 | Law 1's three required properties, Law 2's four risk fields, Law 3's recovery routing all named concretely |
| D6 Resource Integration | 7/10 | Standard path inventory; references `design-contract.md` and `verification-matrix.json` |
| D7 Overall Architecture | 8/10 | Cross-skill law citations are tight: Law 1 anchors `/build` Law 1, Law 3 anchors `/handoff` Law 1 |
| D8 Real-World Performance | 7/10 | Most-typical prompt ("plan the work") drags — Iron Laws prescribe the artifact shape but not the procedure to break a feature into bite-sized tasks |

The audit explicitly suggests the missing surface (line 688): `/plan` could add: "Step 1: Read `prd.md` Iron Law 1 sections. Step 2: For each acceptance criterion, draft 1-3 candidate tasks. Step 3: For each task, verify the bite-sized contract..."

---

## Surface map

### File modified

```
skills/canonical/plan/SKILL.md.tmpl    ← edit (frontmatter + workflow + typical prompts + AskUserQuestion)
skills/canonical/plan/SKILL.md         ← regenerated by gen:skill-docs (claude default)
.agents/skills/nexus-plan/SKILL.md     ← regenerated by gen:skill-docs --host codex
.factory/skills/nexus-plan/SKILL.md    ← regenerated by gen:skill-docs --host factory
.gemini/skills/nexus-plan/SKILL.md     ← regenerated by gen:skill-docs --host gemini-cli
```

### File NOT modified

- `lib/nexus/stage-content/plan/checklist.md`, `overview.md`, `artifact-contract.md` — runtime content; not skill prose
- Iron Laws section (lines 21–64 of the .tmpl) — preserved verbatim
- `scripts/resolvers/completion-advisor.ts` — orchestrator-owned
- All other canonical/support skills — out of scope for this phase

### Frontmatter changes

Description gets trigger phrases appended. `allowed-tools` already contains `AskUserQuestion`; no list change.

---

## Action 1 — Numbered workflow design

Insert a new `## How to run /plan` section between the Iron Laws section (currently ends at line 64) and the Operator Checklist (currently begins at line 66).

The workflow has 9 numbered steps. Each step references Iron Laws by name without restating them. Steps 6 and 8 contain the AskUserQuestion gates (Action 4) — they live inside the workflow rather than as a separate section, mirroring F.1's Step 3 placement.

**Voice (Nexus-native, mirrors F.1 Steps 1–9):**

```markdown
## How to run /plan

These steps execute one `/plan` run. Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply.

### Step 1 — Read upstream framing

Read `.planning/current/frame/prd.md`. Per `/frame` Law 1 it carries seven required sections (problem, hypothesis, success criteria, non-goals, risks, alternatives, decision rationale). The success criteria section is the input to Step 2; the risks section is one input to Step 5.

If `prd.md` does not exist, `/plan` cannot run. Route back to `/frame` instead of synthesizing the framing here.

### Step 2 — Extract success criteria

Per `/frame` Law 1, each success criterion is observable (named command, metric, or artifact — vague criteria like "users love it" were rejected upstream). List them as the seed set for task drafting.

If a success criterion looks vague despite passing `/frame`, treat it as a framing escape and route back to `/frame`. `/plan` does not retroactively sharpen criteria — `/frame` Law 1 owns that contract.

### Step 3 — Draft 1–3 candidate tasks per criterion

For each criterion, draft one to three candidate tasks that, when complete, would satisfy that criterion. Aim for the smallest set that covers the criterion; over-decomposition produces busywork, under-decomposition violates Law 1.

Candidate tasks at this step are drafts. They have not yet been verified against Law 1's three properties — that is Step 4.

### Step 4 — Verify each task is bite-sized

For each candidate task from Step 3, verify all three Law 1 properties:

1. Bounded scope (≤3 sentences, ≤1 day of focused work)
2. Observable acceptance criterion ("X exists at path Y AND Z is true" form)
3. Verification command (one specific `bun test ...` / `bunx tsc --noEmit` / `bun run repo:inventory:check` / etc.)

A task that fails any property is rejected here, before the contract is written. The cost of rejection at `/plan` is minutes; the cost downstream is hours per Law 1's own justification.

### Step 5 — Enumerate ≥3 risks

Per Law 2, every plan needs ≥3 specific failure modes, each with failure description, detection signal, and mitigation/recovery routing. Sources to draw from:

- The framing risks section in `prd.md` (don't duplicate — refine for execution)
- Underestimated-scope risk for any borderline task from Step 4
- Cross-task coupling risk if multiple tasks touch the same module
- Dependency risk for any task that assumes infrastructure or library availability
- Acceptance-criterion-vs-user-value risk for tasks where the verification command passes but the user impact is unclear

If you cannot enumerate three risks, the plan is under-thought (per Law 2). Return to Step 3 and revisit task scope before forcing the count.

### Step 6 — Branching: borderline tasks

If any candidate task from Step 4 is borderline on Law 1 (e.g., "≤1 day if assumptions hold; might be 2 days if X surfaces"), do not silently accept or silently split. Use `AskUserQuestion`:

> "Task `<task-name>` is borderline on Law 1's bite-sized contract — estimated 1 day if `<assumption>` holds, ~2 days otherwise. Should I (a) split into two tasks now, (b) accept as-is and flag the assumption in the risk register, or (c) route back to `/frame` to tighten the success criterion this task serves?"

The operator picks; record the choice in the contract or risk register. The same gate applies if `/build` re-enters `/plan` mid-execution to extend scope (per Law 3) — confirm with the operator whether the discovered work is a contract extension, a split, or a framing reset.

### Step 7 — Write `sprint-contract.md`

Compose the contract with the verified task list (Step 4), the risk register (Step 5), and any operator decisions recorded at Step 6. Each task carries its scope sentence, observable acceptance criterion, and verification command per Law 1. Each risk carries its three Law 2 fields.

The contract is the input `/build`, `/review`, and `/qa` evaluate against. Anything missing here is missing for the rest of the lifecycle.

### Step 8 — Verify the binding-handoff posture

Per Law 3, once the contract is written and `/handoff` records the route, `/build` operates only on the listed tasks. Before writing status, sanity-check:

- Every task has the three Law 1 properties (no exceptions)
- The risk register has ≥3 entries with Law 2's three fields each
- The success criteria from Step 2 are each addressed by at least one task

If any fail, return to the relevant step. The contract is binding once written; under-specification at this gate becomes a routing event in `/build`.

### Step 9 — Write status

Run the canonical command (in the Routing section below). It writes `execution-readiness-packet.md`, `sprint-contract.md`, `verification-matrix.json`, `design-contract.md` when present, and `status.json`. The advisor record carries the verdict; `/handoff` reads it next.
```

This Action lifts D2 Workflow Clarity from 5/10 to ~8.5/10 (+3.5 raw, ×15 = +5.25 weighted) and D8 Real-World Performance from 7/10 to ~9/10 (+2 raw, ×25 = +5 weighted). Combined: ~+10 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus planning command. Writes repo-visible execution readiness artifacts and
  stage status for the governed Nexus lifecycle. Use when the work is framed and needs to
  become execution-ready. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus planning command. Writes repo-visible execution readiness artifacts and
  stage status for the governed Nexus lifecycle. Use when the user says "plan this work",
  "break down the framing into tasks", "make a sprint plan", or when the work is framed
  and needs to become execution-ready. (nexus)
```

Lifts D1 Frontmatter from 6/10 to ~8/10 (+2 raw, ×8 = +1.6 weighted).

Frontmatter character count remains well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the Routing block's Run command, before the Completion Advisor footer. F.1 placed Typical prompts before the footer; F.3 mirrors that placement exactly.

**Voice:**

```markdown
## Typical prompts

These are example user requests `/plan` handles well. The skill is invoked and produces an `execution-readiness-packet.md`, a `sprint-contract.md`, a `verification-matrix.json`, and a `status.json`.

### Prompt 1 — Plan from a fresh PRD

> "Plan the work for the telemetry feature now that /frame is done."

`/plan` walks Step 1–9. Step 1 reads `prd.md` and confirms the seven `/frame` Law 1 sections are present. Step 2 extracts each success criterion. Steps 3–4 draft and verify 1–3 bite-sized tasks per criterion against Law 1. Step 5 builds the risk register from the framing risks plus execution-specific failure modes. Step 7 writes the contract. Verdict `ready` if all gates pass.

### Prompt 2 — Vague task suspected

> "Is 'improve performance' an OK task in the contract?"

Step 4 rejects it: Law 1 explicitly disallows vague acceptance criteria like "code works" or "feature is good." `/plan` proposes a sharpened replacement (e.g., "p95 latency on the search endpoint drops below 300ms, verified by `bun test test/perf/search.test.ts`") or routes back to `/frame` if the underlying success criterion was the source of the vagueness.

### Prompt 3 — Build wants to expand scope mid-run

> "/build hit a 3-strike loop and thinks we need an extra task — should we just add it?"

Law 3 forbids silent scope expansion: stop, route back to `/plan` to extend the contract, then re-enter `/build`. Step 6's AskUserQuestion gate asks whether the discovered work is (a) a contract extension here, (b) a split of an existing task, or (c) a framing reset that needs `/frame` first. The operator's choice is recorded; the contract is updated explicitly, never silently.

These prompts test that `/plan` produces a binding execution contract — bite-sized tasks, enumerated risks, no silent scope drift (Iron Laws 1–3).
```

Lifts D8 from ~9/10 toward 9.5/10 (incremental gain on top of Action 1; cumulative ≈ +0.5 raw, ×25 = +1.25 weighted).

---

## Action 4 — AskUserQuestion gate

The gate is bundled into Action 1's Step 6, mirroring F.1's Step 3 placement pattern. Two natural triggers fire it:

1. **Borderline task at Step 4.** A candidate task that is on the edge of Law 1's "≤1 day of focused work" bound. The gate asks split / accept-and-flag / route-back-to-`/frame`.
2. **Mid-`/build` re-entry.** When `/build` returns to `/plan` per Law 3 with a discovered task, the same gate asks whether it is a contract extension, a task split, or a framing reset.

The single gate placement covers both because the underlying decision is the same: "is this work a Law 1 task as-is, or does it need transformation before it can be one?" Per RFC voice constraints, the gate names explicit options rather than vague "ask user."

Lifts D4 Checkpoint Design from 6/10 to ~7.5/10 (+1.5 raw, ×7 = +1.05 weighted).

---

## Projected Darwin score after F.3

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6 | 8 | +2 | +1.6 |
| D2 Workflow Clarity | 5 | 8.5 | +3.5 | +5.25 |
| D3 Edge Cases | 8 | 8 | 0 | 0 |
| D4 Checkpoint Design | 6 | 7.5 | +1.5 | +1.05 |
| D5 Instruction Specificity | 8 | 8 | 0 | 0 |
| D6 Resource Integration | 7 | 7.5 | +0.5 | +0.25 (Step 1 reads `prd.md` explicitly; small lift) |
| D7 Overall Architecture | 8 | 8 | 0 | 0 (no structural change; Completion Advisor footer already present) |
| D8 Real-World Performance | 7 | 9 | +2 | +5 |

**Projected total:** 69.5 + 13.15 ≈ **82.7/100** (target was 82–84; F.3 lands inside band).

---

## Implementation procedure

### Step 1 — Edit `skills/canonical/plan/SKILL.md.tmpl`

Apply 3 changes:

1. Frontmatter description (Action 2)
2. Insert `## How to run /plan` between Iron Laws and Operator Checklist (Action 1, includes Action 4's AskUserQuestion gate at Step 6)
3. Insert `## Typical prompts` between the Run block and the Completion Advisor footer (Action 3)

The Iron Laws section (lines 21–64) is preserved verbatim. Run a diff after editing to confirm zero lines changed inside that range.

### Step 2 — Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 3 — Verify

```bash
bun run skill:check
git grep -n "How to run /plan" skills/canonical/plan/SKILL.md
git grep -n "Typical prompts" skills/canonical/plan/SKILL.md
git grep -n "plan this work" skills/canonical/plan/SKILL.md
git grep -n "AskUserQuestion" skills/canonical/plan/SKILL.md
```

Each should match. Iron Laws diff:

```bash
git diff origin/main -- skills/canonical/plan/SKILL.md.tmpl | sed -n '/^@@/,/^@@/p' | grep -E "^[+-]" | grep -E "Law [123]" | head -5
```

This should return zero matches (no `+` or `-` lines touching Iron Law headings or content).

### Step 4 — Commit and push

```bash
git add skills/canonical/plan/SKILL.md.tmpl skills/canonical/plan/SKILL.md \
        .agents/skills/nexus-plan/SKILL.md \
        .factory/skills/nexus-plan/SKILL.md \
        .gemini/skills/nexus-plan/SKILL.md \
        docs/architecture/track-f-phase-3-plan-brief.md
git commit -m "skills/canonical/plan: Track F.3 Darwin upgrade"
git push -u origin track-f-phase-3-plan
```

Do not close issue #115. The orchestrator closes it after merge.

---

## Acceptance criteria

This phase is complete when:

1. ✅ Frontmatter description carries trigger phrases ("plan this work" / "break down the framing into tasks" / "make a sprint plan")
2. ✅ `## How to run /plan` section inserted between Iron Laws and Operator Checklist with 9 numbered steps
3. ✅ Step 6 includes explicit `AskUserQuestion` text covering borderline tasks AND `/build` mid-run re-entry
4. ✅ `## Typical prompts` section with 3 examples added between the Run block and the Completion Advisor footer
5. ✅ Iron Laws preserved verbatim (zero changes inside the `## Iron Laws` section)
6. ✅ `bun run skill:check` clean across all 4 hosts
7. ✅ Commit message references Track F.3
8. ✅ Branch `track-f-phase-3-plan` pushed to origin (PR / merge / issue closure are orchestrator's job)

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First do X, then do Y" | "Step 1 — Read upstream framing. Read `.planning/current/frame/prd.md`. Per `/frame` Law 1..." |
| Workflow vs Iron Law | Restating Law 1 inside Step 4 | "Verify all three Law 1 properties: bounded scope, observable acceptance criterion, verification command." Reference, don't restate. |
| AskUserQuestion framing | Vague "ask user" | Explicit options: "AskUserQuestion: 'Should I (a) split into two tasks, (b) accept as-is and flag, or (c) route back to `/frame`?'" |
| Cross-skill reference | "Then build it" | "`/build`, `/review`, and `/qa` evaluate against this contract" — Iron-Law-aware. |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "plan this work", "break down the framing into tasks", "make a sprint plan" |
| MUST/non-negotiable on workflow steps | "Step 1 MUST read `prd.md`" | "Step 1 — Read upstream framing. Read `prd.md`." Workflow is procedure, not constraint. |

The numbered workflow is NOT an Iron Law — it's procedure. No "MUST" or "non-negotiable" framing on the steps. The Iron Laws keep their absolute voice; the workflow keeps its directional voice.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Numbered workflow ends up duplicating Iron Law content | Workflow steps reference Iron Laws by name (e.g., "per Law 1", "per Law 2's three fields") rather than restating them. The two surfaces stay distinct. |
| Step 6's AskUserQuestion fires too aggressively in normal runs | Step 4 explicitly resolves clearly-bite-sized tasks without invoking the gate. Step 6 only fires on borderline cases or `/build` re-entry — both genuine routing events per Law 1 / Law 3. |
| Inserting the workflow between Iron Laws and Operator Checklist breaks reading flow | Reading sequence stays correct: discipline (Iron Laws) → procedure (How to run) → intent (Operator Checklist) → outputs (Artifact Contract) → transport (Routing). The workflow is the new procedural rung; the others stay in place. |
| 9 steps feel verbose for trivial single-task plans | Step 3 explicitly allows 1 candidate task per criterion. Steps 5–8 still apply (risks, gate, contract write, sanity check) — the procedure scales but does not inflate. |
| Iron Law diff check misses a subtle wording change | Run `git diff origin/main -- skills/canonical/plan/SKILL.md.tmpl` and visually scan the `## Iron Laws` block; supplement with the grep in Step 3. |
| Total SKILL.md length grows substantially | Track F's design budget (canonical Track E skills run 565–620 lines). `/plan` baseline is ~612 lines plus the added workflow + typical prompts (~140 lines) lands the file around 750 lines, within the F.1 / F.2 working envelope. |

---

## Effort

- Brief: ~25 min (this document)
- Implementation: ~30–45 min (3 edits to one .tmpl file, regenerate across 4 hosts, verify, commit, push)
- **Total: ~1h** (matches RFC's M effort estimate for F.3)

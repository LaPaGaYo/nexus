# Track F Phase 6 Brief: Darwin upgrade for `/qa` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**
**Darwin baseline:** 73.8/100 (mid-pack of canonical 9; per audit-v3).
**Darwin target:** 84–86/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/qa (canonical)" (lines 171–201).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md` (commit `635a7fa`).
**Exemplar:** `docs/architecture/track-f-phase-1-discover-brief.md` (commit `1a3ac18`).
**Issue:** #118.

---

## Why F.6 here

`/qa` enters Track F with strong rigor (Track E Iron Laws were among the most operationally useful — severity tags, evidence-per-category, and the 3-strike cluster stop are concrete enough to drive a real QA pass). The Darwin gap is procedural, not contractual:

| Dimension | Score | Issue |
|---|---|---|
| D1 Frontmatter | 6/10 | Same canonical pattern; description is contract-shaped, not trigger-shaped |
| D2 Workflow Clarity | 5/10 | No numbered workflow at all — Operator Checklist three bullets |
| D3 Edge Cases | 9/10 | Verdict ladder + 3-strike cluster handles real failure modes |
| D4 Checkpoint Design | 5/10 | Iron Laws don't add new AskUserQuestion gates; verdict ladder is a routing event |
| D5 Instruction Specificity | 9/10 | Strongly concrete: severity tags, evidence requirements per category |
| D6 Resource Integration | 7/10 | Standard path inventory |
| D7 Overall Architecture | 8/10 | Clean; no topology chooser (intentional — QA isn't planning-style) |
| D8 Real-World Performance | 8/10 | Well-rigorous but no step-by-step procedure |

The audit's prescription (audit line 699) is direct: *"`/qa` Law 3 (3-cluster stop) routes to `/investigate` — could surface AskUserQuestion confirming the cluster."* That maps to Action 4 here. Actions 1–3 follow the F.1 template.

`/qa` does NOT have the Stage-Aware Local Topology Chooser section (the audit notes this on line 173 — likely intentional since QA isn't a stage where teams of perspectives compete). Track F.6 does NOT add one. Action 5 is N/A — `/qa` already carries `{{NEXUS_COMPLETION_ADVISOR:qa}}` in the template footer.

---

## Surface map

### File modified

```
skills/canonical/qa/SKILL.md.tmpl    ← edit (frontmatter + workflow + typical prompts + AskUserQuestion in workflow)
skills/canonical/qa/SKILL.md         ← regenerated by gen:skill-docs (Claude host)
.agents/skills/nexus-qa/SKILL.md     ← regenerated by gen:skill-docs --host codex
.factory/skills/nexus-qa/SKILL.md    ← regenerated by gen:skill-docs --host factory
.gemini/skills/nexus-qa/SKILL.md     ← regenerated by gen:skill-docs --host gemini-cli
```

### File NOT modified

- `lib/nexus/stage-content/qa/*` — runtime content; not skill prose
- `scripts/resolvers/completion-advisor.ts` — `/qa` already wired
- Iron Laws section (template lines 20–61) — preserved verbatim
- All other canonical/support skills — out of scope for this phase
- The sibling `skills/support/qa-only/SKILL.md.tmpl` — out of scope (different surface, different audience)
- No new tests added (per task constraints)

### Frontmatter changes

Description gets trigger phrases appended. `allowed-tools` already has `AskUserQuestion` from Track E; no change needed.

---

## Action 1 — Numbered workflow design

Insert a new `## How to run /qa` section between the Iron Laws section (template ends at line 61, Law 3 closing paragraph) and the Operator Checklist (template line 63).

**Voice (Nexus-native, matches F.1):**

```markdown
## How to run /qa

These steps execute one `/qa` run. Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply.

### Step 1 — Define validation scope

Read the upstream advisor record from `/review`. If the review verdict was not `pass`, `/qa` cannot proceed — the operator routes back to `/build` first. If `pass`, enumerate the primary user flows the run touches:

- The home / sign-in / checkout / primary CRUD pages always count as primary flows (per Law 2 #3).
- Any flow named in `prd.md` `## Success Criteria` counts.
- Any flow surfaced by `/review` as design-bearing or browser-facing counts.

The scope is the union of these. A `/qa` run with no enumerated primary flows is not yet scoped — return to `/review` for a clearer handoff.

### Step 2 — Run health check on each primary flow

For every primary flow, execute its happy path once. Capture before-action state, perform the canonical interaction, capture after-action state. A failure here is a P0 candidate per Law 2 #3 regardless of how minor it looks.

If the run has a baseline (`baseline.json` from a prior QA pass), diff console output against it as part of this step. New console errors not in baseline are Law 2 #2 regressions.

### Step 3 — Enumerate findings with severity

For each observed defect, write a finding per Law 1's evidence contract:

1. **Severity tag** — `P0` | `P1` | `P2` | `P3`. Untagged findings are rejected.
2. **Repro that ran in this turn** — step-by-step the operator executed, verified at finding time AND at report-write time.
3. **Evidence appropriate to category**:
   - Interactive: before screenshot + after screenshot + diff snapshot
   - Static: single annotated screenshot
   - Console-only: full console line(s) with stack frame + URL
4. **No credentials** — never paste passwords, tokens, session cookies, OAuth secrets.

A finding missing severity, repro, or evidence is an observation, not a finding. Drop it from the report or upgrade it to a finding.

### Step 4 — Apply Law 2 ship-blocking gate

After findings are enumerated, evaluate the verdict ladder:

- Any P0 finding → verdict `not_ready`. There is no "P0 but ship anyway."
- New console error not in baseline → verdict `not_ready` (regression).
- Failed health check on a primary user flow → verdict `not_ready`.
- Otherwise, only P1+/P2/P3 findings → verdict `ready_with_findings`.
- No findings, all primary flows green → verdict `ready`.

The verdict is mechanical from the findings — it is not a judgment call.

### Step 5 — Detect 3-strike cluster (per Law 3)

Scan the findings for shared root cause: same module, same error class, same broken interaction pattern. If 3+ findings cluster, STOP enumerating. Use `AskUserQuestion`:

> "Three findings appear to share a root cause ([brief cluster description]). Should I (a) confirm cluster and route to `/investigate`, (b) continue gathering more variants, or (c) mark the cluster P0 and write `not_ready` without `/investigate`?"

The operator picks. If (a), record the 3 collected findings, write `not_ready`, route to `/investigate` per Law 3. If (b), continue but flag the risk in the report. If (c), the cluster is recorded as a single P0 with the 3 findings as evidence. The default expectation is (a).

### Step 6 — Write `qa-report.md`

Compile the findings into `qa-report.md` per the artifact contract. At write time, re-verify each repro per Law 1 #2. A finding whose repro fails on re-verification is dropped from the report (it was a transient).

### Step 7 — Write `design-verification.md`

For design-bearing or browser-facing runs, write `design-verification.md` capturing what was visually verified against intent. For non-design runs, this artifact MAY be empty but the file should still be written so `/ship` knows QA touched it.

### Step 8 — Write status

Run the canonical command (in the Routing section below). It writes `status.json` with the verdict from Step 4 and any continuation artifacts. The advisor record carries the verdict; `/ship` reads it and refuses to proceed if `not_ready`.
```

This Action lifts D2 Workflow Clarity from 5/10 to ~8/10 (+3 raw, ×15 = +4.5 weighted) and D8 Real-World Performance from 8/10 to ~9.5/10 (+1.5 raw, ×25 = +3.75 weighted). Combined: ~+8.25 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus QA command. Records governed validation scope, QA findings, and
  structured readiness state for the governed Nexus lifecycle. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus QA command. Records governed validation scope, QA findings, and
  structured readiness state for the governed Nexus lifecycle. Use when the user says
  "QA the build", "validate the change", "run end-to-end checks before ship",
  or after `/review` passes and before `/ship` reads the verdict. (nexus)
```

Lifts D1 Frontmatter from 6/10 to ~8/10 (+2 raw, ×8 = +1.6 weighted).

Frontmatter character count: still well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the `Run:` block, before the Completion Advisor footer.

**Voice:**

```markdown
## Typical prompts

These are example user requests `/qa` handles well. The skill is invoked and produces a structured `qa-report.md` plus `status.json` with a verdict.

### Prompt 1 — Standard QA after `/review` pass

> "QA the build before we ship."

`/qa` walks Step 1–8. Step 1 reads the `/review` advisor record and enumerates primary flows from `prd.md`. Step 2 runs each happy path. Step 3 records findings with severity + repro + evidence. Step 4 applies the verdict ladder mechanically. If clean, `status.json` carries `ready` and `/ship` proceeds.

### Prompt 2 — Multiple findings appearing related

> "I'm seeing 5 broken-link errors across the marketing pages — should I write them all up?"

Step 5's 3-strike rule fires on the first 3 findings; the workflow stops enumerating and uses `AskUserQuestion` to confirm cluster routing. The default route is `/investigate` (Law 3) so the underlying cause gets fixed once instead of patched five times.

### Prompt 3 — Found one P0

> "Sign-in is broken on Safari but works on Chrome. Verdict?"

Law 2 #1: any P0 → `not_ready`. There is no "P0 but ship anyway." Step 4 writes `not_ready` mechanically. Step 6 records the finding with cross-browser repro per Law 1 #3 (interactive: before + after + diff). `/ship` reads the verdict and refuses to merge until the operator routes back to `/build`.

These prompts test that `/qa` produces a verdict-bearing report (not an enumeration), respects the 3-strike cluster rule, and never softens P0 → ship.
```

Lifts D8 from ~9.5/10 toward 9.5/10 (already partially captured by Action 1; cumulative gain ≈ +0.25 raw, ×25 = +0.6 weighted).

---

## Action 4 — AskUserQuestion gate

Bundled into Action 1 Step 5 (3-strike cluster detection). The gate explicitly addresses audit line 699's recommendation: *"`/qa` Law 3 (3-cluster stop) routes to `/investigate` — could surface AskUserQuestion confirming the cluster."*

The three options match the task spec verbatim:

> "Confirm cluster and route to `/investigate` / Continue gathering / Mark P0 and ship_blocked?"

Translated to operator-facing voice:

> "Three findings appear to share a root cause ([brief cluster description]). Should I (a) confirm cluster and route to `/investigate`, (b) continue gathering more variants, or (c) mark the cluster P0 and write `not_ready` without `/investigate`?"

Lifts D4 Checkpoint Design from 5/10 to ~7/10 (+2 raw, ×7 = +1.4 weighted).

---

## Action 5 — Completion Advisor footer

N/A. `/qa` already carries `{{NEXUS_COMPLETION_ADVISOR:qa}}` at the template footer (line 81). The resolver at `scripts/resolvers/completion-advisor.ts` already has a `qa` entry. No change needed.

---

## Projected Darwin score after F.6

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6 | 8 | +2 | +1.6 |
| D2 Workflow Clarity | 5 | 8 | +3 | +4.5 |
| D3 Edge Cases | 9 | 9 | 0 | 0 |
| D4 Checkpoint Design | 5 | 7 | +2 | +1.4 |
| D5 Instruction Specificity | 9 | 9 | 0 | 0 |
| D6 Resource Integration | 7 | 7 | 0 | 0 |
| D7 Overall Architecture | 8 | 8 | 0 | 0 |
| D8 Real-World Performance | 8 | 9.5 | +1.5 | +3.75 |

**Projected total:** 73.8 + 11.25 ≈ **85/100** (target 84–86; on target).

---

## Implementation procedure

### Step 1: Edit `skills/canonical/qa/SKILL.md.tmpl`

Apply 3 changes:
1. Frontmatter description (Action 2)
2. Insert `## How to run /qa` between Iron Laws and Operator Checklist (Action 1, includes Action 4 gate in Step 5)
3. Insert `## Typical prompts` after `Run:` block, before `{{NEXUS_COMPLETION_ADVISOR:qa}}` (Action 3)

### Step 2: Regenerate

```bash
bun run gen:skill-docs                    # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 3: Verify

```bash
bun run skill:check                                                                # clean across all 4 hosts
git grep -n "How to run /qa" skills/canonical/qa/SKILL.md                          # 1 hit
git grep -n "Typical prompts" skills/canonical/qa/SKILL.md                         # 1 hit
git grep -n "QA the build" skills/canonical/qa/SKILL.md                            # 1 hit (frontmatter trigger)
```

### Step 4: Commit + push branch only

```bash
git add docs/architecture/track-f-phase-6-qa-brief.md \
        skills/canonical/qa/SKILL.md.tmpl \
        skills/canonical/qa/SKILL.md \
        .agents/skills/nexus-qa/SKILL.md \
        .factory/skills/nexus-qa/SKILL.md \
        .gemini/skills/nexus-qa/SKILL.md
git commit -m "skills/canonical/qa: Track F.6 Darwin upgrade"
git push -u origin track-f-phase-6-qa
# Issue #118 stays open per task spec.
```

---

## Acceptance criteria

This phase is complete when:

1. ✅ Frontmatter description carries trigger phrases ("QA the build" / "validate the change" / "run end-to-end checks before ship")
2. ✅ `## How to run /qa` section inserted between Iron Laws and Operator Checklist with 8 numbered steps
3. ✅ Step 5 includes explicit `AskUserQuestion` text matching the three task-spec options (confirm cluster / continue gathering / mark P0)
4. ✅ `## Typical prompts` section with 3 examples added
5. ✅ Iron Laws preserved verbatim (no diff in template lines 20–61)
6. ✅ `bun run skill:check` clean across all 4 hosts
7. ✅ Branch `track-f-phase-6-qa` pushed to origin
8. ✅ Issue #118 NOT closed (per task spec)
9. ✅ Commit message references Track F.6
10. ✅ Brief lives at `docs/architecture/track-f-phase-6-qa-brief.md`

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First do X, then do Y" | "Step 1 — Define validation scope. Read the upstream advisor record from `/review`. If the review verdict was not `pass`..." |
| AskUserQuestion framing | Vague "ask user" | Explicit options: "Should I (a) confirm cluster and route to `/investigate`, (b) continue gathering more variants, or (c) mark the cluster P0..." |
| Cross-skill reference | "Then ship it" | Iron-Law-aware: "`/ship` reads the verdict and refuses to merge until the operator routes back to `/build`." |
| Workflow vs Iron Law | Either or both | Both, with explicit relationship: "Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply." |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "QA the build", "validate the change", "run end-to-end checks before ship" |

The numbered workflow is NOT an Iron Law — it's procedure. No "MUST" or "non-negotiable" framing on the steps; the steps are guidance, the Iron Laws are constraints.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Workflow duplicates Law 1's evidence rules | Step 3 references Law 1 by name and lists the categories without restating the evidence-per-category sub-rules. The two surfaces stay distinct: workflow says *do this*, Law 1 says *this must be true*. |
| Step 4 verdict mechanics restate Law 2 | Step 4 says the verdict ladder is mechanical FROM Law 2 — it doesn't re-derive the rule, it walks the operator through the application. |
| AskUserQuestion default could leak operator preference | Step 5 records the default expectation (route to `/investigate`) but the user picks. The skill does not silently bypass the gate. |
| Re-verification at write time (Step 6) might surprise operators | The behavior is already in Law 1 #2 ("verified by running it once at finding time AND once at report-write time"). Step 6 surfaces it as procedure. |
| 8 steps feel long for trivial QA | Steps 7 (`design-verification.md`) is explicitly conditional — non-design runs may write an empty file. A trivial QA uses ~5 of 8 steps. |
| Iron Laws diff non-empty | Acceptance criteria #5 makes this a hard gate. Verification command on the .tmpl confirms. |

---

## Effort

- Brief: ~30 min (this document)
- Implementation: ~25–35 min (3 edits to one .tmpl file, regenerate, verify, commit, push)
- **Total: ~1h** (matches RFC's S effort estimate for `/qa`)

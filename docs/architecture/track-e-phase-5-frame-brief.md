# Track E Phase 5 Brief: strengthen `/frame` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup** (see RFC § "Track E is its own track").

**Audit rank:** 5. **Current discipline score:** 1.4/5 (Mandatory 1, Verification 1, Failure 1, Acceptance 2, Transferability 2) — second-lowest in the canonical 9 (only `/plan` at 1.2/5 was lower before E.2 strengthening).

**Parent RFC:** `docs/architecture/track-e-skill-strengthening-rfc.md` § Phase E.5.
**Anchor data:** `docs/architecture/skill-strength-audit.md` § "frame (canonical)".
**Voice references:**
- `skills/canonical/build/SKILL.md.tmpl` (commit `4209cc4`) — Phase E.1 PoC.
- `skills/canonical/plan/SKILL.md.tmpl` (commit `10974ed`) — Phase E.2.
- `skills/canonical/review/SKILL.md.tmpl` (commit `7fbcb7d`) — Phase E.3.
- `skills/canonical/qa/SKILL.md.tmpl` (commit `52c02fa`) — Phase E.4.
**External reference patterns:** PM `prd-development` (9-section PRD), `epic-hypothesis` (If/Then/Because format), `problem-framing-canvas`.
**Issue:** #107.

---

## Goal

Raise `/frame` SKILL.md prose discipline from 1.4/5 → 4.0+/5 by adding 3 Iron Laws that codify (a) what `prd.md` must contain as content sections, (b) the hypothesis-framing format every scope must articulate, (c) the readiness gate `/plan` enforces on framing entry.

After this lands:
- A `/frame` invocation cannot conclude with a thin `prd.md` that lists three bullets and calls it a PRD.
- Every framed scope carries an explicit "If/Then/Because" hypothesis that downstream `/plan` and `/review` validate against.
- `/plan` cannot accept entry from `/frame` if success criteria aren't observable, non-goals are empty, or no user story has acceptance criteria.

---

## Why this is rank 5 ROI

`/frame` is the input contract for everything downstream. Today its prose is 3 bullets — "scope / non-goals / success criteria" — without saying what the artifacts must contain or how to verify framing is sufficient before `/plan`.

Per audit:

> "Operator checklist is 3 bullets (scope / non-goals / success criteria). The PM `prd-development` skill prescribes a 9-section PRD template with explicit fields for problem evidence, target persona, strategic context, success metrics, edge cases, out-of-scope, and dependencies. Nexus frame writes `decision-brief.md` and `prd.md` but the SKILL.md doesn't say what those documents must contain or how to test that the framing is sufficient."

Strengthening `/frame` is the upstream complement to E.2 (`/plan`). E.2 fixed plan-output discipline; E.5 fixes plan-input discipline. The two together raise the floor of `/build`'s context substantially.

The audit also notes `/frame`'s closest external counterparts are PM-side: `prd-development`, `problem-framing-canvas`, `epic-hypothesis`. This is the strongest case in Track E for absorbing PM patterns into Nexus-native voice.

---

## Current state of `/frame`

Read `skills/canonical/frame/SKILL.md.tmpl` (40 lines) and `skills/canonical/frame/SKILL.md` (auto-generated, ~520 lines mostly preamble).

Per audit:
- **Mandatory 1/5** — no rules; descriptive only
- **Verification 1/5** — no readiness gate; `/plan` accepts whatever `/frame` writes
- **Failure 1/5** — no enumeration of framing failure modes (vague success criteria, missing personas, no rejected alternatives)
- **Acceptance 2/5** — `prd.md` and `decision-brief.md` are named in the artifact contract but not specified
- **Transferability 2/5** — partial; PRD vocabulary is industry-standard but discipline is missing

**Current Operator Checklist (from `lib/nexus/stage-content/frame/checklist.md`)**:
1. define scope
2. define non-goals
3. define success criteria

That's it. Three bullets. The audit's harshest score in the canonical 9.

---

## Surface map

### File modified

```
skills/canonical/frame/SKILL.md.tmpl           ← edit (insert Iron Laws section)
skills/canonical/frame/SKILL.md                 ← regenerated by gen:skill-docs
```

### File NOT modified

- `lib/nexus/stage-content/frame/*` — runtime content; not skill prose
- `docs/product/prd.md` template — there isn't one in-repo; the discipline in Iron Law 1 IS the template until a separate file is created (out of scope for this brief)
- `.planning/current/frame/*` — artifacts; not affected by prose
- PM external skills — referenced for voice patterns, not modified

### File optionally modified (defer)

- `lib/nexus/stage-content/frame/checklist.md` — could expand from 3 bullets to mirror Iron Law 1's section enumeration. Not required for prose strengthening.
- A new `docs/product/prd-template.md` — formal PRD template file. Iron Law 1's 7-section enumeration is the schema for now; standalone template can land as a follow-up.

---

## Iron Law design

### Law 1 — PRD Content Contract

**Discipline absorbed from:** PM `prd-development` (9-section PRD) + audit recommendation #1.

**Voice (Nexus-native):**

```markdown
### Law 1 — PRD Content Contract

`docs/product/prd.md` MUST contain these seven sections, each non-empty:

1. **Problem statement** — who hurts, how, and how much (2-4 sentences). "Users are frustrated" is rejected; specify the user, the action they fail at, and the cost.
2. **Hypothesis** — the "If/Then/Because" statement (see Law 2). Without a hypothesis, the PRD is wishful description, not framing.
3. **Success criteria** — observable, falsifiable conditions. Each one must answer "what command/metric/artifact reveals whether this is achieved?" Vague criteria like "users love it" are rejected; "task completion rate >85% on the X flow" is accepted.
4. **Non-goals** — ≥3 concrete things this work will NOT do. The non-goals section is where scope creep gets pre-empted; an empty non-goals section means scope is undefined.
5. **Risks** — ≥3 specific failure modes the framing acknowledges, each with a one-sentence mitigation or "accepted; will revisit if it materializes" note.
6. **Alternatives considered** — at least one rejected alternative with the reason it was rejected. "We considered nothing else" is a sign the framing is premature.
7. **Decision rationale** — why this scope, why now, what changes when this ships. The rationale is what `/review` reads when judging whether the work matches intent.

A PRD missing any of the seven sections is not a PRD; it is a wishlist. Wishlists don't survive contact with `/plan`.
```

This Law operationalizes "what does `prd.md` actually have to contain."

### Law 2 — Hypothesis Framing

**Discipline absorbed from:** PM `epic-hypothesis` + audit recommendation #3.

**Voice (Nexus-native):**

```markdown
### Law 2 — Hypothesis Framing

Every framed scope MUST articulate its hypothesis as:

> **If we [action], then [users] will [outcome] because [evidence].**

Each clause is mandatory:

- **Action** — what the work does, in concrete verbs (build, ship, change, deprecate). Not "improve" or "enhance"; those are direction without target.
- **Users** — who is affected, named specifically. "All users" is rarely true; specify the segment whose behavior the hypothesis predicts.
- **Outcome** — the observable change in user behavior or system state. Must be falsifiable in the same way Law 1 success criteria are.
- **Evidence** — what makes the team believe this hypothesis is true now. Could be data, prior research, customer interviews, market signal, or explicit reasoning. "We think it'll work" is not evidence; it is hope.

The hypothesis is the contract `/review` evaluates against. When `/review` asks "did this work deliver what `/frame` framed?", the hypothesis is what answers that question. A scope without a hypothesis cannot be reviewed honestly.

Stating a hypothesis up front also surfaces disagreement early. If two teammates can't agree on the [evidence] clause, the framing is not yet ready for `/plan`.
```

This Law gives `/review` something concrete to verify and forces the framing team to commit to a falsifiable claim.

### Law 3 — Framing Readiness Gate Before `/plan`

**Discipline absorbed from:** Superpowers `verification-before-completion` (gate before next stage) + audit recommendation #2.

**Voice (Nexus-native):**

```markdown
### Law 3 — Framing Readiness Gate Before `/plan`

Before `/frame` writes `status.json` with verdict `ready`, ALL of these must hold:

1. **Success criteria are observable and falsifiable** — each one names the command, metric, or artifact that reveals achievement. "Users are happier" fails; "p95 latency on the search endpoint drops below 300ms" passes.
2. **Non-goals lists ≥3 concrete items** — empty or one-bullet non-goals sections are rejected. Pre-empting scope creep is the cheapest at framing time.
3. **At least one user story has explicit acceptance criteria** — in the form "Given X, when Y, then Z." Without this, `/plan` has nothing to translate into bite-sized tasks (per `/plan` Law 1).
4. **`out-of-scope` section is non-empty** — distinct from non-goals; out-of-scope captures things adjacent users may expect that this work doesn't address.
5. **Hypothesis is present per Law 2** — all four clauses (action, users, outcome, evidence) populated.

If any of the five fail, `/frame` returns `not_ready` and the operator either iterates within `/frame` or routes back to `/discover` to gather missing input.

`/plan` reads the framing verdict from the advisor record. If `not_ready`, `/plan` cannot proceed.

The reason: `/plan` translates framing into tasks. If the framing has no falsifiable success criteria, the tasks have no observable acceptance criteria (per `/plan` Law 1) — the ambiguity propagates straight through `/build`, `/review`, `/qa`, and lands in the user's lap as "we shipped what we said we'd ship, but the user expected something different."
```

This Law is the upstream complement to `/plan` Law 1's "bite-sized task contract." The two together make the input-output integrity of plan-from-frame visible.

---

## Implementation procedure

### Step 1: Read existing `/frame` SKILL.md.tmpl

Confirm placement: insert Iron Laws section after `{{NEXUS_STAGE_OVERVIEW}}` and before `## Operator Checklist` (consistent with E.1/E.2/E.3/E.4).

### Step 2: Edit `skills/canonical/frame/SKILL.md.tmpl`

Insert the 3 Iron Laws section (~85-90 lines of prose — slightly longer than prior E phases due to the 7-section enumeration in Law 1). Voice: use Nexus-native concepts (`prd.md`, `decision-brief.md`, advisor record, `ready`/`not_ready` verdict, `/plan` Law 1 reference) — NOT borrowed PM `prd-development` framing wholesale.

### Step 3: Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

This regenerates `skills/canonical/frame/SKILL.md` (canonical Claude, committed) plus the gitignored sidecars (`.agents/`, `.factory/`, `.gemini/`).

### Step 4: Verify

```bash
bun run skill:check                                                                        # clean across all 4 hosts
git grep -n "Iron Law" skills/canonical/frame/SKILL.md                                     # 1 hit (heading)
git grep -n "PRD Content Contract\|Hypothesis Framing\|Framing Readiness Gate" skills/canonical/frame/SKILL.md  # 3 hits
```

### Step 5: Commit

```bash
git add skills/canonical/frame/SKILL.md.tmpl skills/canonical/frame/SKILL.md
git commit -m "skills/canonical/frame: add Iron Laws section (Track E.5)"
```

Per skill-authoring rules, .tmpl + regenerated canonical .md commit together. Sidecars are gitignored.

---

## Acceptance criteria

This phase is complete when:

1. ✅ `skills/canonical/frame/SKILL.md.tmpl` has 3 Iron Laws inserted between `{{NEXUS_STAGE_OVERVIEW}}` and `## Operator Checklist`
2. ✅ `skills/canonical/frame/SKILL.md` is regenerated and contains the same 3 Iron Laws
3. ✅ Iron Law text uses Nexus-native concepts (`prd.md`, `decision-brief.md`, advisor record, `ready`/`not_ready`, explicit reference to `/plan` Law 1) — NOT direct PM/Superpowers/GSD wording
4. ✅ Each Law fits the pattern: (situation/scope) + (≥3 mandatory rules or ≥3 enumerated items) + (consequence of violation)
5. ✅ Law 2's "If/Then/Because" structure is preserved verbatim — it's the recognizable pattern from `epic-hypothesis` and operators expect this format
6. ✅ `bun run skill:check` clean across all 4 hosts
7. ✅ Issue #107 closed with completion comment
8. ✅ Commit message references Track E.5

---

## Voice constraints (CRITICAL)

| Pattern | Don't | Do |
|---|---|---|
| PRD framing | "Write a PRD" | "A PRD missing any of the seven sections is not a PRD; it is a wishlist. Wishlists don't survive contact with `/plan`." |
| Success criteria | "Make them measurable" | "Each one must answer 'what command/metric/artifact reveals whether this is achieved?' … 'task completion rate >85% on the X flow' is accepted." |
| Hypothesis framing | "State a goal" | "**If we [action], then [users] will [outcome] because [evidence].** … 'We think it'll work' is not evidence; it is hope." |
| Cross-skill flow | "Then plan it" | "`/plan` reads the framing verdict from the advisor record. If `not_ready`, `/plan` cannot proceed." |
| Mandatory rule prefix | "Iron Law:" alone | "These three rules apply to every `/frame` invocation regardless of provider, topology, or run mode. They are short and absolute on purpose — discipline that lives in qualifiers does not survive contact with the LLM at decision time." |
| Gate framing | "Check it's done" | "If the framing has no falsifiable success criteria, the tasks have no observable acceptance criteria … the ambiguity propagates straight through `/build`, `/review`, `/qa`, and lands in the user's lap as 'we shipped what we said we'd ship, but the user expected something different.'" |

Reuse the meta-justification ("short and absolute on purpose") from E.1–E.4.

---

## Cross-Iron-Law coherence

Track E's cross-skill chain after E.5 is now end-to-end across the input → output spine:

- **`/frame` Law 1** (PRD content contract) → `prd.md` has 7 sections
- **`/frame` Law 2** (hypothesis framing) → "If we / then / because" populated
- **`/frame` Law 3** (readiness gate) → success criteria falsifiable, non-goals ≥3, user story w/ acceptance, out-of-scope non-empty, hypothesis present
- **`/plan` Law 1** (bite-sized tasks) → each task has observable criterion + verification command
- **`/plan` Law 2** (hypothesis before plan) → ≥3 risks with detection signal + mitigation
- **`/plan` Law 3** (binding handoff) → /build operates only on listed tasks
- **`/build` Law 1** (evidence before claims) → verification command run + output attached
- **`/build` Law 2** (3-strike stop → `/investigate`)
- **`/build` Law 3** (advisories address-or-dispute)
- **`/review` Law 1** (content contract) → 4 artifacts visible at decision time
- **`/review` Law 2** (two-round on block-grade)
- **`/review` Law 3** (disagreement protocol)
- **`/qa` Law 1** (finding evidence contract)
- **`/qa` Law 2** (`/ship`-blocking categories non-negotiable)
- **`/qa` Law 3** (3-strike stop → `/investigate`, mirrors `/build` Law 2)

The chain reads cleanly: framing produces falsifiable hypothesis → plan produces bite-sized tasks bound to that hypothesis → build attaches evidence → review verifies against the contract → QA enforces /ship-blocking → /ship reads the verdicts. Each stage's Iron Laws are visible to the next stage's input check.

---

## Out of scope

- **Schema enforcement at runtime** — this brief covers prose only. A formal `prd.md` schema validator can land as a follow-up if prose proves insufficient.
- **PRD template file** — Iron Law 1's 7-section enumeration is the schema for now. A standalone `docs/product/prd-template.md` is a separate task.
- **`/discover` re-entry on `not_ready`** — Law 3 implies routing to `/discover` is one option. `/discover`'s own Iron Laws are scope of Phase E.6.
- **`design-intent.json` schema** — already declared in artifact contract; not relevant to prose strengthening.
- **PM `prd-development` skill duplication** — we replicate the discipline pattern, NOT the 9-section template wholesale. Nexus's 7-section contract is intentionally tighter.

---

## Risk register

| Risk | Mitigation |
|---|---|
| 7-section requirement feels heavy for small framings | Acceptable. Small framings are fast to fill (alternatives = "considered nothing", risks = "scope underestimation"). The cost of structure is minutes; the cost of a thin PRD is a downstream re-frame. |
| "If we / then / because" format is jargony | The format is explicit on purpose — it's the recognizable industry pattern from `epic-hypothesis`. Operators trained on PM SDD will see it instantly. New operators learn it once. |
| Law 3's gate creates `/frame` → `/discover` round trips | Acceptable. Round trips at framing are minutes; round trips at `/build` are hours. The earlier in the lifecycle the gate fires, the cheaper the iteration. |
| "Falsifiable success criteria" is an unfamiliar standard for some teams | Voice-bake the standard with concrete examples ("p95 latency drops below 300ms" vs "users are happier"). The example is the teaching tool. |
| Iron Laws section lengthens SKILL.md to ~580+ lines | Slightly longer than prior phases due to 7-section enumeration. Within design budget; comparable to `/review` post-strengthening. |

---

## Decision points (resolved)

1. **Law count** — 3 (matches E.1/E.2/E.3/E.4 cadence; audit identified exactly 3 high-leverage gaps)
2. **Section count for PRD contract** — 7 (PM standard is 9; tightened to 7 for Nexus by merging "evidence" into problem statement and "dependencies" into risks)
3. **Hypothesis format** — "If we / then / because" verbatim from `epic-hypothesis`. The format is recognizable and the audit explicitly recommends it.
4. **Cross-skill linkage** — Law 3 references `/plan` Law 1 explicitly. Coherence checked.
5. **Readiness gate vocabulary** — `ready` / `not_ready` (mirrors `/qa` and prior phases)
6. **Out-of-scope vs non-goals distinction** — non-goals are things THIS work will not do; out-of-scope is adjacent things users might expect but this work doesn't address. Both required, separately.
7. **Schema vs prose** — prose only. Schema work deferred.

---

## Effort

- Brief: ~30 min (this document)
- Implementation: ~30-45 min (edit .tmpl with 3 Iron Laws ~85 lines, regenerate, verify, commit)
- **Total: ~1-1.5h**

---

## Implementation kickoff

After this brief commits and the user approves, proceed directly to:

1. Edit `skills/canonical/frame/SKILL.md.tmpl` with the 3 Iron Laws (verbatim from § "Iron Law design" above)
2. `bun run gen:skill-docs` (regenerates canonical Claude + Codex sidecar)
3. `bun run gen:skill-docs --host factory && bun run gen:skill-docs --host gemini-cli` (regenerate other sidecars)
4. `bun run skill:check` — confirm clean across all 4 hosts
5. Verify Iron Laws appear at expected position in `skills/canonical/frame/SKILL.md`
6. Commit canonical .tmpl + .md (sidecars are gitignored)
7. Push to main
8. Close #107 with completion comment

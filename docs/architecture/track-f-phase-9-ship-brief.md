# Track F Phase 9 Brief: Darwin upgrade for `/ship` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**
**Darwin baseline:** 75.2/100 (top of canonical 9 in v3 audit, tied with `/review`).
**Darwin target:** 86–88/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/ship (canonical)" (lines 273–303).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md` (commit `635a7fa`).
**Issue:** #121 (final Track F phase).

---

## Why F.9 (final)

`/ship` is the merge boundary — the last governed gate before code lands on `main`. The Darwin audit scored it joint-highest of the canonical 9 (75.2, tied with `/review`) because Track E's Iron Laws already encode three concrete release-gate disciplines:

| Dimension | Score | Issue |
|---|---|---|
| D1 Frontmatter | 6/10 | Description names release-gate purpose but lacks user-vocabulary triggers |
| D2 Workflow Clarity | 5/10 | Iron Law 3's four-outcome decision tree reads workflow-like, but it's a decision tree, not procedure. No numbered "what to do in what order" surface. |
| D3 Edge Cases | 9/10 | Strongest D3 of canonical 9: Law 1's five mandatory checks + Law 2's three forbidden actions + Law 3's four explicit outcomes |
| D4 Checkpoint Design | 7/10 | Law 2 exception path already requires `AskUserQuestion`; Track F adds a Law 3 branch-decision gate |
| D5 Instruction Specificity | 9/10 | Each check, forbidden action, and outcome named with the artifact field it touches |
| D6 Resource Integration | 7/10 | References `release-gate-record.md`, `pull-request.json`, `canary-status.json`, `deploy-result.json`. Strong. |
| D7 Overall Architecture | 8/10 | Cross-skill law citations to `/review` Law 2, `/qa` Law 2, `/plan` Law 3 |
| D8 Real-World Performance | 8/10 | Rigorous; lacks step-by-step procedure |

The four Track F actions add procedural muscle around the merge boundary without weakening any Iron Law. Because `sensitive: true` is set in the frontmatter, voice must stay precise — `/ship` is the one canonical command where soft framing genuinely costs the user.

---

## Surface map

### File modified

```
skills/canonical/ship/SKILL.md.tmpl    ← edit (frontmatter + workflow + typical prompts)
skills/canonical/ship/SKILL.md         ← regenerated by gen:skill-docs (Claude host)
.agents/skills/ship/SKILL.md           ← regenerated (Codex host)
.factory/skills/ship/SKILL.md          ← regenerated (Factory host)
.gemini/skills/ship/SKILL.md           ← regenerated (Gemini CLI host)
```

### File NOT modified

- `lib/nexus/stage-content/ship/*` — runtime content; not skill prose
- Iron Laws section (Laws 1, 2, 3) — preserved verbatim, including Law 1's five mandatory checks, Law 2's refuse-to-ship-broken with documented exception path, and Law 3's four-outcome branch decision tree
- `sensitive: true` frontmatter field — preserved
- `allowed-tools` — already includes `AskUserQuestion` from Track E
- All other canonical/support skills — out of scope for this phase
- Resolver code (`scripts/gen-skill-docs.ts`, `lib/nexus/...`) — no resolver changes per phase constraints

### Frontmatter changes

Description gets trigger phrases appended. `sensitive: true` and `allowed-tools` (with `AskUserQuestion`) are preserved exactly.

---

## Action 1 — Numbered workflow design

Insert a new `## How to run /ship` section between the Iron Laws section and the Operator Checklist.

**Voice (Nexus-native):**

```markdown
## How to run /ship

These steps execute one `/ship` run. Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply. `/ship` is the merge boundary — every step here either confirms the readiness gate or refuses the merge.

### Step 1 — Verify the pre-merge readiness gate

Per Law 1, `/ship` cannot mark `release-gate-record.md` `ready` without all five mandatory checks visible at decision time:

1. Tests pass with output evidence in this turn
2. CI green on the head SHA
3. `/review` `pass_round` is current and not stale
4. `/qa` verdict is `ready` or `ready_with_findings` (when `/qa` was run)
5. Branch is up-to-date with base

Walk each check explicitly. Stale CI from a prior turn does not count; a `/review` pass invalidated by later commits does not count. If a check needs rerunning, rerun it in this turn.

### Step 2 — If any check fails, refuse the merge

Per Law 2, `/ship` MUST refuse when any readiness check (Law 1) fails. The advisor record carries `release_gate: not_ready` with the failing check enumerated, and the operator routes back through the relevant stage (`/build`, `/review`, `/qa`).

Three shortcuts are forbidden regardless of operator framing:

- No silent merges past failed checks
- No force-push to base or shared branches
- No `[skip ci]`, `--no-verify`, or branch-protection bypasses

Bad work is worse than no work. The exception path (Law 2) is explicit, time-boxed, and requires `AskUserQuestion` confirmation plus a recorded rationale in `release-gate-record.md`. Silent override is not an option.

### Step 3 — Pick the branch outcome

If the readiness gate is verified, surface the four explicit outcomes per Law 3 and let the operator pick. Use `AskUserQuestion`:

> "The readiness gate is verified. How should this branch end?
> (a) Merge — the PR merges to base, branch deleted per repo policy
> (b) Keep alive — record `release_gate: deferred` with a one-paragraph reason (blocked dep, design pending, scheduled for next window)
> (c) Discard — record `release_gate: discarded` with a discovery-style note (what was tried, what was learned)
> (d) Split — record `release_gate: split` and route back to `/plan` Law 3 to extend the contract for the unmergeable subset"

`/ship` does not auto-pick. Outcomes 2–4 are honest endings, not failures; the only failure mode is "neither merging nor explicitly choosing one of the other three." Branches in limbo become operational debt.

### Step 4 — Push the ship branch (if merging)

If the operator picked Merge, ensure the ship branch is pushed before PR creation. If `pull-request.json` records `status: "push_required"`, `/ship` remains not-ready until the branch is pushed and `/ship` is rerun to create or reuse the PR handoff. `/closeout` must not run while `push_required` is on the record.

For Keep alive / Discard / Split, push state is informational only — the outcome is recorded and the merge does not happen.

### Step 5 — Write `release-gate-record.md`

Write the canonical release-gate artifact. It records:

- `release_gate: ready | not_ready | deferred | discarded | split`
- The five Law 1 checks with their evidence (test output reference, CI run URL, `pass_round` value, `/qa` verdict, base-branch SHA)
- For `not_ready`, the failing check with its block reason
- For `deferred | discarded | split`, the rationale per Law 3
- For any Law 2 exception path use, the recorded override with rationale and `AskUserQuestion` confirmation timestamp

This artifact is the contract `/closeout` consumes. Ambiguity here lands as broken main or laundered runs downstream.

### Step 6 — Record outcome in `pull-request.json`

`pull-request.json` carries the PR handoff state (`open`, `merged`, `push_required`, etc.) and the chosen Law 3 outcome. The PR handoff belongs to `/ship` — `/closeout` verifies the completed handoff but does not create or repair PR state (per `/closeout` Law 3).

For follow-on attestations, `/canary` may add `canary-status.json` and `/land-and-deploy` may add `deploy-result.json` without changing the canonical ship state recorded here.

### Step 7 — Write status

Run the canonical command (in the Routing section below). It writes `release-gate-record.md`, `checklist.json`, `deploy-readiness.json`, `pull-request.json`, `status.json`, and (optionally) `learning-candidates.json`. The advisor record carries the verdict; `/closeout` reads it.
```

This Action lifts D2 Workflow Clarity from 5/10 to ~8/10 (+3 raw, ×15 = +4.5 weighted) and D8 Real-World Performance from 8/10 to ~9.5/10 (+1.5 raw, ×25 = +3.75 weighted). Combined: ~+8 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus ship command. Persists the governed release gate, checklist state,
  and structured ship readiness for the governed Nexus lifecycle. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus ship command. Persists the governed release gate, checklist state,
  and structured ship readiness for the governed Nexus lifecycle. Use when the user
  says "ship it", "merge to main", "release the change", or asks to gate a branch
  on its pre-merge readiness checks. (nexus)
```

Lifts D1 Frontmatter from 6/10 to ~8/10 (+2 raw, ×8 = +1.6 weighted).

`sensitive: true` and `allowed-tools` (with `AskUserQuestion`) preserved exactly. Frontmatter character count remains well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the `Run:` block, before the `{{NEXUS_COMPLETION_ADVISOR:ship}}` footer.

**Voice:**

```markdown
## Typical prompts

These are example user requests `/ship` handles well. The skill verifies the readiness gate, surfaces the branch-outcome decision, and writes the canonical release-gate artifact.

### Prompt 1 — Standard merge

> "Ship it."

`/ship` walks Step 1 to verify all five Law 1 checks (tests fresh in this turn, CI green on head SHA, `pass_round` current, `/qa` verdict ready or ready_with_findings, branch up-to-date with base). If any check is stale or fails, Step 2 refuses the merge and surfaces which check blocked. If all pass, Step 3 surfaces the four-outcome decision; on Merge, Steps 4–7 push the branch (if needed), record `release_gate: ready` in `release-gate-record.md`, and write `pull-request.json` with the merged status.

### Prompt 2 — P0 surfaced but operator wants to merge anyway

> "QA found a P0 but I want to ship — we'll patch it next sprint."

Per Law 2, "no silent merges past failed checks." Step 1's check 4 reads the `/qa` verdict (`not_ready` because of the P0); Step 2 refuses the merge and surfaces the failing check with its block reason. The exception path is the only release valve, and it requires explicit `AskUserQuestion` confirmation plus a recorded rationale in `release-gate-record.md` (per Law 2). `/ship` does not silently override; the operator chooses to invoke the exception path on the record, or routes back through `/qa`.

### Prompt 3 — In-progress branch the team isn't ready to delete

> "This branch isn't ready to merge but we don't want to discard it — we'll come back to it."

Step 3 surfaces the four-outcome `AskUserQuestion`; the operator picks Keep alive. `/ship` records `release_gate: deferred` with a one-paragraph reason ("blocked on dependency X", "awaiting design review", "scheduled for next window"). The branch remains; `/closeout` may not consume it yet. This is Law 3 outcome 2 — an honest ending, not a failure.

These prompts test that `/ship` enforces the readiness gate (Law 1), refuses to ship broken (Law 2), and surfaces the four explicit branch outcomes (Law 3) rather than auto-picking or silently merging.
```

Lifts D8 from ~9.5/10 toward 9.5/10 (already partially captured by Action 1; cumulative gain captured above).

---

## Action 4 — AskUserQuestion gate

Bundled into Action 1 Step 3 — the four-outcome branch-decision gate fires when the readiness gate is verified and the operator must pick Merge / Keep alive / Discard / Split. No additional placement needed.

This is a genuine new checkpoint. Track E already added an `AskUserQuestion` for the Law 2 exception path (P0-but-ship-anyway override); Track F adds the Law 3 branch-decision gate so every `/ship` run that reaches the gate explicitly surfaces the four outcomes rather than defaulting to merge.

Lifts D4 Checkpoint Design from 7/10 to ~8.5/10 (+1.5 raw, ×7 = +1.05 weighted).

---

## Projected Darwin score after F.9

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6 | 8 | +2 | +1.6 |
| D2 Workflow Clarity | 5 | 8 | +3 | +4.5 |
| D3 Edge Cases | 9 | 9 | 0 | 0 |
| D4 Checkpoint Design | 7 | 8.5 | +1.5 | +1.05 |
| D5 Instruction Specificity | 9 | 9 | 0 | 0 |
| D6 Resource Integration | 7 | 7.5 | +0.5 | +0.25 |
| D7 Overall Architecture | 8 | 8.5 | +0.5 | +0.75 |
| D8 Real-World Performance | 8 | 9.5 | +1.5 | +3.75 |

**Projected total:** 75.2 + 11.9 ≈ **87.1/100** (target was 86–88; F.9 lands inside the band).

---

## Implementation procedure

### Step 1: Edit `skills/canonical/ship/SKILL.md.tmpl`

Apply 3 changes:

1. Frontmatter description (Action 2)
2. Insert `## How to run /ship` between Iron Laws and Operator Checklist (Action 1)
3. Insert `## Typical prompts` after `Run:` block, before the Completion Advisor footer (Action 3)
4. Action 4 is bundled into Action 1 Step 3 — no separate change

Preserve `sensitive: true` and `allowed-tools` exactly. Preserve all three Iron Laws verbatim — Law 1's five mandatory checks, Law 2's refuse-to-ship-broken with documented exception path, Law 3's four-outcome branch decision tree.

### Step 2: Regenerate

```bash
bun run gen:skill-docs                       # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 3: Verify

```bash
bun run skill:check                                                                       # clean across all 4 hosts
git grep -n "How to run /ship" skills/canonical/ship/SKILL.md                             # 1 hit
git grep -n "Typical prompts" skills/canonical/ship/SKILL.md                              # 1 hit
git grep -n "ship it" skills/canonical/ship/SKILL.md                                      # ≥1 hit (frontmatter trigger)
git grep -n "merge to main" skills/canonical/ship/SKILL.md                                # ≥1 hit (frontmatter trigger)
git grep -n "release the change" skills/canonical/ship/SKILL.md                           # 1 hit (frontmatter trigger)
diff <(sed -n '/^### Law 1/,/^## How to run/p' skills/canonical/ship/SKILL.md.tmpl) \
     <(sed -n '/^### Law 1/,/^## How to run/p' .git/show-ref-baseline-ship.tmpl) || true  # Iron Laws diff empty
```

### Step 4: Commit + push

```bash
git add skills/canonical/ship/SKILL.md.tmpl \
        skills/canonical/ship/SKILL.md \
        .agents/skills/ship/SKILL.md \
        .factory/skills/ship/SKILL.md \
        .gemini/skills/ship/SKILL.md \
        docs/architecture/track-f-phase-9-ship-brief.md
git commit -m "skills/canonical/ship: Track F.9 Darwin upgrade"
git push -u origin track-f-phase-9-ship
```

Issue #121 is NOT closed in this phase per orchestration constraint — the orchestrator verifies the full Track F chain after merging all 8 branches.

---

## Acceptance criteria

This phase is complete when:

1. ✅ Frontmatter description carries trigger phrases ("ship it" / "merge to main" / "release the change")
2. ✅ `sensitive: true` preserved in frontmatter
3. ✅ `## How to run /ship` section inserted between Iron Laws and Operator Checklist with 7 numbered steps
4. ✅ Step 3 includes explicit `AskUserQuestion` text for the Law 3 four-outcome branch decision (Merge / Keep alive / Discard / Split)
5. ✅ `## Typical prompts` section with 3 examples added (standard merge, P0-but-ship-anyway, keep-alive deferral)
6. ✅ Iron Laws preserved verbatim (Laws 1, 2, 3 — no changes to the five mandatory checks, the refuse-to-ship-broken language, or the four-outcome decision tree)
7. ✅ `bun run skill:check` clean across all 4 hosts
8. ✅ Branch `track-f-phase-9-ship` pushed
9. ✅ Issue #121 NOT closed (orchestrator verifies after full chain merges)
10. ✅ Commit message references Track F.9

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First verify the gate, then merge" | "Step 1 — Verify the pre-merge readiness gate. Per Law 1, `/ship` cannot mark `release-gate-record.md` `ready` without all five mandatory checks visible at decision time..." |
| AskUserQuestion framing | Vague "ask the operator" | Explicit four-outcome enumeration: "(a) Merge / (b) Keep alive / (c) Discard / (d) Split" with each option's recorded artifact field |
| Cross-skill reference | "Then closeout reads it" | "`/closeout` reads it. The PR handoff belongs to `/ship`; `/closeout` verifies the completed handoff but does not create or repair PR state (per `/closeout` Law 3)." |
| Workflow vs Iron Law | Either or both | Both, with explicit relationship: "Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply." |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "ship it", "merge to main", "release the change" |
| Sensitivity | Soft "should not silently merge" | Iron-Law-tight: "Bad work is worse than no work. Silent override is not an option." |

The numbered workflow is NOT an Iron Law — it's procedure. Don't add "MUST" or "non-negotiable" framing to the steps; the steps are guidance, the Iron Laws are constraints. But because `/ship` carries `sensitive: true`, voice must stay precise on the refusal language — the failure mode at the merge boundary is irreversible.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Numbered workflow ends up duplicating Iron Law content | Workflow steps reference Iron Laws by name (e.g., "per Law 1", "per Law 2", "per Law 3") rather than restating them. The two surfaces stay distinct. |
| 7 steps feel verbose for happy-path "ship it" | Steps 4 and 6 are explicitly conditional on the Step 3 outcome. A standard merge runs Steps 1, 3, 4, 5, 6, 7 — six concrete actions, all gated on the readiness check. Keep-alive / Discard / Split runs Steps 1, 3, 5 — three actions. |
| Law 3 four-outcome `AskUserQuestion` could feel paternalistic | The audit specifically flagged the absence of this gate (D4 7/10 → 8.5/10). The gate is the only structural way to prevent "merge by default" when the operator hasn't explicitly chosen. The text frames it as "how should this branch end?" rather than "are you sure?" |
| Total SKILL.md length grows | Within design budget (canonical Track E skills are 565–632 lines; current `/ship` is 632 lines per audit). The added sections add ~120–150 lines of prose, comparable to F.1's `/discover` upgrade. |
| `sensitive: true` accidentally dropped during regeneration | The frontmatter resolver passes through frontmatter fields verbatim; we verify post-regeneration with `git grep -n "sensitive: true" skills/canonical/ship/SKILL.md` (≥1 hit expected). |
| Iron Laws drift during edit | Pre-edit, capture a baseline of the Iron Laws block; post-edit, diff. Acceptance criterion 6 is explicit. |

---

## Effort

- Brief: ~20 min (this document)
- Implementation: ~25–35 min (3 edits to one .tmpl file, regenerate 4 hosts, verify, commit, push branch)
- **Total: ~1h** (matches RFC's S effort estimate)

---

## What this phase finishes

F.9 is the final Track F phase. After all 8 Track F branches (F.2 through F.9) merge, the orchestrator runs the v4 Darwin re-audit to confirm:

- Canonical 9 Darwin mean ≥ 85/100 (control parity, up from 73.1)
- v2 5-dim mean unchanged at ~4.42 (no rigor regression)
- All 9 canonical SKILL.md files carry the four Darwin actions: numbered workflow, frontmatter triggers, typical prompts, and (where applicable) AskUserQuestion gates at Iron Law branching points

If the v4 audit confirms the projection, Track F closes. The Iron Laws scaffold from Track E remains the structural backbone; Track F's procedural muscle sits on top.

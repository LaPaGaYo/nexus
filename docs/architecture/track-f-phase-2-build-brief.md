# Track F Phase 2 Brief: Darwin upgrade for `/build` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**
**Darwin baseline:** 69.5/100 (tied with `/plan` for second-lowest of canonical 9).
**Darwin target:** 82–84/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/build (canonical)" (lines 62–99).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md` (commit `635a7fa`).
**Predecessor brief:** `docs/architecture/track-f-phase-1-discover-brief.md` (commit `cb50dc4`).
**Issue:** #114.

---

## Why F.2 second

`/build` and `/plan` share the lowest Darwin score after `/discover` (69.5 each) and pull the canonical mean down. F.2 takes `/build` because:

1. **Sequence dependency:** `/build` is the execution surface that consumes `/plan`'s sprint contract. Lifting it first keeps the lifecycle ordered top-down (discover → build → plan) when readers walk the canonical 9.
2. **High-leverage Iron Law 2 hook:** `/build` Law 2 (3-strike stop) is the strongest Iron Law branching point in the canonical 9. F.2 surfaces it as an explicit `AskUserQuestion` gate, which is the single largest D4 lift available across the track.
3. **Verification matrix already concrete.** Iron Law 1 names per-change-type verification commands (`bun test`, `bunx tsc --noEmit`, `bun run skill:check`, etc.). The numbered workflow can lean on that matrix without restating it.

Per audit (lines 62–99):

| Dimension | Score | Issue |
|---|---|---|
| D1 Frontmatter | 6/10 | "no trigger phrases, no when-to-invoke language, no examples of the user request that should fire it" |
| D2 Workflow Clarity | 5/10 | "There are no numbered Step 1 / Step 2 / Step 3 sections that an operator could follow if the runtime were absent" |
| D3 Edge Cases | 8/10 | Iron Law 2 + Law 3 + Stage-Aware Topology Chooser cover most failure paths |
| D4 Checkpoint Design | 6/10 | "3-strike rule is a routing event, not a user-confirmation gate" |
| D5 Instruction Specificity | 8/10 | Verification matrix is concrete; advisory address-or-dispute is concrete |
| D6 Resource Integration | 7/10 | Path inventory is clean |
| D7 Overall Architecture | 8/10 | Clean separation; cross-skill law citations strong |
| D8 Real-World Performance | 7/10 | Most-typical prompt ("just implement task X") drags the average; reader gets constraints, not concrete steps |

The audit explicitly recommends (line 687):

> `/build` could add: "Step 1: Read the binding contract from `sprint-contract.md`. Step 2: For each task, run the verification command listed in `verification-matrix.json` BEFORE editing. Step 3: Edit the task's files... Step 4: Re-run the verification command and attach output. Step 5: Repeat per task. Step 6: Aggregate results into `build-result.md`."

And (line 698):

> `/build` Law 2 (3-strike) routes to `/investigate` — could surface AskUserQuestion ("Continue investigating / Escalate / Add logging") like `/investigate` Phase 3 does.

F.2 implements both.

---

## Surface map

### File modified

```
skills/canonical/build/SKILL.md.tmpl    ← edit (frontmatter + workflow + typical prompts + AskUserQuestion gate)
skills/canonical/build/SKILL.md         ← regenerated by gen:skill-docs
.agents/skills/build/SKILL.md           ← regenerated by gen:skill-docs --host codex
factory/.factory/skills/build/SKILL.md  ← regenerated by gen:skill-docs --host factory
.gemini/skills/build/SKILL.md           ← regenerated by gen:skill-docs --host gemini-cli
```

### File NOT modified

- `lib/nexus/stage-content/build/*` — runtime content; not skill prose
- Iron Laws section (lines 20–55 of the .tmpl) — preserved verbatim
- `scripts/resolvers/completion-advisor.ts` — already has `build` from Track E
- All other canonical/support skills — out of scope for this phase

### Frontmatter changes

Description gets trigger phrases appended. `allowed-tools` already has `AskUserQuestion` from Track E (used by completion advisor and now by Action 4's gate).

---

## Action 1 — Numbered workflow design

Insert a new `## How to run /build` section between the Iron Laws section (ends line 55) and the Operator Checklist (starts line 57).

**Voice (Nexus-native):**

```markdown
## How to run /build

These steps execute one `/build` run. Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply.

### Step 1 — Read the binding contract

Open `.planning/current/plan/sprint-contract.md` (and `.planning/current/plan/design-contract.md` if present). The sprint contract is the binding handoff per `/plan` Law 3 — every task in scope is named there with an observable acceptance criterion and a verification command. Anything not named is out of scope.

If the contract is missing, `/build` cannot proceed. Route back to `/plan` rather than improvising scope.

### Step 2 — Pick the next task and identify its verification command

Each task in `sprint-contract.md` has a verification command per `/plan` Law 1 (e.g., `bun test test/foo.test.ts`, `bunx tsc --noEmit`, `bun run skill:check`). That command is the contract for "this task is done" — you will run it twice, once before editing and once after.

If the task lacks a verification command, treat that as a `/plan` Law 1 violation and route back to `/plan` to extend the contract before editing.

### Step 3 — Run the verification command BEFORE editing

Execute the task's verification command first. Capture exit code and output. The pre-edit run establishes the baseline failure (red state) the edit must turn green; it also catches the case where the test was already passing and the task description was stale.

If the pre-edit run is already green, stop and ask: is the task already done, is the verification command wrong, or is the test under-specified? Resolve before editing.

### Step 4 — Edit the task's files

Make the smallest change that satisfies the task's acceptance criterion. Touch only files the task names; resist scope creep into adjacent code per `/plan` Law 3 (no silent scope expansion).

If the edit reveals the task needs more files than `sprint-contract.md` lists, STOP. Do not silently expand. Route back to `/plan` to extend the contract.

### Step 5 — Re-run the verification command and attach output

Execute the same verification command from Step 3. Capture exit code and full output. Per Law 1 (evidence-before-claims), this output is what the advisor record will read; "tests passed" without attached output is not verification.

If the run is green, the task is verified — proceed to Step 7.

If the run is red, proceed to Step 6.

### Step 6 — Handle failure (per Law 2)

A failed verification is normal. Diagnose, edit, re-verify. Track consecutive failures on the same root cause.

When **3 consecutive failures share the same root cause**, Law 2's hard stop fires. Use `AskUserQuestion`:

> The same failure has reproduced 3 times: [one-line root-cause description].
> Per `/build` Law 2, continuing to patch symptoms past 3 strikes is the most common path to a wrong fix shipped under stage-status `ready`.
>
> RECOMMENDATION: Choose B if the failure is genuinely about understanding why; choose A only if you have a fresh hypothesis that wasn't tested in the prior 3 attempts. Completeness: B 10/10, A 7/10, C 6/10.
> A) Continue investigating with a new hypothesis (human: ~15m / CC: ~5m) — Completeness: 7/10
> B) Route to `/investigate` for the 4-phase root-cause protocol (human: ~30m / CC: ~10m) — Completeness: 10/10
> C) Add diagnostic logging and re-run before forming the next hypothesis (human: ~15m / CC: ~5m) — Completeness: 6/10

Record the operator's choice in `build-result.md`. If B, hand off to `/investigate` and stop the current `/build` run; the investigate output drives the next `/build` re-entry.

### Step 7 — Repeat per task

Loop Step 2 through Step 6 for every task in `sprint-contract.md`. Per-task verification output stays attached for `/review` Law 1 to consume — fresh evidence is non-fungible.

If a task can't be completed without breaking another, that's a `/plan` Law 3 scope-extension trigger, not a license to silently rework the contract.

### Step 8 — Address prior advisories (per Law 3)

If this `/build` is a re-entry from `/review` with advisories on the advisor record:

- Address every advisory before claiming complete, OR
- Document explicit dispute in `build-result.md` (cite advisory id, state rationale, propose alternative resolution)

Silently dropping advisories is forbidden per Law 3. Disputed advisories remain visible to `/review` and `/ship` for downstream re-evaluation.

### Step 9 — Aggregate into `build-result.md` and write status

Collect per-task verification output, resolved advisories, disputed advisories, and any scope notes. Write `build-result.md` and `status.json` via the canonical command (in the Routing section below). The advisor record carries the verdict; `/review` reads it next.

If the advisor returns `requires_user_choice`, follow the advisor's branching prompt — that's a runtime checkpoint, not a workflow step the operator picks unprompted.
```

This Action lifts D2 Workflow Clarity from 5/10 to ~8/10 (+3 raw, ×15 = +4.5 weighted) and D8 Real-World Performance from 7/10 to ~9/10 (+2 raw, ×25 = +5 weighted). Combined: ~+9.5 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus build command. Executes the bounded governed implementation contract
  and writes repo-visible build request, result, and stage status artifacts. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus build command. Executes the bounded governed implementation contract
  and writes repo-visible build request, result, and stage status artifacts. Use when
  the user says "implement task X", "build the feature", "execute the sprint contract",
  or otherwise asks to turn a planned task into shipped code with verification. (nexus)
```

Lifts D1 Frontmatter from 6/10 to ~8/10 (+2 raw, ×8 = +1.6 weighted).

Frontmatter character count: still well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the Routing block, before the Completion Advisor footer.

**Voice:**

```markdown
## Typical prompts

These are example user requests `/build` handles well. The skill consumes the bounded sprint contract, runs per-task verification, and produces `build-result.md` plus `status.json`.

### Prompt 1 — Single bite-sized task

> "Implement task #3 from sprint-contract.md."

`/build` walks Step 1 (read contract) → Step 2 (pick task #3, find its verification command) → Step 3 (pre-edit run, captures red baseline) → Step 4 (edit) → Step 5 (post-edit run, captures green output) → Step 8 (no prior advisories on a fresh build) → Step 9 (aggregate into `build-result.md`). The advisor record carries `stage_outcome: ready`, `/review` picks up next.

### Prompt 2 — Stuck on the same error

> "I've tried fixing this test failure three times and it keeps coming back the same way."

Step 6 fires. Per Law 2, three consecutive failures on the same root cause is a hard stop. The `AskUserQuestion` gate surfaces three options: continue with a new hypothesis (only valid if genuinely new), route to `/investigate` for the 4-phase protocol (recommended), or add diagnostic logging before re-attempting. The operator's choice is recorded in `build-result.md`; if `/investigate` is chosen, the current build halts and re-enters after investigation completes.

### Prompt 3 — Re-entering after review found advisories

> "Review left 4 advisories on my last build — finish the implementation."

Step 8 governs. Per Law 3, every advisory is address-or-dispute. The build addresses each advisory, OR documents disagreement with explicit rationale (advisory id, why-disagree, alternative-resolution) in `build-result.md`. Silently dropping any advisory is forbidden — disputed ones remain visible to `/review` and `/ship` for downstream re-evaluation.

These prompts test that `/build` produces verified, contract-bounded implementation output — not symptomatic patches (Iron Law 2's failure mode) and not silent scope expansion (Iron Law 3's failure mode).
```

Lifts D8 from ~9/10 toward 9.5/10 (already partially captured by Action 1; cumulative gain ≈ +0.5 raw, ×25 = +1.25 weighted).

---

## Action 4 — AskUserQuestion gate at Iron Law 2 branching point

Bundled into Action 1 Step 6. Law 2's 3-strike stop has always implied operator branching ("STOP. Route to `/investigate`."), but the prose left the choice implicit. Step 6 makes it explicit:

- A) Continue with a new hypothesis (only if genuinely new)
- B) Route to `/investigate` (recommended — Law 2's named recovery path)
- C) Add diagnostic logging before re-attempting

Mirrors `/investigate` Phase 3's own 3-strike gate so the two skills handle stuckness with parallel UX. Recorded choice goes in `build-result.md`.

Lifts D4 Checkpoint Design from 6/10 to ~7.5/10 (+1.5 raw, ×7 = +1.05 weighted).

---

## Projected Darwin score after F.2

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6 | 8 | +2 | +1.6 |
| D2 Workflow Clarity | 5 | 8 | +3 | +4.5 |
| D3 Edge Cases | 8 | 8.5 | +0.5 | +0.5 (Step 3 catches stale-test edge case Iron Laws didn't name) |
| D4 Checkpoint Design | 6 | 7.5 | +1.5 | +1.05 |
| D5 Instruction Specificity | 8 | 8 | 0 | 0 |
| D6 Resource Integration | 7 | 7.5 | +0.5 | +0.25 (Step 1 names sprint-contract.md + design-contract.md upstream) |
| D7 Overall Architecture | 8 | 8 | 0 | 0 |
| D8 Real-World Performance | 7 | 9 | +2 | +5 |

**Projected total:** 69.5 + 12.9 ≈ **82.4/100** (target was 82–84; F.2 lands in the band).

---

## Implementation procedure

### Step 1: Edit `skills/canonical/build/SKILL.md.tmpl`

Apply 3 changes:
1. Frontmatter description (Action 2)
2. Insert `## How to run /build` between Iron Laws section and Operator Checklist (Action 1 — includes Step 6's AskUserQuestion gate from Action 4)
3. Insert `## Typical prompts` after the Routing block (Action 3)

### Step 2: Regenerate

```bash
bun run gen:skill-docs                  # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 3: Verify

```bash
bun run skill:check                                                                          # clean across all 4 hosts
git grep -n "How to run /build" skills/canonical/build/SKILL.md                              # 1 hit
git grep -n "Typical prompts" skills/canonical/build/SKILL.md                                # 1 hit
git grep -n "implement task X" skills/canonical/build/SKILL.md                               # 1 hit (frontmatter trigger)
git diff main..track-f-phase-2-build skills/canonical/build/SKILL.md.tmpl | grep '^-.*Iron Law\|^-.*Law [123]'   # empty (Iron Laws preserved)
```

### Step 4: Commit + push

```bash
git add docs/architecture/track-f-phase-2-build-brief.md \
        skills/canonical/build/SKILL.md.tmpl \
        skills/canonical/build/SKILL.md \
        .agents/skills/build/SKILL.md \
        factory/.factory/skills/build/SKILL.md \
        .gemini/skills/build/SKILL.md
git commit -m "skills/canonical/build: Track F.2 Darwin upgrade"
git push -u origin track-f-phase-2-build
```

Issue #114 is left open — the orchestrator closes it after the branch merges.

---

## Acceptance criteria

This phase is complete when:

1. ✅ Frontmatter description carries trigger phrases ("implement task X" / "build the feature" / "execute the sprint contract")
2. ✅ `## How to run /build` section inserted between Iron Laws and Operator Checklist with 9 numbered steps
3. ✅ Step 6 includes explicit `AskUserQuestion` text for Law 2's 3-strike stop with three options (continue / investigate / add logging)
4. ✅ `## Typical prompts` section with 3 examples added
5. ✅ Iron Laws preserved verbatim (no changes to lines 20–55 of the .tmpl)
6. ✅ `bun run skill:check` clean across all 4 hosts
7. ✅ Commit message references Track F.2
8. ✅ Branch `track-f-phase-2-build` pushed to origin

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First do X, then do Y" | "Step 1 — Read the binding contract. Open `.planning/current/plan/sprint-contract.md`..." |
| AskUserQuestion framing | Vague "ask user" | Explicit options: "A) Continue with new hypothesis / B) Route to `/investigate` / C) Add diagnostic logging" with completeness scores |
| Cross-skill reference | "Then review it" | "`/review` Law 1 reads what you attached. Fresh evidence is non-fungible." |
| Workflow vs Iron Law | Either or both | Both, with explicit relationship: "Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply." |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "implement task X", "build the feature", "execute the sprint contract" |

The numbered workflow is NOT an Iron Law — it's procedure. Don't add "MUST" or "non-negotiable" framing to the steps; the steps are guidance, the Iron Laws are constraints.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Numbered workflow ends up duplicating Iron Law content | Workflow steps reference Iron Laws by name (e.g., "per Law 1", "Law 2's hard stop") rather than restating them. The two surfaces stay distinct. |
| Step 3 (pre-edit run) feels heavy for trivial changes | Pre-edit run catches the stale-task case at no real cost — the verification command is fast for unit tests and `tsc --noEmit`. For long-running suites the operator can scope (`bun test test/foo.test.ts`) per `/plan` Law 1's per-task command. |
| AskUserQuestion gate in Step 6 fires on transient flakes | The gate is gated on "same root cause" — flaky-test transients are different root causes per attempt and don't trigger 3-strike. Operator judgment on "same root cause" is the existing Law 2 contract; F.2 just makes the choice explicit. |
| Adding Typical prompts increases SKILL.md length beyond budget | Current `/build` SKILL.md is 611 lines per audit. F.1's `/discover` ended near 600 with similar additions; F.2 should land 600–650, comparable. |
| Workflow voice drifts toward "MUST" framing under pressure | F.1 brief explicitly forbade this; F.2 brief mirrors. Steps 4, 5, 6 use "make", "execute", "diagnose" — not "MUST". |

---

## Effort

- Brief: ~25 min (this document)
- Implementation: ~30–45 min (3 edits to one .tmpl file, regenerate across 4 hosts, verify, commit)
- **Total: ~1h** (matches RFC's M effort estimate)

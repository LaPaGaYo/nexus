# Track F Phase 4 Brief: Darwin upgrade for `/handoff` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**
**Darwin baseline:** 72.7/100 (mid-pack of canonical 9; #4 lowest in v3 audit).
**Darwin target:** 83–85/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/handoff (canonical)" (lines 341–371).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md` (commit `635a7fa`).
**Issue:** #116.

---

## Why F.4 is unusual

`/handoff` already carries the strongest D3/D5 instrumentation of any canonical Track E skill (Law 1's four-dimension freshness check, Law 2's six recovery categories, Law 3's four context-dump fields). The Iron Laws are mechanical-grade. The deficiency is upstream of constraint quality: the operator can read all three laws and still not know what to *do first*.

Per audit (lines 341–371):

| Dimension | Score | Issue |
|---|---|---|
| D1 Frontmatter | 6/10 | "Use after `/plan` declares the work execution-ready" — accurate but ambient; lacks user-vocabulary triggers |
| D2 Workflow Clarity | 5/10 | NO numbered workflow; Operator Checklist three bullets ("record requested route / validate transport / require separate Nexus approval"); Iron Laws constrain re-validation, not workflow |
| D3 Edge Cases | 9/10 | Best-in-class Iron Law coverage of failure modes |
| D4 Checkpoint Design | 7/10 | Law 2's blocked-recovery `AskUserQuestion` is a real gate; Law 1 (freshness) implies routing decisions but no explicit gate |
| D5 Instruction Specificity | 9/10 | Best-in-class — every dimension and label enumerated |
| D6 Resource Integration | 7/10 | Refs `governed-execution-routing.md`, `governed-handoff.md`, status.json, `sprint-contract.md` |
| D7 Overall Architecture | 8/10 | Single canonical continuation into `/build`; advisor footer present |
| D8 Real-World Performance | 7/10 | The most-typical prompt ("hand off to Codex") needs the runtime to assemble routing; skill specifies contract but not assembly order |

The bottleneck: Law 1 has four dimensions but no operator-visible procedure for *checking* them. An operator reading the skill knows what must be true, not what to do to discover whether it is true.

---

## Surface map

### File modified

```
skills/canonical/handoff/SKILL.md.tmpl    ← edit (frontmatter + workflow + typical prompts)
skills/canonical/handoff/SKILL.md         ← regenerated by gen:skill-docs
skills/codex/handoff/SKILL.md             ← regenerated
skills/factory/handoff/SKILL.md           ← regenerated
skills/gemini-cli/handoff/SKILL.md        ← regenerated
```

### File NOT modified

- `lib/nexus/stage-content/handoff/*` — runtime content; not skill prose
- Iron Laws section (lines 21–62 of .tmpl) — preserved verbatim
- `scripts/resolvers/completion-advisor.ts` — `handoff` entries already present in STAGE_FIELDS and STAGE_NOTES from Track E; no change needed
- All other canonical/support skills — out of scope for this phase
- `test/gen-skill-docs.test.ts` — orchestrator handles centrally per Track F constraints

### Frontmatter changes

Description gets trigger phrases appended. `allowed-tools` already has `AskUserQuestion` from Track E — no change.

---

## Action 1 — Numbered "How to run /handoff" workflow

Insert a new `## How to run /handoff` section between the Iron Laws section (ends line 62) and the Operator Checklist (starts line 64).

**Voice (Nexus-native):**

```markdown
## How to run /handoff

These steps execute one `/handoff` run. Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply.

### Step 1 — Validate framing + plan completeness

Before opening any routing question, confirm the upstream contract is real:

- `prd.md` exists and `/frame` Law 1 was satisfied (named user, falsifiable hypothesis, design contract if design-bearing).
- `sprint-contract.md` exists and `/plan` Law 3 declared this run handoff-ready.
- The `status.json` for `/plan` shows the work execution-ready, not advisory or returned-to-frame.

If any upstream artifact is missing or in a non-ready state, `/handoff` cannot legitimately approve a route. Return to the owning skill (`/frame` or `/plan`) before proceeding.

### Step 2 — Run the four-dimension freshness check

Per Law 1, the prior approval (if any) must be re-validated against four dimensions before reuse. Even on first entry, capture the current state of each dimension so future re-entries have a baseline:

1. **Route** — read `governed-execution-routing.md` (if it exists). Note the named stage pack, provider lane, and topology.
2. **Provider availability** — confirm the provider lane (CCB session, Codex CLI, Gemini CLI, local) is mounted and online *now*. A provider online at approval time but unmounted now invalidates the prior approval.
3. **Branch state** — `git rev-parse HEAD` and `git status` to capture the current branch and tree. Compare to the branch the prior handoff approved against. Force-pushes, branch renames, or operator-initiated switches all invalidate.
4. **Upstream contract** — diff `sprint-contract.md` against the version the prior handoff bound. New tasks added, existing tasks modified, or scope-changing edits all invalidate.

Time alone is not invalidating, but per Law 1 any approval older than 24 hours is automatically re-checked regardless of whether the operator believes nothing has changed.

### Step 3 — If any dimension shows drift, surface the gate

If Step 2 found a dimension that no longer matches the prior approval, STOP. Use `AskUserQuestion`:

> "Handoff approval is stale on the [route | provider | branch | contract] dimension: [specifics]. Should I (a) re-validate the route with the new state, (b) route back to `/plan` to refresh the upstream contract, or (c) abort and reframe?"

The operator picks; record the choice in `governed-handoff.md`. Per Law 1, never silently treat a stale approval as fresh.

If all four dimensions pass (or this is a first-entry run with no prior approval), proceed to Step 4.

### Step 4 — Request route approval

The operator names the route explicitly: which provider lane, which stage pack, which topology. Capture the rationale per Law 3 #1 — not just "use Codex" but "use Codex because the work touches `lib/nexus/adapters/*` and Codex carries the type-system context this run."

If the operator has not stated a route, prompt for one. `/handoff` does not pick the route; it records and validates the operator's choice.

### Step 5 — Write `governed-handoff.md` with cross-operator context dump

Per Law 3, the handoff artifact MUST carry enough context for a different operator (different person, different session, different agent provider) to begin without spelunking. At minimum include:

1. **What was decided** — the route and the rationale (per Step 4).
2. **What the receiving operator needs to read first** — explicit pointers to `prd.md`, `sprint-contract.md`, the design contract if present, prior advisor records relevant to the run.
3. **What's already verified vs what hasn't** — prior `/build` runs, partial verification, or fresh-run state. Prevents both re-verifying things already verified AND trusting things that weren't actually verified.
4. **Open decisions deferred to the receiving operator** — choices the approving operator did not make, with the option set visible.

A handoff between the same operator across sessions counts as cross-operator. Memory across sleep cycles is not reliable; the artifact is.

### Step 6 — Write `governed-execution-routing.md`

The routing artifact captures the dispatch target named in the route. This is what `/build` reads when it begins. Per Law 1 #1, the route names the specific stage pack, provider lane, and topology — the granularity at which a future freshness check can detect drift.

### Step 7 — Write status

Run the canonical command (in the Routing section below). It writes `status.json` and any continuation artifacts. The advisor record carries the verdict; `/build` reads it.

If governed execution cannot begin (transport error, provider went offline between approval and dispatch, route revoked), do NOT silently retry. Per Law 2, write `status: handoff-blocked` to `status.json` with the named failing leg (`route_unavailable` | `provider_offline` | `branch_drift` | `contract_mismatch` | `transport_error` | `approval_revoked`) and surface to the operator via `AskUserQuestion`.
```

This Action lifts D2 Workflow Clarity from 5/10 to ~8.5/10 (+3.5 raw, ×15 = +5.25 weighted) and D8 Real-World Performance from 7/10 to ~9/10 (+2 raw, ×25 = +5 weighted). Combined: ~+10 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus governed handoff command. Records explicit routing, provider intent,
  and handoff state before governed execution begins. Use after `/plan` declares the
  work execution-ready. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus governed handoff command. Records explicit routing, provider intent,
  and handoff state before governed execution begins. Use when the user says
  "approve the route", "hand off to /build", "lock in the routing", or after `/plan`
  declares the work execution-ready and a provider lane needs to be named before
  governed execution starts. (nexus)
```

Lifts D1 Frontmatter from 6/10 to ~8/10 (+2 raw, ×8 = +1.6 weighted).

Frontmatter character count: still well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the Run block, before the Completion Advisor footer.

**Voice:**

```markdown
## Typical prompts

These are example user requests `/handoff` handles well. The skill is invoked and produces a `governed-handoff.md`, `governed-execution-routing.md`, and a `status.json` with route approval recorded.

### Prompt 1 — Standard governed handoff to a provider lane

> "Hand off the planned work to Codex."

`/handoff` walks Step 1–7. Step 1 verifies `prd.md` and `sprint-contract.md` are ready. Step 2 captures branch/route/provider/contract baseline. Step 4 records "route: Codex CLI, rationale: work touches `lib/nexus/adapters/*` and Codex carries type-system context." Step 5 writes the cross-operator context dump per Law 3. Step 6 writes the routing artifact. Step 7 writes status; advisor surfaces canonical continuation into `/build`.

### Prompt 2 — Re-entering after a paused run

> "I approved this handoff yesterday but the branch was force-pushed — still valid?"

Step 2's freshness check finds branch drift. Step 3 fires the `AskUserQuestion` gate: "Handoff approval is stale on the branch dimension: prior approval was against `track-f-phase-4-handoff` at sha X, current head is sha Y (force-pushed). Re-validate / route back to `/plan` / abort?" The operator picks; the choice is recorded in `governed-handoff.md`. Per Law 1, the prior approval is invalidated until the operator re-approves explicitly or refreshes the upstream contract.

### Prompt 3 — Recovery from a blocked /build

> "/build failed transport — auto-retry the handoff?"

Per Law 2, NEVER silent retry. Step 7's failure path writes `status: handoff-blocked` with leg `transport_error` to `status.json` and surfaces via `AskUserQuestion`: "Build transport failed mid-dispatch. Re-approve with current state / route back to provider mount step / abort?" The operator decides; the recovery decision is recorded in `governed-handoff.md`. The skill does not pick the recovery path; it records what the operator chose.

These prompts test that `/handoff` re-validates freshness, surfaces blocks rather than silently retrying, and produces an artifact a different operator could resume from.
```

Lifts D8 from ~9/10 toward 9.5/10 (already partially captured by Action 1; cumulative gain ≈ +0.5 raw, ×25 = +1.25 weighted).

---

## Action 4 — AskUserQuestion gate at Law 1 freshness check

Track E already gave Law 2 (recovery from blocked) an `AskUserQuestion` — that's the strongest single D4 signal in the canonical Track E set. F.4 does NOT duplicate that.

INSTEAD, F.4 adds a *new* gate at Law 1 (freshness check) — when any of the 4 dimensions show drift, the operator is surfaced an explicit choice rather than the skill silently treating drift as approval-still-valid.

The gate is bundled into Action 1's Step 3 (no separate placement needed). The text:

> "Handoff approval is stale on the [route | provider | branch | contract] dimension: [specifics]. Should I (a) re-validate the route with the new state, (b) route back to `/plan` to refresh the upstream contract, or (c) abort and reframe?"

This is a *different* checkpoint than Law 2's blocked-recovery gate:
- Law 2 fires when `/build` cannot begin — execution-time block.
- Law 1 (new F.4 gate) fires when the operator re-enters `/handoff` and prior approval no longer matches reality — approval-time check.

Both checkpoints are now operator-visible, not implicit.

Lifts D4 Checkpoint Design from 7/10 to ~8.5/10 (+1.5 raw, ×7 = +1.05 weighted).

---

## Projected Darwin score after F.4

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6 | 8 | +2 | +1.6 |
| D2 Workflow Clarity | 5 | 8.5 | +3.5 | +5.25 |
| D3 Edge Cases | 9 | 9 | 0 | 0 |
| D4 Checkpoint Design | 7 | 8.5 | +1.5 | +1.05 |
| D5 Instruction Specificity | 9 | 9 | 0 | 0 |
| D6 Resource Integration | 7 | 7.5 | +0.5 | +0.25 (small lift from Step 1 reading prd.md / sprint-contract.md explicitly) |
| D7 Overall Architecture | 8 | 8 | 0 | 0 |
| D8 Real-World Performance | 7 | 9 | +2 | +5 |

**Projected total:** 72.7 + 13.2 ≈ **85.9/100** (target was 83–85; F.4 likely meets upper end of target).

---

## Implementation procedure

### Step 1: Edit `skills/canonical/handoff/SKILL.md.tmpl`

Apply 3 changes (Action 4 is bundled into Action 1):

1. Frontmatter description (Action 2)
2. Insert `## How to run /handoff` between Iron Laws and Operator Checklist (Action 1, with Action 4's gate at Step 3)
3. Insert `## Typical prompts` after Run block, before `{{NEXUS_COMPLETION_ADVISOR:handoff}}` footer (Action 3)

### Step 2: Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 3: Verify

```bash
bun run skill:check                                                                                   # clean across all 4 hosts
git grep -n "How to run /handoff" skills/canonical/handoff/SKILL.md                                   # 1 hit
git grep -n "Typical prompts" skills/canonical/handoff/SKILL.md                                       # 1 hit
git grep -n "approve the route" skills/canonical/handoff/SKILL.md                                     # 1 hit (frontmatter trigger)
git grep -n "Iron Laws (mandatory; non-negotiable)" skills/canonical/handoff/SKILL.md                 # 1 hit (preserved verbatim)
```

### Step 4: Commit + push to branch

```bash
git add skills/canonical/handoff/SKILL.md.tmpl skills/canonical/handoff/SKILL.md \
        skills/codex/handoff/SKILL.md skills/factory/handoff/SKILL.md \
        skills/gemini-cli/handoff/SKILL.md \
        docs/architecture/track-f-phase-4-handoff-brief.md
git commit -m "skills/canonical/handoff: Track F.4 Darwin upgrade"
git push -u origin track-f-phase-4-handoff
```

DO NOT close issue #116. DO NOT merge to main.

---

## Acceptance criteria

This phase is complete when:

1. Frontmatter description carries trigger phrases ("approve the route" / "hand off to /build" / "lock in the routing")
2. `## How to run /handoff` section inserted between Iron Laws and Operator Checklist with 7 numbered steps
3. Step 3 includes explicit `AskUserQuestion` text for Law 1 freshness drift (NEW gate, distinct from Law 2's blocked-recovery gate)
4. `## Typical prompts` section with 3 examples added
5. `{{NEXUS_COMPLETION_ADVISOR:handoff}}` footer preserved (already present from Track E)
6. Iron Laws preserved verbatim (no changes to Lines 21–62 of the .tmpl)
7. `bun run skill:check` clean across all 4 hosts
8. Branch `track-f-phase-4-handoff` pushed to origin; issue #116 NOT closed
9. Commit message references Track F.4

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First do X, then do Y" | "Step 1 — Validate framing + plan completeness. Before opening any routing question..." |
| AskUserQuestion framing | Vague "ask user" | Explicit options: "Re-validate the route / route back to `/plan` / abort and reframe?" |
| Cross-skill reference | "Then build it" | "Per `/plan` Law 3 declared this run handoff-ready" |
| Workflow vs Iron Law | Either or both | Both, with explicit relationship: "Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply." |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "approve the route", "hand off to /build", "lock in the routing" |
| Law 1 vs Law 2 gate | Conflate them | Law 1 gate: approval-time freshness drift. Law 2 gate: execution-time blocked recovery. Different checkpoints; both surfaced via `AskUserQuestion`. |

The numbered workflow is NOT an Iron Law — it's procedure. Don't add "MUST" or "non-negotiable" framing to the steps; the steps are guidance, the Iron Laws are constraints.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Numbered workflow ends up duplicating Iron Law content | Workflow steps reference Iron Laws by name (e.g., "per Law 1", "per Law 2", "per Law 3 #1") rather than restating them. The two surfaces stay distinct. |
| 7 steps feel verbose for one-shot handoffs | Steps 2–3 are explicitly conditional (re-entry vs first entry). A first-entry handoff with no drift uses ~5 steps; a stale re-entry uses all 7. |
| Law 1's new AskUserQuestion gate could be triggered too aggressively | Gate fires only when at least one of four named dimensions shows drift. The dimensions are mechanical (route name, provider mount state, branch sha, contract diff) — not subjective. |
| Track E already added Law 2 AskUserQuestion; adding another could feel redundant | The two gates fire at different times (approval-time vs execution-time) and address different failure modes (stale approval vs blocked execution). Both are operator-visible, both have explicit option sets. |
| Total SKILL.md length grows from ~574 to ~720 lines | Within design budget (canonical Track F .1 skill ended at comparable length post-upgrade). |
| `scripts/resolvers/completion-advisor.ts` change required | NOT required for F.4 — handoff entries already exist in STAGE_FIELDS (line 40) and STAGE_NOTES (line 132) from Track E. The footer already renders. |

---

## Effort

- Brief: ~25 min (this document)
- Implementation: ~25–35 min (3 edits to one .tmpl file, regenerate, verify, commit, push)
- **Total: ~1h** (matches RFC's M effort estimate, identical to F.1)

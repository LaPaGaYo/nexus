# Track E Phase 9 Brief: strengthen `/learn` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup** (see RFC § "Track E is its own track").

**Audit rank:** 9. **Current discipline score:** 2.8/5 (Mandatory 3, Verification 3, Failure 2, Acceptance 3, Transferability 3).

**Note:** `/learn` is a **support skill** (procedural multi-mode wrapper for show/search/prune/export/stats/add), not a canonical lifecycle stage. Iron Laws here are about contributor-facing capture discipline rather than gate verdicts.

**Parent RFC:** `docs/architecture/track-e-skill-strengthening-rfc.md` § Phase E.9.
**Anchor data:** `docs/architecture/skill-strength-audit.md` § "learn (support)".
**Voice references:** E.1–E.8 (commits `4209cc4`, `10974ed`, `7fbcb7d`, `52c02fa`, `16fccc8`, `fcbcbbe`, `c5bacef`, `0a90629`).
**Internal references:** Cross-skill capture hooks live in `/closeout` (already produces `LEARNINGS.md`), `/review`, `/build`, `/qa`, `/investigate`, `/simplify`, `/retro`, `/cso`.
**Issue:** #111.

---

## Goal

Raise `/learn` SKILL.md prose discipline from 2.8/5 → 4.0+/5 by adding 3 Iron Laws that codify (a) the confidence rubric so contributors calibrate the same way, (b) which skills MUST log a learning on non-obvious findings (cross-skill capture discipline), (c) the maintenance cadence that prevents the learnings store from rotting into noise.

After this lands:
- Every learning entry carries a confidence value computed against a published rubric, not against contributor intuition.
- The skills that produce learnings (`/investigate`, `/simplify`, `/cso`, `/retro`, `/closeout`, plus situational triggers in `/review`/`/build`/`/qa`) have a clear "you must log this" trigger written into their workflow.
- The `/learn prune` cadence is documented (quarterly + on-demand), not implicit.

---

## Why this is rank 9 ROI

`/learn` is the team-memory layer. Its discipline only matters when other skills consistently capture insights — but the prose for those other skills doesn't always include a "log a learning" hook today. Per audit:

> "The learnings system is most useful when other skills consistently capture insights, but the prose for those other skills doesn't always include a 'log a learning' hook."

E.9 is not about strengthening `/learn`'s mechanics (already well-specified per audit: HARD GATE good, stale-detection good, conflict-detection good). It's about making the calibration rubric explicit (so confidence values mean the same thing across contributors) and naming the upstream skills that must hook into `/learn`.

This is the second-to-last phase. After E.9 + E.10, Track E is complete and the skill-strength audit can be re-run to verify aggregate ≥4.0/5.

---

## Current state of `/learn`

Read `skills/support/learn/SKILL.md.tmpl` (226 lines — substantially longer than the canonical 9 since it's a procedural multi-mode wrapper).

Per audit:
- **Mandatory 3/5** — HARD GATE ("do NOT implement code changes") is enforced
- **Verification 3/5** — Verification Evidence section names per-mode checks
- **Failure 2/5** — stale/conflict detection logic exists but the rubric for "what's a low-confidence learning vs a high-confidence one" is implicit
- **Acceptance 3/5** — Output Contract enumerates per-mode outputs
- **Transferability 3/5** — confidence-rubric pattern generalizes; capture cadence pattern generalizes

**Existing strengths:**
- HARD GATE is already in place
- Multi-mode operational structure (show / search / prune / export / stats / add)
- JSONL format with `key`, `type`, `insight`, `confidence`, `source`, `files`, `ts` fields
- Stale-detection (deleted file references) + conflict-detection (same-key contradictions)
- Append-only with "latest entry wins" semantics

**Gaps Iron Laws will close:**
- No published confidence rubric — what does "7/10" actually mean?
- No cross-skill capture discipline — which skills MUST log when?
- No maintenance cadence — when does prune run?

---

## Surface map

### File modified

```
skills/support/learn/SKILL.md.tmpl             ← edit (insert Iron Laws section)
skills/support/learn/SKILL.md                  ← regenerated by gen:skill-docs
```

### File NOT modified

- `~/.claude/skills/nexus/bin/nexus-learnings-*` scripts — runtime; not skill prose
- `~/.nexus/projects/<slug>/learnings.jsonl` — data store; not affected by prose
- `.planning/archive/runs/*/closeout/learnings.json` — produced by `/closeout`; not modified here
- Other skills that should hook into `/learn` (`/investigate`, `/simplify`, etc.) — Law 2 names them; modifying their prose to add capture hooks is **out of scope for E.9** and lives in their respective phases or follow-ups.

### Frontmatter note

`/learn` already has a rich `allowed-tools` list (`Bash`, `Read`, `Write`, `Edit`, `AskUserQuestion`, `Glob`, `Grep`). No frontmatter changes needed.

---

## Iron Law design

### Law 1 — Confidence Rubric (Calibrated Across Contributors)

**Discipline absorbed from:** Existing `/learn` JSONL schema (confidence 1-10) + audit recommendation #1.

**Voice (Nexus-native):**

```markdown
### Law 1 — Confidence Rubric (Calibrated Across Contributors)

Every learning entry MUST carry a confidence value calibrated against this published rubric:

- **9-10 — Verified universal**: confirmed by ≥2 independent runs, or backed by code/test that enforces the pattern. Safe to act on without re-checking. Example: "Bun test exits 0 even with failing tests on macOS — verified across runs #41, #67, #99."
- **7-8 — Verified specific**: confirmed in one run with explicit evidence (test output, error trace, profile data, etc.) attached. Likely to repeat for similar work; re-check before relying on it for materially different work.
- **5-6 — Pattern hint**: observed once with reasoning that suggests it generalizes, but not yet re-verified. Worth checking before acting.
- **3-4 — Working assumption**: the team currently believes this but has not gathered explicit evidence. Treat as hypothesis, not fact.
- **1-2 — Speculation**: noted because it might matter, but no confirming signal yet. Pruning candidate if not promoted within 3 months.

**Evidence type** field accompanies confidence: `test-output` | `code-pattern` | `profile-data` | `multi-run-observation` | `single-run-observation` | `team-consensus` | `external-reference` | `speculation`.

A learning logged without an explicit confidence + evidence-type pair is incomplete. Future contributors cannot calibrate against an incomplete rubric; one contributor's "I'm pretty confident" is another's "we're guessing."

The rubric is the contract between contributors. When in doubt, log lower; promotion is cheap, retraction is costly.
```

This Law makes "confidence" a shared vocabulary, not an individual feeling.

### Law 2 — When To Log A Learning (Cross-Skill Capture Discipline)

**Discipline absorbed from:** Audit recommendation #3 (named cross-skill rule) + cross-Iron-Law coherence (E.1–E.8 produce learnable signals).

**Voice (Nexus-native):**

```markdown
### Law 2 — When To Log A Learning (Cross-Skill Capture Discipline)

These skills MUST log a learning when their workflow produces a non-obvious finding:

1. **`/investigate`** — every confirmed root cause is a learning entry (`type: pattern` or `type: pitfall`, `evidence: test-output` typical). Investigation without a learning record loses the insight to memory decay.
2. **`/simplify`** — every removed duplication or simplification reveals a pattern about the codebase. Log the pattern (`type: pattern`, `evidence: code-pattern`).
3. **`/cso`** — every security finding (low or high severity) is a learning. The threat model is updatable; learnings are how it gets updated.
4. **`/retro`** — retrospectives produce process learnings (`type: pattern` or `type: preference`, `evidence: team-consensus` typical).
5. **`/closeout`** — already produces `LEARNINGS.md` per artifact contract. The 3-strike triggers from `/build` Law 2 and `/qa` Law 3 (`route to /investigate`) feed candidate learnings here.

These skills SHOULD log a learning when surprised:

6. **`/review`** — when an audit slot finds something the synthesis didn't expect. Surprises are the highest-information events; not logging them wastes the surprise.
7. **`/build`** — when a 3-strike stop reveals a substrate issue (per `/build` Law 2). The substrate finding is a candidate learning, not just a routing event.
8. **`/qa`** — when a 3-strike same-root-cause cluster reveals a pattern (per `/qa` Law 3). The cluster is the learning, even if the individual findings were P3 cosmetic.

The MUST list is mandatory because those skills generate insights that are otherwise lost. The SHOULD list is situational because not every routing event produces a generalizable learning — the operator judges.

A learning that is not logged within 24h of the producing event has a high probability of being lost entirely. The "I'll capture later" path is the most reliable way to lose insights.
```

This Law gives upstream skills a concrete trigger for "capture a learning now."

### Law 3 — Learning Hygiene (Quarterly Prune + Staleness Discipline)

**Discipline absorbed from:** Audit recommendation #2 + existing `/learn prune` mechanics.

**Voice (Nexus-native):**

```markdown
### Law 3 — Learning Hygiene (Quarterly Prune + Staleness Discipline)

The learnings store rots into noise without explicit maintenance. Two cadences:

1. **Quarterly prune** — `/learn prune` SHOULD run at the start of each quarter (or whenever 3 months have passed since the last prune). The operator reviews flagged entries (deleted-file references, contradictions, low-confidence entries older than 3 months without promotion) and accepts/rejects each via `AskUserQuestion`. Quarterly prune is the maintenance contract; without it, the store accumulates dead references and contradictions that drown the signal.

2. **On-demand prune** — `/learn prune` MUST run before `/learn export` to CLAUDE.md or any documentation surface. Exporting stale or contradicting learnings into the team's working memory propagates the noise. Pre-export prune is non-negotiable.

**Staleness signals** (Iron Law 3-relevant, mirrors existing prune logic):

- **Dead-file reference**: learning's `files` field points to deleted paths. Either remove the learning or update it to reference the current location.
- **Same-key contradiction**: two entries with the same `key` and `type` carry contradicting `insight` values. The latest entry wins by append-only convention, but explicit prune resolves the contradiction in the operator's favor (delete the older one or promote the newer with a "supersedes #N" note).
- **Low-confidence aging**: entries at confidence 1-2 (Speculation tier per Law 1) that haven't been promoted to ≥3 within 3 months are pruning candidates. They were noted; if they didn't matter enough to verify, they don't earn permanent storage.

A learnings store with quarterly hygiene scales with the project. A store without it accumulates contradictions until contributors stop trusting it — at which point the team has a wiki nobody reads.
```

This Law turns prune from "available command" into "scheduled discipline."

---

## Implementation procedure

### Step 1: Read existing `/learn` SKILL.md.tmpl

Note: `/learn`'s structure differs from the canonical 9. The HARD GATE is at the top of the prose (line 32), followed by a `---` separator and the per-mode sections (Detect command, Show current, Search, Prune, Export, Stats, Manual add). Iron Laws section goes between the HARD GATE block and the per-mode content.

### Step 2: Edit `skills/support/learn/SKILL.md.tmpl`

Insert the 3 Iron Laws section after the existing HARD GATE block (around line 33), before the `---` separator that precedes `## Detect command` (around line 34).

Voice: use `/learn`-native concepts (confidence rubric tiers, evidence-type field, cross-skill capture triggers, prune cadence) — NOT borrowed framing from elsewhere.

### Step 3: Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 4: Verify

```bash
bun run skill:check                                                                                          # clean across all 4 hosts
git grep -n "Iron Law" skills/support/learn/SKILL.md                                                         # 1 hit (heading)
git grep -n "Confidence Rubric\|When To Log A Learning\|Learning Hygiene" skills/support/learn/SKILL.md      # 3 hits
```

### Step 5: Commit

```bash
git add skills/support/learn/SKILL.md.tmpl skills/support/learn/SKILL.md
git commit -m "skills/support/learn: add Iron Laws section (Track E.9)"
```

Per skill-authoring rules, .tmpl + regenerated canonical .md commit together. Sidecars (.agents/, .factory/, .gemini/) are gitignored.

---

## Acceptance criteria

This phase is complete when:

1. ✅ `skills/support/learn/SKILL.md.tmpl` has 3 Iron Laws inserted between the HARD GATE block and the `## Detect command` section
2. ✅ `skills/support/learn/SKILL.md` is regenerated and contains the same 3 Iron Laws
3. ✅ Iron Law text uses `/learn`-native concepts (confidence rubric tiers 1-10, `evidence type` field, cross-skill triggers naming `/investigate`/`/simplify`/`/cso`/`/retro`/`/closeout`/`/review`/`/build`/`/qa`, quarterly cadence, dead-file/contradiction/low-confidence aging signals)
4. ✅ Each Law fits the pattern: (situation/scope) + (≥3 mandatory rules or ≥3 enumerated items) + (consequence of violation)
5. ✅ Law 2's MUST/SHOULD distinction preserved — MUST = always log, SHOULD = log when surprised
6. ✅ `bun run skill:check` clean across all 4 hosts
7. ✅ Issue #111 closed with completion comment
8. ✅ Commit message references Track E.9

---

## Voice constraints (CRITICAL)

| Pattern | Don't | Do |
|---|---|---|
| Confidence framing | "Use your judgment" | "One contributor's 'I'm pretty confident' is another's 'we're guessing.' The rubric is the contract between contributors." |
| Capture-cadence framing | "Log when useful" | "A learning that is not logged within 24h of the producing event has a high probability of being lost entirely. The 'I'll capture later' path is the most reliable way to lose insights." |
| Hygiene framing | "Clean up regularly" | "A learnings store with quarterly hygiene scales with the project. A store without it accumulates contradictions until contributors stop trusting it — at which point the team has a wiki nobody reads." |
| Cross-skill flow | "Log when relevant" | "These skills MUST log a learning when their workflow produces a non-obvious finding: `/investigate`, `/simplify`, `/cso`, `/retro`, `/closeout`." |
| Surprise framing | "Note interesting things" | "Surprises are the highest-information events; not logging them wastes the surprise." |
| Mandatory rule prefix | "Iron Law:" alone | "These three rules apply to every `/learn` invocation regardless of provider, topology, or run mode. They are short and absolute on purpose — discipline that lives in qualifiers does not survive contact with the LLM at decision time." |

Reuse the meta-justification ("short and absolute on purpose") from E.1–E.8.

---

## Cross-Iron-Law coherence

After E.9, the Track E chain extends to the team-memory layer:

- `/learn` Law 2 names upstream skills with capture obligations:
  - **`/investigate`** — root cause findings → learnings
  - **`/build`** Law 2 (3-strike → `/investigate`) → produces candidate learning
  - **`/qa`** Law 3 (3-strike → `/investigate`) → produces candidate learning
  - **`/closeout`** → already writes `LEARNINGS.md`; Law 1 confidence rubric calibrates those entries
  - **`/review`** when surprised, **`/cso`**, **`/retro`**, **`/simplify`** — generate cross-cutting insights

- `/learn` Law 1's confidence rubric is consumed by every upstream skill that logs (vocabulary unification across the lifecycle).

- `/learn` Law 3's prune-before-export cadence ensures CLAUDE.md / docs reflect curated knowledge, not raw accumulation.

E.9 is the first Track E phase that depends on cross-skill behavior change rather than per-skill prose. The MUST/SHOULD distinction in Law 2 acknowledges this — Law 2 declares the contract; the upstream skills' adoption of the contract is gradual.

---

## Out of scope

- **Adding capture hooks to upstream skills' prose** — Law 2 names which skills MUST log; modifying their SKILL.md.tmpl to include explicit "log a learning here" instructions is **out of scope for E.9**. This can land as a follow-up phase or per-skill (e.g., `/investigate` SKILL.md.tmpl could add a "log root cause as learning" step in its Phase 4).
- **Schema enforcement of confidence + evidence-type fields** — runtime concern; the rubric in Law 1 is contributor-facing, not validator-facing.
- **Auto-prune scheduling** — Law 3 says "SHOULD run quarterly"; making this a cron job or hook is operational, not skill-prose.
- **Confidence calibration tooling** — no separate calibration validator; the rubric IS the calibration tool.
- **Cross-project learnings sharing** — `/learn` is project-scoped today (`~/.nexus/projects/<slug>/`); cross-project sharing is a feature, not a Track E concern.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Law 1's 5-tier rubric (9-10 / 7-8 / 5-6 / 3-4 / 1-2) is finer-grained than current "1-10" usage | Acceptable. The rubric maps the existing 1-10 scale into discrete tiers with prose definitions; no schema change. Existing entries can stay at their current numbers; future entries calibrate against the rubric. |
| Law 2's MUST list creates obligations for skills that don't currently auto-log | The MUST list is forward-looking; current skills aren't violating until they're updated. Track E phases or follow-ups can add hooks; the Law gives target state. |
| Law 3's "quarterly" cadence may not match team rhythms | "Quarterly" is suggested; the rule is "SHOULD run regularly + MUST run before export." Operators adjust frequency; the discipline is "schedule it explicitly" not "do it on this exact date." |
| 8-skill cross-reference list in Law 2 is the densest in Track E | Acceptable. `/learn` IS the cross-cutting skill; density of cross-references is appropriate. Each skill name appears once with a one-clause rationale. |
| `/learn` SKILL.md becomes ~810+ lines (already 732 lines pre-strengthening due to multi-mode structure) | Within budget. The Iron Laws section adds ~85 lines; total is comparable to qa-only's 944 lines. |

---

## Decision points (resolved)

1. **Law count** — 3 (matches E.1–E.8 cadence)
2. **Confidence rubric tiers** — 5 (9-10 / 7-8 / 5-6 / 3-4 / 1-2); maps cleanly into existing 1-10 scale without schema change
3. **MUST vs SHOULD distinction in Law 2** — MUST = the skill always produces logable insight; SHOULD = the skill sometimes does. Mirrors RFC 2119 conventions for prose contracts.
4. **Cross-skill list in Law 2** — 5 MUST + 3 SHOULD = 8 skills total. RFC sketch named 4 (investigate, simplify, cso, retro); audit added closeout implicitly. SHOULD list adds review/build/qa as situational triggers tied to existing Iron Laws (review L3, build L2, qa L3).
5. **Prune cadence** — quarterly + on-demand pre-export. Quarterly is suggested; "schedule it explicitly" is the rule.
6. **Placement in SKILL.md.tmpl** — after HARD GATE block, before per-mode sections. Iron Laws are about contributor discipline, not per-mode mechanics.
7. **Schema vs prose** — prose only. `evidence type` field formalization is a follow-up.

---

## Effort

- Brief: ~30 min (this document)
- Implementation: ~30-35 min (edit .tmpl with 3 Iron Laws ~85 lines + correct insertion point relative to existing HARD GATE structure, regenerate, verify, commit)
- **Total: ~1h** (S effort per RFC)

---

## Implementation kickoff

After this brief commits and the user approves, proceed directly to:

1. Edit `skills/support/learn/SKILL.md.tmpl` with the 3 Iron Laws (verbatim from § "Iron Law design") — inserted after the HARD GATE block, before the per-mode sections
2. `bun run gen:skill-docs` (regenerates canonical Claude + Codex sidecar)
3. `bun run gen:skill-docs --host factory && bun run gen:skill-docs --host gemini-cli`
4. `bun run skill:check` — confirm clean across all 4 hosts
5. Verify Iron Laws appear at expected position in `skills/support/learn/SKILL.md`
6. Commit canonical .tmpl + .md (sidecars are gitignored)
7. Push to main
8. Close #111 with completion comment

# Track E Phase 8 Brief: strengthen `/closeout` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup** (see RFC § "Track E is its own track").

**Audit rank:** 8. **Current discipline score:** 2.2/5 (Mandatory 2, Verification 3, Failure 2, Acceptance 3, Transferability 1).

**Parent RFC:** `docs/architecture/track-e-skill-strengthening-rfc.md` § Phase E.8.
**Anchor data:** `docs/architecture/skill-strength-audit.md` § "closeout (canonical)".
**Voice references:**
- `skills/canonical/build/SKILL.md.tmpl` (commit `4209cc4`) — Phase E.1 PoC.
- `skills/canonical/plan/SKILL.md.tmpl` (commit `10974ed`) — Phase E.2.
- `skills/canonical/review/SKILL.md.tmpl` (commit `7fbcb7d`) — Phase E.3.
- `skills/canonical/qa/SKILL.md.tmpl` (commit `52c02fa`) — Phase E.4.
- `skills/canonical/frame/SKILL.md.tmpl` (commit `16fccc8`) — Phase E.5.
- `skills/canonical/discover/SKILL.md.tmpl` (commit `fcbcbbe`) — Phase E.6.
- `skills/canonical/ship/SKILL.md.tmpl` (commit `c5bacef`) — Phase E.7.
**External reference patterns:** GSD `complete-milestone` + `audit-milestone`.
**Issue:** #110.

---

## Goal

Raise `/closeout` SKILL.md prose discipline from 2.2/5 → 4.0+/5 by adding 3 Iron Laws that codify (a) the explicit integrity checklist for what closeout must verify, (b) the stale-evidence rejection rule that prevents archiving stale audits, (c) the read-only invariant that protects lifecycle authority from accidental closeout writes.

After this lands:
- A `/closeout` invocation cannot conclude with verdict `archived` while review/QA/ship artifacts are missing or success criteria are unaddressed.
- Stale audits (older than the latest `/build`, or carrying a mismatched run_id) are rejected — closeout requires fresh evidence, not historical evidence.
- `/closeout` cannot edit `.planning/current/*/status.json`; if state is wrong, the operator routes back to the owning skill.

---

## Why this is rank 8 ROI

`/closeout` closes the lifecycle loop. After E.7, every input → output stage carries Iron Laws (`/discover` → `/ship`). E.8 closes the loop by adding Iron Laws to the verification + archive step.

Per audit:

> "Closeout is well-defined as an archive verification step (good artifact contract), but the prose doesn't describe what makes a closeout 'high quality.' There's no explicit list of things to verify ('did the audit set actually exercise the success criteria from `/frame`?'), no failure taxonomy ('what does a stale audit look like?'), and no link between closeout and learnings/retros beyond the advisor."

E.8 is small (S effort) but high-leverage because closeout is the place where ambiguity from earlier stages becomes permanent. A loose closeout means archived runs that don't actually verify what they claim to verify; future `/discover` runs that consume that archive (via `next-run-bootstrap.json`) propagate the looseness.

---

## Current state of `/closeout`

Read `skills/canonical/closeout/SKILL.md.tmpl` (39 lines) and `skills/canonical/closeout/SKILL.md` (auto-generated, ~538 lines).

Per audit:
- **Mandatory 2/5** — checklist exists ("verify audit completeness, verify archive state, verify legal transition history") but rules are descriptive not enforced
- **Verification 3/5** — runtime advisor checks artifact presence; prose doesn't enumerate WHAT to verify
- **Failure 2/5** — no taxonomy of "what does a bad closeout look like?" (stale audit, missing artifact, success criteria unaddressed)
- **Acceptance 3/5** — `CLOSEOUT-RECORD.md` is named in artifact contract but not specified
- **Transferability 1/5** — entirely Nexus-specific governance vocabulary

**Current Operator Checklist (from `lib/nexus/stage-content/closeout/checklist.md`)**:
1. verify audit completeness
2. verify archive state
3. verify legal transition history

That's it. Three bullets, each abstract.

---

## Surface map

### File modified

```
skills/canonical/closeout/SKILL.md.tmpl        ← edit (insert Iron Laws section)
skills/canonical/closeout/SKILL.md             ← regenerated by gen:skill-docs
```

### File NOT modified

- `lib/nexus/stage-content/closeout/*` — runtime content; not skill prose
- `.planning/current/closeout/*` — artifacts; not affected by prose
- `.planning/current/*/status.json` (any stage) — Law 3 explicitly forbids closeout from modifying these
- GSD external skills — referenced for voice, not modified

### Frontmatter note

`/closeout` already has `Bash`, `Read`, `AskUserQuestion` in `allowed-tools`. Sufficient. No frontmatter changes needed.

---

## Iron Law design

### Law 1 — Closeout Integrity Checklist (Five Mandatory Verifications)

**Discipline absorbed from:** GSD `complete-milestone` + `audit-milestone` (intent-vs-delivered audit) + audit recommendation #1.

**Voice (Nexus-native):**

```markdown
### Law 1 — Closeout Integrity Checklist (Five Mandatory Verifications)

`/closeout` cannot mark `CLOSEOUT-RECORD.md` `archived` without ALL of these visible in the advisor record at decision time:

1. **`/review` artifacts present** — `.planning/audits/current/*` contains the dual audit set, `.planning/current/review/status.json` records `pass_round` ≥ 1 with verdict `pass` or `pass_with_advisories`. Missing review artifacts ≡ closeout incomplete.
2. **`/qa` artifacts present when `/qa` was run** — `.planning/current/qa/qa-report.md` and `.planning/current/qa/status.json` exist with verdict `ready` or `ready_with_findings`. If `/qa` was not run for this run, that fact is recorded explicitly in `CLOSEOUT-RECORD.md` (not silently absent).
3. **`/ship` handoff complete** — `.planning/current/ship/release-gate-record.md` records the Law 3 outcome (merge / keep alive / discard / split per `/ship` Law 3). The outcome is explicit; "in-flight branch with no recorded decision" blocks closeout.
4. **Ledger consistent** — the artifact index in `CLOSEOUT-RECORD.md` matches files on disk. Every artifact referenced exists; every artifact on disk in the canonical paths is referenced. Drift between index and disk ≡ closeout incomplete.
5. **Success criteria from `/frame` actually addressed by the audit set** — each success criterion in `prd.md` (per `/frame` Law 1) is verified by at least one audit finding in `.planning/audits/current/*` or one QA finding in `qa-report.md`. Unaddressed criteria are listed in `CLOSEOUT-RECORD.md`'s "what we did NOT verify" section. Silent omission is rejected.

If any check fails, `/closeout` returns `not_archived` and surfaces which check failed. The operator routes to the owning skill (per Law 3) to address the gap.

This is the place where ambiguity from earlier stages becomes permanent. A loose closeout produces archives that don't actually verify what they claim — future `/discover` runs reading this archive propagate the looseness.
```

This Law makes the integrity contract explicit and ties success criteria back to `/frame` — closing the framing → verification loop.

### Law 2 — Stale Evidence Rejection

**Discipline absorbed from:** RFC sketch #1 + audit recommendation #2.

**Voice (Nexus-native):**

```markdown
### Law 2 — Stale Evidence Rejection

`/closeout` rejects audit and QA artifacts that are stale relative to the current state of the work:

1. **Stale relative to `/build` head** — if any audit in `.planning/audits/current/*` was generated against a commit older than the latest `/build` head SHA, that audit is stale. Closeout marks the run `stale_audit` and requires `/review` rerun against the current head.
2. **Stale relative to run_id** — every audit, QA report, and ship artifact carries a `run_id` field. Closeout verifies all artifacts in the run share the same `run_id`. Mismatched run_ids ≡ artifacts from different runs accidentally collated; closeout returns `not_archived`.
3. **Stale relative to clock** — audits older than 7 days are flagged for review even when run_id and head SHA align. Long-paused runs accumulate context drift; the world a 7-day-old audit described may not match the world the work shipped into.

A stale audit is not a small failure mode. The audit was generated against a tree the user never shipped. Archiving the audit as evidence of "what was verified" produces a record that lies about what was verified.

When `/closeout` flags `stale_audit`, the operator either reruns the relevant stage (`/review`, `/qa`) against the current state or explicitly accepts the staleness with a documented note in `CLOSEOUT-RECORD.md` ("audit is N days old; re-verify before re-shipping any related change"). Silent acceptance is forbidden.
```

This Law makes "what we verified vs what we shipped" auditable.

### Law 3 — Closeout Is Read-Only With Respect To Lifecycle Authority

**Discipline absorbed from:** audit recommendation #3 + Nexus governance principle (each stage owns its own state).

**Voice (Nexus-native):**

```markdown
### Law 3 — Closeout Is Read-Only With Respect To Lifecycle Authority

`/closeout` MUST NOT modify any of these from inside `/closeout`:

1. **`.planning/current/*/status.json`** for any owning stage (`/discover`, `/frame`, `/plan`, `/build`, `/review`, `/qa`, `/ship`). These are owned by their respective skills; closeout reads them, never writes them.
2. **Owning-stage artifacts** — `prd.md`, `idea-brief.md`, `sprint-contract.md`, `qa-report.md`, `release-gate-record.md`, etc. If the artifact is wrong, the owning skill fixes it; closeout does not patch around the wrongness.
3. **Stage transition history** — the ledger of stage outcomes. Closeout reads this to verify Law 1 + Law 2 checks; if the history shows a problem, closeout reports `not_archived` rather than rewriting the history to make the archive valid.

Closeout writes only to `.planning/current/closeout/*` — its own outputs (CLOSEOUT-RECORD, FOLLOW-ON-SUMMARY, NEXT-RUN, learnings, status). Everything else is read-only.

The reason: lifecycle authority is single-owner. If `/build` says the build is `complete` and the audit says otherwise, the conflict is real and the resolution belongs in `/build` or `/review`, not in `/closeout`. A closeout that "fixes" upstream state silently is not closing out the run; it is laundering the run.

When closeout finds inconsistent state, the answer is to surface it and route back. The operator decides which owning skill to re-enter; closeout records the routing decision in its own status, not by editing upstream artifacts.
```

This Law preserves the single-owner invariant for lifecycle state.

---

## Implementation procedure

### Step 1: Read existing `/closeout` SKILL.md.tmpl

Confirm placement: insert Iron Laws section after `{{NEXUS_STAGE_OVERVIEW}}` and before `## Operator Checklist`.

### Step 2: Edit `skills/canonical/closeout/SKILL.md.tmpl`

Insert the 3 Iron Laws section (~85-90 lines of prose). Voice: use Nexus-native concepts (`CLOSEOUT-RECORD.md`, `FOLLOW-ON-SUMMARY.md`, `run_id`, `stale_audit`, `archived`/`not_archived`, explicit cross-references to `/frame` Law 1, `/review` Law 2 `pass_round`, `/qa` Law 2, `/ship` Law 3) — NOT borrowed GSD framing wholesale.

### Step 3: Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 4: Verify

```bash
bun run skill:check                                                                                                    # clean across all 4 hosts
git grep -n "Iron Law" skills/canonical/closeout/SKILL.md                                                              # 1 hit (heading)
git grep -n "Closeout Integrity Checklist\|Stale Evidence Rejection\|Closeout Is Read-Only" skills/canonical/closeout/SKILL.md  # 3 hits
```

### Step 5: Commit

```bash
git add skills/canonical/closeout/SKILL.md.tmpl skills/canonical/closeout/SKILL.md
git commit -m "skills/canonical/closeout: add Iron Laws section (Track E.8)"
```

---

## Acceptance criteria

This phase is complete when:

1. ✅ `skills/canonical/closeout/SKILL.md.tmpl` has 3 Iron Laws inserted between `{{NEXUS_STAGE_OVERVIEW}}` and `## Operator Checklist`
2. ✅ `skills/canonical/closeout/SKILL.md` is regenerated and contains the same 3 Iron Laws
3. ✅ Iron Law text uses Nexus-native concepts (CLOSEOUT-RECORD.md, run_id, stale_audit, advisor record, archived/not_archived, explicit cross-references to upstream Iron Laws) — NOT direct GSD wording
4. ✅ Each Law fits the pattern: (situation/scope) + (≥3 mandatory rules or ≥3 enumerated items) + (consequence of violation)
5. ✅ Law 1 check 5 explicitly cites `/frame` Law 1 (closes the framing → verification loop)
6. ✅ `bun run skill:check` clean across all 4 hosts
7. ✅ Issue #110 closed with completion comment
8. ✅ Commit message references Track E.8

---

## Voice constraints (CRITICAL)

| Pattern | Don't | Do |
|---|---|---|
| Integrity framing | "Make sure everything is there" | "This is the place where ambiguity from earlier stages becomes permanent. A loose closeout produces archives that don't actually verify what they claim." |
| Stale evidence framing | "Re-run if old" | "A stale audit is not a small failure mode. The audit was generated against a tree the user never shipped. Archiving the audit as evidence of 'what was verified' produces a record that lies about what was verified." |
| Read-only framing | "Don't edit upstream" | "A closeout that 'fixes' upstream state silently is not closing out the run; it is laundering the run." |
| Cross-skill flow | "Check the review" | "`/review` artifacts present — `.planning/audits/current/*` contains the dual audit set, `.planning/current/review/status.json` records `pass_round` ≥ 1 with verdict `pass` or `pass_with_advisories`." |
| Mandatory rule prefix | "Iron Law:" alone | "These three rules apply to every `/closeout` invocation regardless of provider, topology, or run mode. They are short and absolute on purpose — discipline that lives in qualifiers does not survive contact with the LLM at decision time." |
| Owning-skill framing | "Talk to the right team" | "The conflict is real and the resolution belongs in `/build` or `/review`, not in `/closeout`." |

Reuse the meta-justification ("short and absolute on purpose") from E.1–E.7.

---

## Cross-Iron-Law coherence

After E.8, the Iron Law chain spans the full lifecycle including verification:

```
/discover (Iron Laws ✓) → /frame (✓) → /plan (✓) → /build (✓) → /review (✓) → /qa (✓) → /ship (✓) → /closeout (✓)
```

`/closeout` is the explicit consumer of every upstream Iron Law verdict. The chain reads:

- `/closeout` Law 1 check 1 reads `/review` Law 2's `pass_round`
- `/closeout` Law 1 check 2 reads `/qa` Law 2's verdict ladder
- `/closeout` Law 1 check 3 reads `/ship` Law 3's branch outcome
- `/closeout` Law 1 check 5 reads `/frame` Law 1's success criteria — closes the framing → verification loop
- `/closeout` Law 2 enforces freshness across all upstream artifacts (run_id alignment + head SHA freshness)
- `/closeout` Law 3 preserves the single-owner invariant for upstream state

E.8 is the closure of the input → output → verification spine. After E.8, only the support stages remain (E.9 `/learn`, E.10 `/handoff`).

---

## Out of scope

- **Schema enforcement at runtime** — this brief covers prose only. `run_id` field schema, `stale_audit` flag in advisor record — runtime concerns, can land as follow-up.
- **`audit-milestone` skill direct adoption** — we replicate the discipline pattern (intent-vs-delivered verification via Law 1 check 5), NOT the GSD skill wholesale. Nexus's 3-Law contract is intentionally tighter.
- **Retro / learnings extraction** — `/learn` and `/closeout`'s `LEARNINGS.md` artifact are referenced but learning quality discipline is scope of Phase E.9.
- **Long-paused-run threshold (7 days)** — Law 2 check 3 cites 7 days as the staleness threshold. This is heuristic; the rule is "long-paused runs accumulate context drift." Adjustable in a follow-up if 7 days proves wrong.
- **Detailed `next-run-bootstrap.json` schema** — already in artifact contract; not relevant to prose strengthening.
- **Documentation-sync follow-on integration** — `/document-release`'s `documentation-sync.md` is referenced in artifact contract but is its own support skill.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Law 1 check 5 ("success criteria addressed by audit set") requires audit set to map to `/frame` PRD criteria; runtime may not enforce this mapping today | Acceptable. The prose discipline ("listed in 'what we did NOT verify' section") works even without runtime enforcement; mapping rigor improves over time as PRDs adopt Law 1 from `/frame`. |
| Law 2's "older than 7 days" threshold is heuristic | Stated as heuristic in the Law itself ("flagged for review"); the run_id check (Law 2 #2) is the hard rule. Time-based check is a soft signal. |
| Law 3's "no edits to upstream status.json" may conflict with current closeout behavior | Audit recommendation #3 explicitly flags this as a needed invariant. If current closeout violates it, that's a code bug to file separately, not a reason to soften the Law. |
| Iron Laws section makes /closeout SKILL.md ~620+ lines | Within design budget; comparable to /ship post-strengthening. |
| Cross-references to 4 upstream Iron Laws make /closeout densely linked | Acceptable. /closeout IS the verification step — it should explicitly reference what it verifies. Density is the point. |

---

## Decision points (resolved)

1. **Law count** — 3 (matches E.1–E.7 cadence; audit + RFC identified 3 high-leverage gaps with clean overlap)
2. **Check count in Law 1** — 5 (audit recommended 5: review, QA, ship, ledger, success criteria)
3. **Stale-evidence triggers in Law 2** — 3 (head SHA freshness, run_id alignment, time-based staleness as heuristic)
4. **Cross-skill linkage** — Law 1 cites `/frame` Law 1, `/review` Law 2, `/qa` Law 2, `/ship` Law 3. Law 3 cites all owning stages. Coherence checked; closeout is the consumer of every upstream Iron Law.
5. **"Read-only" scope in Law 3** — `.planning/current/*/status.json` of every other stage + owning-stage artifacts + stage transition history. Closeout writes ONLY its own outputs in `.planning/current/closeout/*`.
6. **Schema vs prose** — prose only. Schema work for `run_id` and `stale_audit` deferred.
7. **`/learn` integration** — out of scope; closeout creates `LEARNINGS.md` per current artifact contract; `/learn`'s own discipline is scope of Phase E.9.

---

## Effort

- Brief: ~30 min (this document)
- Implementation: ~25-30 min (edit .tmpl with 3 Iron Laws ~85 lines, regenerate, verify, commit)
- **Total: ~1h** (S effort per RFC)

---

## Implementation kickoff

After this brief commits and the user approves, proceed directly to:

1. Edit `skills/canonical/closeout/SKILL.md.tmpl` with the 3 Iron Laws (verbatim from § "Iron Law design")
2. `bun run gen:skill-docs` (regenerates canonical Claude + Codex sidecar)
3. `bun run gen:skill-docs --host factory && bun run gen:skill-docs --host gemini-cli`
4. `bun run skill:check` — confirm clean across all 4 hosts
5. Verify Iron Laws appear at expected position in `skills/canonical/closeout/SKILL.md`
6. Commit canonical .tmpl + .md (sidecars are gitignored)
7. Push to main
8. Close #110 with completion comment

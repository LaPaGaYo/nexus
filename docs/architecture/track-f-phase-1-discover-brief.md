# Track F Phase 1 Brief: Darwin upgrade for `/discover` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**
**Darwin baseline:** 66.8/100 (lowest of canonical 9; bottom-3 in v3 audit).
**Darwin target:** 80–82/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/discover (canonical)" (lines 239–273).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md` (commit `635a7fa`).
**Issue:** #113.

---

## Why F.1 first

`/discover` has two compounding deficiencies in the Darwin lens:

1. **Lowest baseline (66.8).** Largest absolute upside — every action moves the needle more here than elsewhere.
2. **Missing Completion Advisor footer.** Action 5 (single-skill fix) applies only here; bundling it with Actions 1–4 in F.1 captures the structural-asymmetry win in one phase.

Per audit (lines 239–273):

| Dimension | Score | Issue |
|---|---|---|
| D1 Frontmatter | 6/10 | Description is "still vague and needs clarification" — ambient, not trigger-shaped |
| D2 Workflow Clarity | 4/10 | NO numbered workflow at all (largest dim gap of any Track E skill) |
| D3 Edge Cases | 8/10 | Iron Law 1's 5 anti-patterns saved this dimension |
| D4 Checkpoint Design | 5/10 | Iron Law 1 implies routing decisions but no explicit AskUserQuestion gate |
| D5 Instruction Specificity | 9/10 | Iron Laws 2 & 3 are specific |
| D6 Resource Integration | 6/10 | No links to upstream artifacts (e.g., closeout's NEXT-RUN.md) |
| D7 Overall Architecture | 7/10 | Missing Completion Advisor footer that 9/10 other Track E skills have |
| D8 Real-World Performance | 7/10 | Well-rigorous but hard to actually execute step-by-step |

---

## Surface map

### File modified

```
skills/canonical/discover/SKILL.md.tmpl    ← edit (frontmatter + workflow + typical prompts + AskUserQuestion + footer)
skills/canonical/discover/SKILL.md         ← regenerated by gen:skill-docs
```

### File NOT modified

- `lib/nexus/stage-content/discover/*` — runtime content; not skill prose
- Iron Laws section — preserved verbatim
- All other canonical/support skills — out of scope for this phase

### Frontmatter changes

Description gets trigger phrases appended. `allowed-tools` already has `AskUserQuestion` from Track E.

---

## Action 1 — Numbered workflow design

Insert a new `## How to run /discover` section between the Iron Laws section (ends line 62) and the Operator Checklist (starts line 64).

**Voice (Nexus-native):**

```markdown
## How to run /discover

These steps execute one `/discover` run. Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply.

### Step 1 — Read upstream context

If a prior closeout produced `next-run-bootstrap.json` or `RETRO-CONTINUITY.md` for this run, read them first. They carry seeded discovery context and prior open questions; ignoring them duplicates work the team already did.

If no prior closeout context exists, this is a fresh discovery run — proceed to Step 2.

### Step 2 — Capture Look Inward

Before gathering any evidence, write the team's current beliefs into a draft `idea-brief.md` `## Look Inward` section (per Law 3). Capture:
- The problem the team thinks exists
- The user segment the team thinks is affected
- The hypothesis the team is bringing in
- Any prior framings or assumptions the team is operating under

This step exists so confirmation bias is on the record. Skip it and Law 3 fails.

### Step 3 — Detect anti-patterns early

If the captured Look Inward already reads like any of Law 1's 5 anti-patterns (feature spec, user testing, substitute-for-shipping, single-stakeholder, solutioning in disguise), STOP. Use `AskUserQuestion`:

> "The current Look Inward reads like a [feature spec | solutioning | etc.]. Should I (a) reframe as a problem statement, (b) route to `pol-probe` for lightweight validation, or (c) continue and flag in the brief?"

The operator picks; record the choice in the brief.

### Step 4 — Gather Look Outward evidence

Per Law 2 the brief needs ≥2 distinct evidence sources. Sources to consider:
- User interviews (transcripts or notes)
- Support tickets matching the problem space
- Usage metrics or telemetry
- Prior PRDs or design docs
- Market signals (competitor moves, public commentary)
- Customer email threads
- Internal usage observations

Cite each piece of evidence to its source. Two interview transcripts count as two sources; the same email quoted twice does not.

### Step 5 — Synthesize hypothesis hint

From Look Outward findings, draft a one-paragraph "If/Then/Because" hint per Law 2 #4. This is not the final hypothesis — `/frame` Law 2 sharpens it. The hint just commits to a direction the framing pass can refine.

### Step 6 — Enumerate open questions

List ≥3 explicit questions for `/frame` to answer (per Law 2 #5). Examples:
- "What's the falsifiable success metric?"
- "Which user segment ships first?"
- "What's the rollback plan if hypothesis fails?"

If you can't enumerate three, the discovery has overclaimed — return to Step 4 and gather more.

### Step 7 — Write Reframe section (optional but recommended)

If Look Outward changed Look Inward in any way, write a brief `## Reframe` section noting:
- What the team thought going in
- What it concluded after evidence
- Why the change matters

The Reframe is the receipt that discovery actually happened.

### Step 8 — Verify all 5 Law 2 checks pass

Before writing `status.json`, verify the brief has:
1. Named user segment (not "users")
2. Observed pain with concrete cost
3. ≥2 distinct evidence sources cited
4. Hypothesis hint
5. ≥3 open questions

If any fail, return to the relevant step. If all pass, proceed to Step 9.

### Step 9 — Write status

Run the canonical command (in the Routing section below). It writes `idea-brief.md`, `status.json`, and any continuation artifacts. The advisor record carries the verdict; `/frame` reads it.
```

This Action lifts D2 Workflow Clarity from 4/10 to ~8/10 (+4 raw, ×15 = +6 weighted) and D8 Real-World Performance from 7/10 to ~9/10 (+2 raw, ×25 = +5 weighted). Combined: ~+11 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus discovery command. Writes repo-visible discovery artifacts and stage
  status through Nexus-owned normalization and governance. Use when the problem is still
  vague and needs clarification. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus discovery command. Writes repo-visible discovery artifacts and stage
  status through Nexus-owned normalization and governance. Use when the user says
  "I want to explore X", "we should look into Y", "I'm not sure what we should build for Z",
  or when the problem is still vague and needs clarification before /frame can scope it.
  (nexus)
```

Lifts D1 Frontmatter from 6/10 to ~8/10 (+2 raw, ×8 = +1.6 weighted).

Frontmatter character count: still well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the Routing section, before the Completion Advisor footer.

**Voice:**

```markdown
## Typical prompts

These are example user requests `/discover` handles well. The skill is invoked and produces a structured `idea-brief.md` plus `status.json`.

### Prompt 1 — Vague feature idea

> "I think Nexus should have some kind of telemetry, but I don't really know what shape."

`/discover` walks Step 1–9. Look Inward captures the team's prior ("telemetry would help with X"). Look Outward gathers evidence (existing logs? competitor approaches? user requests?). Hypothesis hint emerges. Open questions enumerate what `/frame` must decide. Brief returns `ready` if all 5 Law 2 checks pass.

### Prompt 2 — Reactive problem framing

> "Multiple users are reporting `/build` feels broken when 3-strike fires; I want to understand why before we patch."

Step 1 reads any prior `LEARNINGS.md` for relevant context. Step 2 captures team's current theory. Step 3 detects whether the prompt is already a solution (it isn't — "understand why" is discovery-shaped). Step 4 gathers actual session logs, prior reports. Step 5 produces hypothesis hint. The brief frames the problem rather than proposing fixes.

### Prompt 3 — Strategic exploration

> "Should Nexus expand beyond CCB transport? What would that look like?"

Step 2 captures team's strategic prior. Step 4 gathers competitive landscape, current architectural constraints, prior strategic docs. Hypothesis hint commits to one direction (e.g., "If we add X transport, then Y users will Z because evidence W"). Open questions are aggressive — multiple items the team can't yet answer.

These prompts test that `/discover` produces structured discovery output, not feature specs or implementation plans (Iron Law 1's anti-patterns).
```

Lifts D8 from ~9/10 toward 9.5/10 (already partially captured by Action 1; cumulative gain ≈ +0.5 raw, ×25 = +1.25 weighted).

---

## Action 4 — AskUserQuestion gate

Already covered in Action 1's Step 3. The gate fires when Law 1 anti-patterns are detected at Look Inward. No additional placement needed.

Lifts D4 Checkpoint Design from 5/10 to ~7/10 (+2 raw, ×7 = +1.4 weighted).

---

## Action 5 — Completion Advisor footer

`/discover` is the only canonical Track E skill missing `{{NEXUS_COMPLETION_ADVISOR:discover}}`. Add it at the end of the file:

**Old end of file:**

```markdown
Run:

```bash
{{NEXUS_RUN_COMMAND:discover}}
```
```

**New end of file:**

```markdown
Run:

```bash
{{NEXUS_RUN_COMMAND:discover}}
```

{{NEXUS_COMPLETION_ADVISOR:discover}}
```

Lifts D7 Overall Architecture from 7/10 to ~8.5/10 (+1.5 raw, ×15 = +2.25 weighted).

---

## Projected Darwin score after F.1

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6 | 8 | +2 | +1.6 |
| D2 Workflow Clarity | 4 | 8 | +4 | +6 |
| D3 Edge Cases | 8 | 8 | 0 | 0 |
| D4 Checkpoint Design | 5 | 7 | +2 | +1.4 |
| D5 Instruction Specificity | 9 | 9 | 0 | 0 |
| D6 Resource Integration | 6 | 7 | +1 | +0.5 (+ small lift from Step 1 reading next-run-bootstrap) |
| D7 Overall Architecture | 7 | 8.5 | +1.5 | +2.25 |
| D8 Real-World Performance | 7 | 9 | +2 | +5 |

**Projected total:** 66.8 + 16.75 ≈ **83.5/100** (target was 80–82; F.1 likely overshoots).

---

## Implementation procedure

### Step 1: Edit `skills/canonical/discover/SKILL.md.tmpl`

Apply 5 changes:
1. Frontmatter description (Action 2)
2. Insert `## How to run /discover` between Iron Laws and Operator Checklist (Action 1)
3. Insert `## Typical prompts` after Routing block (Action 3)
4. Action 4 is bundled into Action 1 Step 3 — no separate change
5. Append `{{NEXUS_COMPLETION_ADVISOR:discover}}` at end of file (Action 5)

### Step 2: Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 3: Verify

```bash
bun run skill:check                                                                                  # clean across all 4 hosts
git grep -n "How to run /discover" skills/canonical/discover/SKILL.md                                # 1 hit
git grep -n "Typical prompts" skills/canonical/discover/SKILL.md                                     # 1 hit
git grep -n "Completion Advisor" skills/canonical/discover/SKILL.md                                  # 1 hit (or whatever the resolver outputs)
git grep -n "I want to explore X" skills/canonical/discover/SKILL.md                                 # 1 hit (frontmatter trigger)
```

### Step 4: Commit + push + close issue

```bash
git add skills/canonical/discover/SKILL.md.tmpl skills/canonical/discover/SKILL.md
git commit -m "skills/canonical/discover: Track F.1 Darwin upgrade"
git push
gh issue close 113 --reason completed --comment "F.1 done. Projected Darwin 66.8 → ~83.5/100."
```

---

## Acceptance criteria

This phase is complete when:

1. ✅ Frontmatter description carries trigger phrases ("I want to explore X" / "we should look into Y" / "I'm not sure what we should build for Z")
2. ✅ `## How to run /discover` section inserted between Iron Laws and Operator Checklist with 9 numbered steps
3. ✅ Step 3 includes explicit `AskUserQuestion` text for anti-pattern detection
4. ✅ `## Typical prompts` section with 3 examples added
5. ✅ `{{NEXUS_COMPLETION_ADVISOR:discover}}` template footer appended
6. ✅ Iron Laws preserved verbatim (no changes to Lines 21–62 of the .tmpl)
7. ✅ `bun run skill:check` clean across all 4 hosts
8. ✅ Issue #113 closed with completion comment
9. ✅ Commit message references Track F.1

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First do X, then do Y" | "Step 1 — Read upstream context. If a prior closeout produced `next-run-bootstrap.json`..." |
| AskUserQuestion framing | Vague "ask user" | Explicit options: "AskUserQuestion: 'Reframe / Route to pol-probe / Continue and flag in brief?'" |
| Cross-skill reference | "Then frame it" | "`/frame` Law 2 sharpens it. The hint just commits to a direction the framing pass can refine." |
| Workflow vs Iron Law | Either or both | Both, with explicit relationship: "Iron Laws constrain *what must be true at decision time*; this workflow defines *what to do in what order*. Both apply." |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "I want to explore X", "we should look into Y" |

The numbered workflow is NOT an Iron Law — it's procedure. Don't add "MUST" or "non-negotiable" framing to the steps; the steps are guidance, the Iron Laws are constraints.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Numbered workflow ends up duplicating Iron Law content | Workflow steps reference Iron Laws by name (e.g., "per Law 1", "per Law 2 #4") rather than restating them. The two surfaces stay distinct. |
| 9 steps feel verbose for short discoveries | Steps 1, 7 are explicitly conditional. Step 7 (Reframe) is "MAY". A trivial discovery uses ~4 steps end-to-end. |
| Completion Advisor footer addition might require runtime change | The `{{NEXUS_COMPLETION_ADVISOR:discover}}` template variable already exists in `gen-skill-docs.ts` resolvers (verified via Track E phases that used it). Adding it to /discover is template-only. |
| AskUserQuestion gate in Step 3 could be triggered too aggressively | Voice limits trigger to the 5 specific Law 1 anti-patterns. The AskUserQuestion is the operator's release valve, not an automatic block. |
| Total SKILL.md length grows from ~454 to ~600 lines | Within design budget (canonical Track E skills are 565–586 lines; /discover ends comparable). |

---

## Effort

- Brief: ~25 min (this document)
- Implementation: ~30–45 min (5 edits to one .tmpl file, regenerate, verify, commit)
- **Total: ~1h** (matches RFC's M effort estimate)

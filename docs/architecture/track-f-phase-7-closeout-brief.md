# Track F Phase 7 Brief: Darwin upgrade for `/closeout` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup.**
**Darwin baseline:** 74.5/100.
**Darwin target:** 85–87/100.
**Audit anchor:** `docs/architecture/skill-strength-audit-v3-darwin.md` § "/closeout (canonical)" (lines 307–337).
**Parent RFC:** `docs/architecture/track-f-skill-darwin-upgrade-rfc.md`.
**Issue:** #119.

---

## Why F.7 now

Of the canonical 9, `/closeout` is the seventh-lowest under the Darwin lens (74.5/100). Its rigor profile is already strong — Iron Laws 1, 2, 3 each name concrete files, fields, and failure modes, and D5 Instruction Specificity scores 13.5/15. The shortfall is procedural and operational:

| Dimension | Score | Issue |
|---|---|---|
| D1 Frontmatter | 4.8/8 | Trigger phrase ("Use after `/review`") is present but ambient; doesn't carry user vocabulary |
| D2 Workflow Clarity | 7.5/15 | Iron Law 1's five-check integrity checklist reads close to a workflow but is verification, not procedure — no numbered "what to do in what order" |
| D3 Edge Cases | 9/10 | Already strong (Law 1's 5 checks, Law 2's 3 staleness signals, Law 3's 3 read-only categories) |
| D4 Checkpoint Design | 4.2/7 | No explicit `AskUserQuestion` gate at Law 2's staleness branching point |
| D5 Instruction Specificity | 13.5/15 | Already strong (concrete file names, fields, paths) |
| D6 Resource Integration | 3.5/5 | Already names CLOSEOUT-RECORD, FOLLOW-ON-SUMMARY, NEXT-RUN, learnings — clean |
| D7 Overall Architecture | 12/15 | Already strong (cross-skill reads from `/review`, `/qa`, `/ship`, `/frame`; single-owner principle codified) |
| D8 Real-World Performance | 20/25 | Test prompts execute well, but no in-skill `## Typical prompts` showing the operator how the skill actually runs |

The leverage is concentrated in D2 (workflow), D4 (checkpoint), D8 (typical prompts), and D1 (trigger phrasing) — the four Darwin actions defined by the RFC.

---

## Surface map

### File modified

```
skills/canonical/closeout/SKILL.md.tmpl    ← edit (frontmatter + workflow + typical prompts + AskUserQuestion)
skills/canonical/closeout/SKILL.md         ← regenerated by gen:skill-docs
```

### Files NOT modified

- `lib/nexus/stage-content/closeout/*` — runtime content; not skill prose
- Iron Laws section (lines 20–62) — preserved verbatim, especially Law 3's read-only contract
- All other canonical/support skills — out of scope for this phase
- `lib/nexus/` runtime contracts — not touched

### Frontmatter changes

Description gets trigger phrases appended. `allowed-tools` already contains `AskUserQuestion` from Track E; no change needed there.

---

## Action 1 — Numbered workflow design

Insert a new `## How to run /closeout` section between the Iron Laws section (ends line 62) and the Operator Checklist (starts line 64).

**Voice (Nexus-native, mirrors F.1):**

```markdown
## How to run /closeout

These steps execute one `/closeout` run. Iron Laws constrain *what must be true at decision time* (especially Law 3's read-only boundary — closeout writes only to `.planning/current/closeout/*`); this workflow defines *what to do in what order*. Both apply.

### Step 1 — Read all upstream stage status.json files

Per Law 1 and Law 3, closeout is read-only over upstream stages. Begin by reading every stage's `status.json` from `.planning/current/<stage>/status.json` for `/discover`, `/frame`, `/plan`, `/build`, `/review`, `/qa`, `/ship` (whichever ran for this run). These tell you what the run claims as truth.

Do not modify them. If they look wrong, that is a Law 3 surface-and-route signal, not a closeout edit.

### Step 2 — Run Law 1 integrity checklist (five mandatory verifications)

Walk Law 1's five checks against the read state from Step 1:

1. `/review` artifacts present in `.planning/audits/current/*`; review status.json records `pass_round` ≥ 1 with verdict `pass` or `pass_with_advisories`.
2. `/qa` artifacts present when `/qa` was run, with verdict `ready` or `ready_with_findings`. If `/qa` was not run, capture that fact explicitly for `CLOSEOUT-RECORD.md`.
3. `/ship` `release-gate-record.md` records the Law 3 outcome (merge / keep alive / discard / split). The decision is explicit, not silent.
4. Ledger consistency — every artifact referenced exists; every artifact on disk in canonical paths is referenced.
5. Each `/frame` success criterion is addressed by at least one audit or QA finding. List unaddressed criteria for the "what we did NOT verify" section.

If any check fails, do NOT proceed. Closeout returns `not_archived` and reports which check failed (per Law 1).

### Step 3 — Run Law 2 stale-evidence checks

Independent of Law 1, verify against Law 2's three staleness signals:

1. **Head-SHA staleness** — does any audit in `.planning/audits/current/*` carry a commit SHA older than the current `/build` head SHA?
2. **Run-id mismatch** — do all audits, QA report, and ship artifacts share the same `run_id` field? Mismatched run_ids ≡ artifacts collated from different runs.
3. **Clock staleness** — is any audit older than 7 days even when run_id and SHA align?

If any signal fires, the run is `stale_audit`. Do NOT silently archive.

### Step 4 — Surface staleness via AskUserQuestion (gate)

When Step 3 flags `stale_audit`, do not auto-resolve. Use `AskUserQuestion`:

> "The audit set is stale (head SHA / run_id / 7-day clock — name which fired). Should I (a) rerun `/review` against the current head, (b) accept staleness with a documented note in `CLOSEOUT-RECORD.md` ("audit is N days old; re-verify before re-shipping"), or (c) block archive until upstream re-runs complete?"

The operator picks. Record the choice in the closeout record. Silent acceptance is forbidden (per Law 2's last paragraph).

### Step 5 — Assemble follow-on summary

Walk `.planning/current/closeout/` and any attached follow-on artifacts (e.g., `documentation-sync.md` from `/document-release`). Compose `FOLLOW-ON-SUMMARY.md` and `follow-on-summary.json` so future `/discover`, `/retro`, and `/learn` runs consume one stable index instead of rediscovering raw artifacts.

This is closeout writing to its own outputs only — Law 3 still holds.

### Step 6 — Write learnings if candidates exist

If `/review`, `/qa`, or `/ship` left valid learning-candidate artifacts behind, assemble the canonical run learnings into `LEARNINGS.md` and `learnings.json` under `.planning/current/closeout/`. If no valid candidates are available on a rerun, remove any stale learnings artifacts and ledger references rather than leaving them behind.

If no candidates exist at all, skip this step — do not synthesize learnings from nothing.

### Step 7 — Write CLOSEOUT-RECORD.md

Compose the closeout record with:

- Outcome of all five Law 1 checks (pass / fail / N/A with reason)
- Outcome of all three Law 2 staleness checks
- The Step 4 staleness disposition if it fired
- Artifact ledger (every file referenced exists; every canonical-path file is referenced)
- "What we did NOT verify" section listing any unaddressed `/frame` success criteria

If any Law 1 check failed, set `archive_state: not_archived` and include the routing target (which owning skill to re-enter, per Law 3).

### Step 8 — Write next-run-bootstrap.json (for `/discover` continuation)

If a future `/discover` will resume from this run, write `next-run-bootstrap.json` and the human-readable `NEXT-RUN.md` carrying:

- Run-id, branch, head SHA at archive time
- Open questions left unanswered
- Follow-on items not yet routed
- Any explicit "next discovery should consider" notes

`/discover` Step 1 reads this on the next run. A missing or empty bootstrap is fine — it just means fresh discovery rather than seeded continuation.

### Step 9 — Write status

Run the canonical command (in the Routing section below). It writes `status.json` with the verdict (`archived` / `not_archived` / `stale_audit`). The advisor record carries the verdict; downstream tooling reads it.

If the verdict is anything other than `archived`, the closeout has surfaced a problem — Law 3 says route back to the owning skill, not patch from inside `/closeout`.
```

This Action lifts D2 Workflow Clarity from 7.5/15 to ~12/15 (+4.5 raw weighted, 9 numbered steps with explicit reads/writes per step) and D8 Real-World Performance from 20/25 to ~22/25 (+2 weighted). Combined: ~+6.5 weighted points.

---

## Action 2 — Frontmatter trigger phrases

**Old:**

```yaml
description: |
  Canonical Nexus closeout command. Verifies review completeness, archive state, and
  final governed readiness before concluding the work unit. Use after `/review`. (nexus)
```

**New:**

```yaml
description: |
  Canonical Nexus closeout command. Verifies review completeness, archive state, and
  final governed readiness before concluding the work unit. Use when the user says
  "close out the run", "archive this work", "finalize and prepare next run", or after
  `/review` and `/ship` complete and the work unit needs to be sealed. (nexus)
```

Lifts D1 Frontmatter from 4.8/8 to ~6.5/8 (+1.7 weighted). Triggers are concrete user vocabulary, not aspirational.

Frontmatter character count: still well under the 1024-char limit.

---

## Action 3 — Typical prompts section

Insert a new `## Typical prompts` section after the Run block, before the Completion Advisor footer.

**Voice (mirrors F.1):**

```markdown
## Typical prompts

These are example user requests `/closeout` handles well. The skill is invoked and produces a structured `CLOSEOUT-RECORD.md` plus `status.json` (and learnings/next-run artifacts when relevant).

### Prompt 1 — Clean closeout

> "Close out this run."

`/closeout` walks Step 1–9. Step 1 reads all upstream status.json files. Step 2 confirms Law 1's five checks pass. Step 3 confirms no Law 2 staleness signal fires. Steps 5–7 assemble the follow-on summary, learnings (if candidates exist), and CLOSEOUT-RECORD. Step 9 writes `archive_state: archived`.

### Prompt 2 — Stale audit branching

> "Audit was generated yesterday but I made commits today — ok to archive?"

Step 1 reads upstream state. Step 3 fires the head-SHA staleness signal (audit's commit SHA is older than current `/build` head). Step 4 surfaces the `AskUserQuestion` gate: rerun `/review`, accept staleness with note, or block until upstream re-runs. The operator picks; closeout records the disposition rather than silently archiving (per Law 2).

### Prompt 3 — Inconsistent upstream state

> "`/build` says complete but `/review` says fail — closeout fix it?"

Step 1 reads both status.json files. The conflict is real. Per Law 3, closeout is read-only over upstream stages — it does not patch the inconsistency. Step 7 writes `CLOSEOUT-RECORD.md` with `archive_state: not_archived`, names the conflict, and identifies the owning skill to re-enter (`/build` or `/review`). The operator decides which stage to re-run; closeout does not launder the run by editing upstream artifacts.

These prompts test that `/closeout` runs the integrity + staleness checklist faithfully, surfaces operator choice when staleness fires, and refuses to patch upstream state (Law 3).
```

Lifts D8 from 20/25 toward ~22/25 (already partially captured by Action 1; cumulative gain ≈ +2 weighted).

---

## Action 4 — AskUserQuestion gate (Law 2 stale audit)

Already covered in Action 1's Step 4. The gate fires when any of Law 2's three staleness signals trigger (head SHA / run_id / 7-day clock). The three operator options:

1. Rerun `/review` against the current head
2. Accept staleness with a documented note in `CLOSEOUT-RECORD.md`
3. Block archive until upstream re-runs complete

This is a real new checkpoint — Law 2 already required surfacing staleness; the AskUserQuestion makes the surfacing operator-gated rather than implicit.

Lifts D4 Checkpoint Design from 4.2/7 to ~5.5/7 (+1.3 weighted).

---

## Action 5 — Completion Advisor footer

`/closeout` already has `{{NEXUS_COMPLETION_ADVISOR:closeout}}` at end of file (line 82 of current .tmpl). No change needed. (Action 5 was a `/discover`-only fix in F.1.)

---

## Projected Darwin score after F.7

| Dim | Before | After | Δ raw | Δ weighted |
|---|---|---|---|---|
| D1 Frontmatter | 6/10 | 8/10 | +2 | +1.6 |
| D2 Workflow Clarity | 5/10 | 8/10 | +3 | +4.5 |
| D3 Edge Cases | 9/10 | 9/10 | 0 | 0 |
| D4 Checkpoint Design | 6/10 | 7.5/10 | +1.5 | +1.05 |
| D5 Instruction Specificity | 9/10 | 9/10 | 0 | 0 |
| D6 Resource Integration | 7/10 | 7/10 | 0 | 0 |
| D7 Overall Architecture | 8/10 | 8/10 | 0 | 0 |
| D8 Real-World Performance | 8/10 | 9/10 | +1 | +2.5 |

**Projected total:** 74.5 + ~9.65 ≈ **84–86/100** (target 85–87; F.7 lands at the lower edge of target). The structural margin is smaller than F.1 because closeout already had strong D3/D5/D7 from Track E.

---

## Implementation procedure

### Step 1: Edit `skills/canonical/closeout/SKILL.md.tmpl`

Apply 3 changes:

1. Frontmatter description (Action 2)
2. Insert `## How to run /closeout` between Iron Laws and Operator Checklist (Action 1, includes Action 4's AskUserQuestion in Step 4)
3. Insert `## Typical prompts` after Run block, before the Completion Advisor footer (Action 3)

### Step 2: Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 3: Verify

```bash
bun run skill:check                                                                     # clean across all 4 hosts
git grep -n "How to run /closeout" skills/canonical/closeout/SKILL.md                   # 1 hit
git grep -n "Typical prompts" skills/canonical/closeout/SKILL.md                        # 1 hit
git grep -n "close out the run" skills/canonical/closeout/SKILL.md                      # 1 hit (frontmatter trigger)
git diff -U0 skills/canonical/closeout/SKILL.md.tmpl -- 'Iron Laws' | head            # Iron Laws diff should be empty
```

### Step 4: Commit + push branch

```bash
git add skills/canonical/closeout/SKILL.md.tmpl skills/canonical/closeout/SKILL.md
git commit -m "skills/canonical/closeout: Track F.7 Darwin upgrade"
git push -u origin track-f-phase-7-closeout
```

Issue #119 stays open per phase brief instructions.

---

## Acceptance criteria

This phase is complete when:

1. Frontmatter description carries trigger phrases ("close out the run" / "archive this work" / "finalize and prepare next run")
2. `## How to run /closeout` section inserted between Iron Laws and Operator Checklist with 9 numbered steps
3. Step 4 includes explicit `AskUserQuestion` text for Law 2 staleness branching
4. `## Typical prompts` section with 3 examples added (clean closeout / stale audit / inconsistent upstream state)
5. Iron Laws preserved verbatim (no changes to lines 20–62 of the .tmpl)
6. `bun run skill:check` clean across all 4 hosts
7. Workflow steps explicitly respect Law 3's read-only boundary (Steps 1, 5–8 only write to `.planning/current/closeout/*`)
8. Commit message references Track F.7

---

## Voice constraints

| Pattern | Don't | Do |
|---|---|---|
| Numbered step framing | "First check audits, then check QA" | "Step 1 — Read all upstream stage status.json files. Per Law 1 and Law 3, closeout is read-only over upstream stages..." |
| AskUserQuestion framing | Vague "ask user about staleness" | Explicit options: "(a) rerun `/review` against current head, (b) accept staleness with documented note, or (c) block archive until upstream re-runs" |
| Cross-skill reference | "Then close out" | "`/discover` Step 1 reads this on the next run. A missing or empty bootstrap is fine — it just means fresh discovery rather than seeded continuation." |
| Workflow vs Iron Law | Either or both | Both, with explicit relationship: "Iron Laws constrain *what must be true at decision time* (especially Law 3's read-only boundary — closeout writes only to `.planning/current/closeout/*`); this workflow defines *what to do in what order*. Both apply." |
| Trigger phrases | Aspirational | Concrete user-vocabulary: "close out the run", "archive this work", "finalize and prepare next run" |
| Law 3 respect | Steps that write to upstream paths | Steps 5–8 explicitly write only under `.planning/current/closeout/*` |

The numbered workflow is NOT an Iron Law — it's procedure. Don't add "MUST" or "non-negotiable" framing to the steps; the steps are guidance, the Iron Laws are constraints.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Numbered workflow ends up duplicating Iron Law content | Workflow steps reference Iron Laws by name (e.g., "per Law 1", "per Law 2's three staleness signals", "per Law 3"). The two surfaces stay distinct. |
| Step 4 AskUserQuestion fires too aggressively on routine closeouts | Voice limits trigger to Law 2's three explicit staleness signals (head SHA / run_id / 7-day clock). Clean runs skip Step 4 entirely. |
| Workflow tempts an editor to widen Law 3's read-only contract | Step 1 and the workflow preamble explicitly cite Law 3. Any "fix it from closeout" suggestion in steps would fail Law 3 review. |
| Steps 5/6/7/8 read like four separate writes that complicate routing | Each step writes to its own canonical-path artifact (FOLLOW-ON-SUMMARY, LEARNINGS, CLOSEOUT-RECORD, next-run-bootstrap), all under `.planning/current/closeout/`. The split mirrors what the runtime artifact-contract.md already specifies. |
| Total SKILL.md length grows from ~83 to ~190+ lines | Within design budget; F.1 ended at 192 lines, mirror is fine. |

---

## Effort

- Brief: ~25 min (this document)
- Implementation: ~30 min (3 edits to one .tmpl file, regenerate, verify, commit)
- **Total: ~1h** (matches RFC's S effort estimate for F.7)

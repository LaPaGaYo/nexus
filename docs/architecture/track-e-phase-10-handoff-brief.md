# Track E Phase 10 Brief: strengthen `/handoff` SKILL.md

**Status:** Ready for manual authoring. **Manual track — NOT for Codex auto-pickup** (see RFC § "Track E is its own track").

**Audit rank:** 10 (final phase). **Current discipline score:** 2.4/5 (Mandatory 3, Verification 3, Failure 2, Acceptance 3, Transferability 1).

**Parent RFC:** `docs/architecture/track-e-skill-strengthening-rfc.md` § Phase E.10.
**Anchor data:** `docs/architecture/skill-strength-audit.md` § "handoff (canonical)".
**Voice references:** E.1–E.9 (commits `4209cc4`, `10974ed`, `7fbcb7d`, `52c02fa`, `16fccc8`, `fcbcbbe`, `c5bacef`, `0a90629`, `18143a0`).
**External reference patterns:** GSD `pause-work` + `resume-work` (cross-session context handoff), Superpowers `subagent-driven-development` (task dispatch).
**Issue:** #112.

---

## Goal

Raise `/handoff` SKILL.md prose discipline from 2.4/5 → 4.0+/5 by adding 3 Iron Laws that codify (a) the freshness check that prevents reusing stale handoffs, (b) the recovery protocol when an approved handoff can't execute, (c) the cross-operator context-dump requirement.

After this lands, **Track E is complete** and the skill-strength audit can be re-run to verify aggregate ≥4.0/5.

After E.10:
- An approved handoff cannot be silently reused if route/provider/branch/contract has shifted since approval.
- Blocked handoffs surface explicitly with `handoff-blocked` status and the failing leg named — no silent retry loops.
- Cross-operator handoffs (operator pause + resume, session boundary) carry a context dump in the artifact, not in human memory.

---

## Why this is rank 10 ROI (final)

`/handoff` is the routing approval gate between `/plan` and `/build`. It's appropriately Nexus-specific — handoff governance has no clean upstream counterpart (the closest are GSD `pause-work` and Superpowers `subagent-driven-development`, neither of which solves the same problem). The audit's gap:

> "Handoff is appropriately Nexus-specific and mostly does its job (route provenance, transport availability, separate approval). The weakness is on failure modes: what happens when a handoff is approved but the route then disappears (provider unmounted, branch shifted)? The prose doesn't tell the model how to detect and surface that."

E.10 is a small, focused phase that closes the failure-mode gap. After E.10, every canonical lifecycle stage AND `/learn` carry Iron Laws. The Track E chain is complete.

---

## Current state of `/handoff`

Read `skills/canonical/handoff/SKILL.md.tmpl` (39 lines) and `skills/canonical/handoff/SKILL.md` (auto-generated, ~521 lines).

Per audit:
- **Mandatory 3/5** — checklist enforces route + transport + separate approval
- **Verification 3/5** — runtime advisor checks route availability; prose doesn't enumerate WHAT to verify on re-use
- **Failure 2/5** — no handoff-blocked taxonomy; no recovery protocol
- **Acceptance 3/5** — `governed-handoff.md` is named in artifact contract but not specified for failure cases
- **Transferability 1/5** — entirely Nexus-specific governance

**Current Operator Checklist (from `lib/nexus/stage-content/handoff/checklist.md`)**:
1. record requested route
2. validate transport availability
3. require separate Nexus approval

That's it. Three bullets covering happy-path approval. No coverage of stale handoffs, blocked handoffs, or cross-operator continuity.

---

## Surface map

### File modified

```
skills/canonical/handoff/SKILL.md.tmpl         ← edit (insert Iron Laws section)
skills/canonical/handoff/SKILL.md              ← regenerated by gen:skill-docs
```

### File NOT modified

- `lib/nexus/stage-content/handoff/*` — runtime content; not skill prose
- `.planning/current/handoff/*` — artifacts; not affected by prose
- `/plan` and `/build` SKILL.md.tmpl — Iron Laws here REFERENCE these (`/plan` Law 3 binding contract; `/build` Law 1 evidence) but don't modify them

### Frontmatter note

`/handoff` has only `Bash` and `Read` in `allowed-tools` — no `AskUserQuestion`. Adding `AskUserQuestion` enables Law 2's blocked-recovery escalation path. **Decision: add `AskUserQuestion` to frontmatter.**

---

## Iron Law design

### Law 1 — Handoff Freshness Check (Route + Provider + Branch + Contract)

**Discipline absorbed from:** Audit recommendation #1 + RFC sketch #1.

**Voice (Nexus-native):**

```markdown
### Law 1 — Handoff Freshness Check (Route + Provider + Branch + Contract)

Before reusing an existing handoff approval (e.g., re-entering `/handoff` after a `/build` failure or a session resume), `/handoff` MUST re-validate four dimensions. ANY change invalidates the prior approval:

1. **Route** — the dispatch target named in `governed-execution-routing.md`. If the route mentioned a specific stage pack, provider lane, or topology and any of those shifted since approval, the prior approval is stale.
2. **Provider availability** — the provider lane (CCB session, Codex CLI, Gemini CLI, local) named in the route MUST currently be mounted and online. A provider that was online at approval but is now unmounted invalidates the approval.
3. **Branch state** — the git branch the handoff approved against is the same branch present now. Branch renames, force-push to the same branch name, or operator-initiated branch switches all invalidate.
4. **Upstream contract** — `sprint-contract.md` (per `/plan` Law 3 binding handoff) is unchanged since approval. New tasks added, existing tasks modified, or scope-changing edits all invalidate. The handoff approves a specific contract; a different contract is a different handoff.

The 24-hour soft trigger: any handoff approval older than 24 hours is automatically re-validated against the four dimensions above, regardless of whether the operator believes nothing has changed. Time alone is not invalidating — change is — but 24h is the heuristic that always re-checks.

If any dimension fails, `/handoff` writes `governed-handoff.md` with `status: stale_approval` listing the failed dimension(s), and the operator either re-approves explicitly or routes back to `/plan` to refresh the upstream contract.

Stale handoff approval is the failure mode behind "we approved this two days ago and now it's running against a different tree." The freshness check makes that impossible.
```

This Law turns "approval is forever" into "approval is fresh."

### Law 2 — Recovery From Blocked Handoff (No Silent Retry)

**Discipline absorbed from:** Audit recommendation #2 + RFC sketch #2.

**Voice (Nexus-native):**

```markdown
### Law 2 — Recovery From Blocked Handoff (No Silent Retry)

When `/handoff` records approval but governed execution cannot begin, `/handoff` MUST write a `handoff-blocked` status entry to `status.json` rather than silently re-running:

1. **Identify the failing leg** — which dimension blocked execution: `route_unavailable` | `provider_offline` | `branch_drift` | `contract_mismatch` | `transport_error` | `approval_revoked`. Generic "couldn't continue" is not enough; the failing leg is named.
2. **Surface the blocker via `AskUserQuestion`** — the operator decides recovery: re-approve with the new state, route back to the owning skill (`/plan` for contract issues, the provider mount step for transport issues, etc.), or abort the run. `/handoff` does not pick the recovery path.
3. **Record the recovery decision in `governed-handoff.md`** — what blocked, what the operator chose, when. The artifact carries the recovery context for future operators (per Law 3).
4. **NEVER silently retry** — if the prior approval was for state A and the current state is B, attempting to "just run it again" risks executing against B with an approval that meant A. Silent retry is how mismatched approvals execute against drifted state.

A blocked handoff is not a small failure mode. The handoff is the contract `/build` reads when it begins; if that contract was approved against a state that no longer exists, what `/build` reads is fiction. Surfacing the block is cheaper than discovering the fiction during execution.
```

This Law makes "stuck handoff" a visible artifact rather than a silent retry loop.

### Law 3 — Cross-Operator Context Dump

**Discipline absorbed from:** GSD `pause-work` + `resume-work` (cross-session continuity) + RFC sketch #3.

**Voice (Nexus-native):**

```markdown
### Law 3 — Cross-Operator Context Dump

When a `/handoff` will be consumed by a different operator (different person, different session, different agent provider) than approved it, `governed-handoff.md` MUST carry enough context for the receiving operator to begin without spelunking. At minimum:

1. **What was decided** — the route, the rationale for choosing this route over alternatives, the relevant constraints. Not just "use Codex"; "use Codex because the work touches `lib/nexus/adapters/*` and Codex carries the type-system context this run."
2. **What the receiving operator needs to read first** — explicit pointers to `prd.md` (per `/frame` Law 1), `sprint-contract.md` (per `/plan` Law 1), the design contract if present, and any prior advisor records relevant to the run. The pointer set is the minimum reading list, not "all of `.planning/current/`."
3. **What's already been verified vs what hasn't** — at handoff time, what evidence exists (prior `/build` runs, partial verification) and what the receiving operator must produce fresh. This prevents re-verifying things already verified AND prevents trusting things that weren't actually verified.
4. **Open decisions deferred to the receiving operator** — choices the approving operator did not make and is intentionally passing along, with the option set visible. "Picking between Approach A and B" is a deferred decision; "make it work" is not.

Cross-operator handoffs without context dumps produce sessions where the receiving operator spends 30 minutes reconstructing what the approver knew, and frequently reaches different conclusions about what's been verified. The context dump is the contract between operators, the same way `prd.md` is the contract between framings.

A handoff between the same operator across sessions counts as cross-operator. Memory across sleep cycles is not reliable; the artifact is.
```

This Law treats handoffs as documents, not memos.

---

## Implementation procedure

### Step 1: Read existing `/handoff` SKILL.md.tmpl

Confirm placement: insert Iron Laws section after `{{NEXUS_STAGE_OVERVIEW}}` and before `## Operator Checklist`. Also note: frontmatter currently lacks `AskUserQuestion` — add it (needed for Law 2 escalation path).

### Step 2: Edit `skills/canonical/handoff/SKILL.md.tmpl`

- Add `AskUserQuestion` to `allowed-tools` list in frontmatter
- Insert the 3 Iron Laws section (~85-90 lines of prose) between `{{NEXUS_STAGE_OVERVIEW}}` and `## Operator Checklist`

Voice: use `/handoff`-native concepts (`governed-handoff.md`, `governed-execution-routing.md`, `handoff-blocked`, `stale_approval`, `route` / `provider` / `branch` / `contract` dimensions, explicit `/plan` Law 3 reference) — NOT borrowed GSD framing wholesale.

### Step 3: Regenerate

```bash
bun run gen:skill-docs              # canonical Claude
bun run gen:skill-docs --host codex
bun run gen:skill-docs --host factory
bun run gen:skill-docs --host gemini-cli
```

### Step 4: Verify

```bash
bun run skill:check                                                                                                             # clean across all 4 hosts
git grep -n "Iron Law" skills/canonical/handoff/SKILL.md                                                                        # 1 hit (heading)
git grep -n "Handoff Freshness Check\|Recovery From Blocked Handoff\|Cross-Operator Context Dump" skills/canonical/handoff/SKILL.md  # 3 hits
```

### Step 5: Commit

```bash
git add skills/canonical/handoff/SKILL.md.tmpl skills/canonical/handoff/SKILL.md
git commit -m "skills/canonical/handoff: add Iron Laws section (Track E.10)"
```

---

## Acceptance criteria

This phase is complete when:

1. ✅ `skills/canonical/handoff/SKILL.md.tmpl` has 3 Iron Laws inserted between `{{NEXUS_STAGE_OVERVIEW}}` and `## Operator Checklist`
2. ✅ `AskUserQuestion` added to `allowed-tools` frontmatter (needed for Law 2 escalation path)
3. ✅ `skills/canonical/handoff/SKILL.md` is regenerated and contains the same 3 Iron Laws
4. ✅ Iron Law text uses `/handoff`-native concepts (`governed-handoff.md`, `governed-execution-routing.md`, `handoff-blocked`, `stale_approval`, four-dimension freshness check, explicit `/plan` Law 3 cross-reference) — NOT direct GSD/Superpowers wording
5. ✅ Each Law fits the pattern: (situation/scope) + (≥3 mandatory rules or ≥3 enumerated items) + (consequence of violation)
6. ✅ Law 2's failing-leg taxonomy enumerates ≥5 specific values (route_unavailable, provider_offline, branch_drift, contract_mismatch, transport_error, approval_revoked)
7. ✅ Law 3's "same operator, different session counts as cross-operator" framing preserved
8. ✅ `bun run skill:check` clean across all 4 hosts
9. ✅ Issue #112 closed with completion comment
10. ✅ Commit message references Track E.10 + final-phase signal

---

## Voice constraints (CRITICAL)

| Pattern | Don't | Do |
|---|---|---|
| Freshness framing | "Re-check before reuse" | "Stale handoff approval is the failure mode behind 'we approved this two days ago and now it's running against a different tree.'" |
| Blocked-recovery framing | "Surface errors" | "If the prior approval was for state A and the current state is B, attempting to 'just run it again' risks executing against B with an approval that meant A. Silent retry is how mismatched approvals execute against drifted state." |
| Context-dump framing | "Document the handoff" | "A handoff between the same operator across sessions counts as cross-operator. Memory across sleep cycles is not reliable; the artifact is." |
| Cross-skill flow | "Tied to plan" | "`sprint-contract.md` (per `/plan` Law 3 binding handoff) is unchanged since approval. … The handoff approves a specific contract; a different contract is a different handoff." |
| Mandatory rule prefix | "Iron Law:" alone | "These three rules apply to every `/handoff` invocation regardless of provider, topology, or run mode. They are short and absolute on purpose — discipline that lives in qualifiers does not survive contact with the LLM at decision time." |
| Failing-leg framing | "Note the error" | "Generic 'couldn't continue' is not enough; the failing leg is named." |

Reuse the meta-justification ("short and absolute on purpose") from E.1–E.9.

---

## Cross-Iron-Law coherence (Track E final view)

After E.10, the Iron Law chain is complete:

```
/discover ✓ → /frame ✓ → /plan ✓ → /handoff ✓ → /build ✓ → /review ✓ → /qa ✓ → /ship ✓ → /closeout ✓
                                                                                                          ↓
                                                                                                      /learn ✓
```

`/handoff` Law 1's four-dimension freshness check explicitly cites `/plan` Law 3 (binding handoff) — closing the plan→build approval boundary. Law 3's context dump references `/frame` Law 1 (PRD content) and `/plan` Law 1 (sprint-contract).

The full Track E chain (28 Iron Laws across 9 canonical + 1 support skill, plus Iron Laws on `/learn` Law 2's named upstream MUST/SHOULD list connecting all of them):

| Stage | Law 1 | Law 2 | Law 3 |
|---|---|---|---|
| `/discover` | What `/discover` is NOT | Minimum signal bar before `/frame` | Look Inward before Look Outward |
| `/frame` | PRD content contract | Hypothesis framing | Framing readiness gate before `/plan` |
| `/plan` | Bite-sized task contract | Hypothesis before plan | Plan→Build handoff is binding |
| `/handoff` | Freshness check | Recovery from blocked | Cross-operator context dump |
| `/build` | Evidence before claims | No fixes without root cause | Prior advisories are address-or-dispute |
| `/review` | Review content contract | Two-round protocol | Reviewer disagreement protocol |
| `/qa` | Finding evidence contract | `/ship`-blocking categories | Stop after 3 same-root-cause failures |
| `/ship` | Pre-merge readiness gate | Refuse to ship broken | Branch decision tree is explicit |
| `/closeout` | Integrity checklist (5 verifications) | Stale evidence rejection | Read-only with respect to lifecycle authority |
| `/learn` | Confidence rubric | When to log a learning | Learning hygiene |

**Track E is complete after E.10 lands.**

---

## Out of scope

- **Schema enforcement at runtime** — this brief covers prose only. `stale_approval`, `handoff-blocked`, `failing_leg` field schemas are runtime concerns; can land as follow-up.
- **The 24h heuristic threshold** — Law 1 cites 24h as the soft re-validation trigger. Adjustable in a follow-up if 24h proves wrong; the rule is "time-based heuristic + change-based hard rule."
- **Multi-leg failure aggregation** — Law 2 names individual failing legs; aggregating multiple failures into a single block (e.g., provider offline AND branch drifted) is procedural, not prose.
- **`/plan` SKILL.md additions for "extending the contract"** — Law 1 references re-routing back to `/plan` when contract changes; mechanics of contract extension live in `/plan` (already strengthened in E.2).
- **Approval-revoked workflow mechanics** — Law 2's `approval_revoked` failing-leg value names the case but doesn't define the revocation flow. That's runtime.

---

## Risk register

| Risk | Mitigation |
|---|---|
| Law 1's 4-dimension check (route + provider + branch + contract) creates re-validation overhead on every `/handoff` reuse | Acceptable. Re-validation overhead is seconds; running against a stale approval is hours of wrong-direction work. The rule trades operator time for run honesty. |
| Law 2's failing-leg taxonomy has 6 values; may not cover all real failure modes | Voice-bake extensibility — taxonomy is "at minimum these values," not "exclusively these values." If a new failure mode emerges, it's named explicitly rather than collapsed into "transport_error." |
| Law 3's "same operator across sessions ≡ cross-operator" framing may surprise operators | Prose explicitly explains this: "Memory across sleep cycles is not reliable; the artifact is." First-time readers learn the framing; the rule has clear rationale. |
| Frontmatter change (adding `AskUserQuestion`) is a small but visible diff | Low risk. Symmetric with most other canonical stages already. The rationale (Law 2 escalation path) is documented in the brief. |
| Iron Laws section adds ~90 lines to a stage that was 521 lines before | Within budget; comparable to /closeout (582 lines) and /ship (586 lines) post-strengthening. /handoff lands at ~610 lines. |

---

## Decision points (resolved)

1. **Law count** — 3 (matches E.1–E.9 cadence; audit + RFC identified 3 high-leverage gaps with overlap that resolved cleanly)
2. **Freshness dimensions in Law 1** — 4 (route, provider, branch, contract). The 24h heuristic is the soft trigger; change is the hard rule.
3. **Failing-leg values in Law 2** — 6 enumerated (route_unavailable, provider_offline, branch_drift, contract_mismatch, transport_error, approval_revoked). Extensible.
4. **Cross-skill linkage** — Law 1 cites `/plan` Law 3; Law 3 references `/frame` Law 1 and `/plan` Law 1 PRD/sprint-contract. Coherence checked.
5. **Frontmatter change** — yes, add `AskUserQuestion` (needed for Law 2 escalation path).
6. **"Same operator, different session" framing** — yes, explicit in Law 3. The rule mirrors GSD pause-work/resume-work intent without borrowing the wording.
7. **Schema vs prose** — prose only. Field schemas deferred.

---

## Effort

- Brief: ~30 min (this document)
- Implementation: ~30 min (frontmatter edit + 3 Iron Laws ~90 lines, regenerate, verify, commit)
- **Total: ~1h** (S effort per RFC; final phase)

---

## Implementation kickoff

After this brief commits and the user approves, proceed directly to:

1. Edit `skills/canonical/handoff/SKILL.md.tmpl`:
   - Add `AskUserQuestion` to `allowed-tools` frontmatter
   - Insert 3 Iron Laws (verbatim from § "Iron Law design")
2. `bun run gen:skill-docs` (regenerates canonical Claude + Codex sidecar)
3. `bun run gen:skill-docs --host factory && bun run gen:skill-docs --host gemini-cli`
4. `bun run skill:check` — confirm clean across all 4 hosts
5. Verify Iron Laws appear at expected position in `skills/canonical/handoff/SKILL.md`
6. Commit canonical .tmpl + .md (sidecars are gitignored)
7. Push to main
8. Close #112 with completion comment that includes the Track E final summary
9. (Optional, follow-up) Re-run skill-strength audit to verify aggregate ≥4.0/5

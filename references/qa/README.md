# QA References

Active source: `references/qa/`

Installed compatibility paths include:

- `$NEXUS_ROOT/qa/templates/qa-report-template.md`
- `$NEXUS_ROOT/qa/references/issue-taxonomy.md`

Setup links the installed `$NEXUS_ROOT/qa/...` paths back to the moved
`references/qa/...` source paths, so generated skills can keep their historical
runtime references while the repository source tree is organized under
`references/`.

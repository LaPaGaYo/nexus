# Design References

Active source: `references/design/`

Installed compatibility path:

- `$NEXUS_ROOT/design/references`

The source files have moved into this directory. Setup still links the
historical installed path so generated design skills can continue to read
`$NEXUS_ROOT/design/references/...` without requiring host-facing prompt
changes.
